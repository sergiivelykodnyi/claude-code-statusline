---
phase: 04-fable-weekly-f-segment
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - README.md
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [FAB-01, FAB-02, FAB-03, FAB-04]

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "README.md shows the Fable segment in the 'What it shows' example ('· Fable 74%/1w (2d:4h:30m)' at the end of line 2), in the legend (one row), and in one short 'Fable weekly segment' note: token read from ~/.claude/.credentials.json or the macOS Keychain item 'Claude Code-credentials' (read-only, never refreshed, never printed); one request per 5 minutes to api.anthropic.com/api/oauth/usage with a 2 s timeout, cached 0600 in ~/.claude/statusline-usage-cache.json, last value kept up to an hour when a refresh fails, then hidden; kill switch STATUSLINE_NO_FABLE=1 (shell or settings.json env); a one-time macOS Keychain 'Always Allow' may appear; in Docker Sandboxes the segment renders when /home/agent/.claude/.credentials.json exists (sbx anthropic secret or /login) and the kit never copies the host token; curl listed under Requirements (FAB-02, FAB-03, FAB-04, D-61, D-63, D-64, D-66)"
    - "Planning docs are reconciled with D-51 (D-52): .planning/REQUIREMENTS.md FAB-01 describes the separate 'Fable pct/1w (countdown)' segment rendered last on line 2 and FAB-04 says 'Fable segment'; .planning/PROJECT.md target layout, whole example, segment definition, validated/active checklist lines and the rate-limit decision row carry the new form plus a 'Layout correction (Phase 4)' note; .planning/ROADMAP.md Phase 4 goal and success criteria 1-3 use the new form and a criterion 5 records the layout correction; no in-segment notation remains in README.md, REQUIREMENTS.md or PROJECT.md; project-brief.md is left untouched as the historical brief (discretion recorded in the SUMMARY) (FAB-01)"
    - "Every edit is scoped: the Phase 1-3 ROADMAP sections, the ROADMAP Plans list and Progress table, the REQUIREMENTS checkboxes and traceability table, the PROJECT.md Phase 2 layout-correction note and project-brief.md stay byte-identical as far as this plan's own edits go (the per-file `git diff -- <file>` touches only the named lines; whole-tree cleanliness is never asserted because 04-03 shares the working tree in the same wave); /bin/bash tests/run.sh stays green because no code changes (D-52)"
    - "ROADMAP.md is the one resource shared with the orchestrator in this wave: its post-04-03 Plans-list checkbox tick (`- [x] 04-03-PLAN.md`) may interleave with this plan; the executor re-reads .planning/ROADMAP.md immediately before its scoped Phase-4-block edit, confines that edit to the Goal and Success Criteria lines, and treats any such tick as not-its-edit — no lost update in either direction (D-52)"
    - "Nothing under the host ~/.claude is created, modified, or re-pointed by the docs work (D-46 carry-over)"
  artifacts:
    - path: "README.md"
      provides: "example line, legend row and the Fable weekly note (token sources, cache, kill switch, Keychain prompt, sandbox credentials), curl in Requirements"
      contains: "STATUSLINE_NO_FABLE"
    - path: ".planning/REQUIREMENTS.md"
      provides: "FAB-01/FAB-04 wording reconciled with D-51"
      contains: "Fable pct/1w"
    - path: ".planning/PROJECT.md"
      provides: "target layout, example, segment definition and decision rows reconciled with D-51 + layout-correction note"
      contains: "Layout correction (Phase 4)"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 goal and success criteria reconciled with D-51"
      contains: "Fable 60%/1w"
  key_links:
    - from: "README.md Fable note"
      to: "kit/files/home/.claude/statusline.sh env inputs"
      via: "documents STATUSLINE_NO_FABLE and the two credential sources the script reads (same names and defaults as the script header's Env inputs block)"
      pattern: "Claude Code-credentials"
    - from: ".planning/REQUIREMENTS.md FAB-01"
      to: ".planning/ROADMAP.md Phase 4 success criterion 1"
      via: "both describe the same separate last 'Fable pct/1w (countdown)' segment (D-51/D-52) so the verifier judges FAB-01 against one form"
      pattern: "Fable pct/1w (countdown)"
  prohibitions:
    - statement: "MUST NOT revert the layout to the in-segment form the brief described or leave the docs describing it — the Fable value is a separate last segment with its own countdown everywhere a reader can see it (D-51, D-52)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT rewrite any planning file wholesale or touch the ROADMAP Plans list / Progress table, the REQUIREMENTS checkboxes / traceability table, or project-brief.md — scoped edits only; the orchestrator owns the plan list and ticks requirements at phase completion (D-52)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT document token handling beyond naming the two read-only sources the script reads and the kill switch — no instructions to paste, copy, export, or refresh tokens, and no mention of the sandbox token's shape (D-62, D-63)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT write under the host ~/.claude or touch tests/sandbox.sh, kit/spec.yaml, kit/files/home/.claude/statusline.sh or tests/run.sh — those are plan 03's / earlier plans' files; this plan is docs only (D-46 carry-over)"
      status: resolved
      verification: judgment
---

<objective>
**As a** reader of this repo — a Claude Code Max subscriber installing the status line on macOS or in a Docker Sandbox — **I want to** see the Fable segment in the README's example and legend and read where its token comes from, how it is cached, and how to switch it off, and I want every planning document to describe the layout the phase actually ships, **so that** the feature needs no tribal knowledge and the verifier judges FAB-01 against one consistent form.

This plan is the documentation half of Phase 4, split out of the live-evidence plan (04-03) so that plan stays small and re-runnable and is the only plan gated on Docker Desktop: the README Fable note + example/legend (D-66) and the D-52 reconciliation of REQUIREMENTS.md, PROJECT.md and ROADMAP.md with the D-51 separate-segment layout. It runs in the same wave as 04-03 on disjoint plan-owned files; 04-03 records the observed sandbox credentials branch in its SUMMARY and never edits README.md. The one shared mutable resource is `.planning/ROADMAP.md`, which the orchestrator also ticks after 04-03 completes — handled by scoped git assertions and a re-read-before-edit rule (flagged assumption 4, Task 2). Wave choice: 04-04 deliberately does NOT depend on 04-03 (which would move it to wave 4 behind the Docker Desktop human gate); the two ROADMAP writes touch different lines (Plans-list checkbox vs Goal/criteria), so re-reading the file immediately before the scoped edit is sufficient and keeps the docs plan independent of Docker being up.

Purpose: D-52 and the documentation half of D-66 are the last reader-facing items; after this plan no document a reader sees describes the superseded in-segment notation.
Output: `README.md`, `.planning/REQUIREMENTS.md`, `.planning/PROJECT.md`, `.planning/ROADMAP.md` (scoped edits only).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-fable-weekly-f-segment/04-CONTEXT.md
@.planning/phases/04-fable-weekly-f-segment/04-RESEARCH.md
@.planning/phases/04-fable-weekly-f-segment/04-PATTERNS.md
@.planning/phases/04-fable-weekly-f-segment/04-01-SUMMARY.md
@.claude/CLAUDE.md
@README.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@kit/files/home/.claude/statusline.sh
</context>

<!-- planner-discipline-allow: f( -->

## Artifacts this phase produces

See 04-01-PLAN.md "Artifacts this phase produces" for the full Phase 4 list. This plan creates:

- `README.md` — example line with `· Fable 74%/1w (2d:4h:30m)`, legend row `Fable 74%/1w (2d:4h:30m)`, new `## Fable weekly segment` section, `curl` in Requirements, a verify-command comment about the live segment
- `.planning/REQUIREMENTS.md` FAB-01/FAB-04 wording; `.planning/PROJECT.md` layout/example/definition/checklist/decision rows + `> Layout correction (Phase 4)` note; `.planning/ROADMAP.md` Phase 4 goal + success criteria 1-3 reworded + criterion 5 (layout correction)

## Flagged assumptions (spec-less edge probe — carried from 04-01)

1. **FAB-01:** this is the plan that lands the D-52 wording; until it runs the verifier judges FAB-01 against the D-51 form (roadmap SC1 "weekly segment followed by the Fable segment"), afterwards against the reconciled ROADMAP criteria.
2. **Research A1** (`settings.json` `env` entries reach the statusline subprocess): the README names both the shell-export form and the `"env"` key of `~/.claude/settings.json` for the kill switch; the shell form always works, and if A1 is later found false the README sentence is the only thing to adjust.
3. **Sandbox credentials branch:** the README Docker sentence covers both branches (renders when `/home/agent/.claude/.credentials.json` exists, hidden otherwise); 04-03's live run records the observed branch in its SUMMARY — there is no wording dependency between the two plans, which is why they share a wave on disjoint files.
4. **Same-wave working tree and the ROADMAP tick:** 04-03 may have uncommitted edits to `tests/sandbox.sh` / `kit/spec.yaml` (and gitignored `tests/out/`) while this plan runs, so every git assertion in this plan is pathspec-scoped (`git diff --stat -- README.md`, `git status --porcelain -- .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/ROADMAP.md`) — never a whole-tree `git diff --stat` / `git status --porcelain`. `.planning/ROADMAP.md` is additionally written by the orchestrator when it ticks `- [ ] 04-03-PLAN.md` → `- [x] 04-03-PLAN.md` in the Phase 4 Plans list after 04-03 completes; that tick touches different lines than Task 2's Goal/criteria edits and may interleave. Task 2 therefore re-reads ROADMAP.md immediately before its scoped Phase-4-block edit, keeps the edit confined to the Goal and Success Criteria lines, and never reverts or re-applies a checkbox state it finds. A `- [x] 04-03-PLAN.md` line (or an orchestrator Progress-table count bump) appearing in `git diff -- .planning/ROADMAP.md` is not this plan's edit and is excluded from the byte-identical judgement.

<tasks>

<task type="auto">
  <name>Task 1: README — Fable segment in the example and legend, the "Fable weekly segment" note (token sources, cache, kill switch, Keychain prompt, sandbox credentials), curl in Requirements</name>
  <files>README.md</files>
  <read_first>README.md (whole file: example block 7-10, sentence 12, legend 16-28, install notes 46-70 incl. the verify command 63-68, Docker section 72-92, Requirements 94-98), kit/files/home/.claude/statusline.sh (post-plan-01 header "Env inputs:" block — the env-var names, defaults, TTL 300 s and grace 3600 s the README must match; get_token's source order), .planning/phases/04-fable-weekly-f-segment/04-RESEARCH.md (§Docs Reconciliation table — exact README lines; §Verified Facts 3-4 — where the token lives on each side; Pitfall 4 — Keychain prompt; §Assumptions A1), .planning/phases/04-fable-weekly-f-segment/04-CONTEXT.md (D-51, D-61, D-63, D-64, D-66; the user's target line-2 example), .planning/phases/04-fable-weekly-f-segment/04-01-SUMMARY.md (the live host line for the README example wording)</read_first>
  <action>
    Use scoped edits only; keep every other README section byte-identical. README.md (D-66):
    - Example block: line 2 of the `text` block becomes `10%/100k/1M · 50%/5h (2h:50m) · 15%/1w (3d:5h:57m) · Fable 74%/1w (2d:4h:30m)` (D-51 layout: the Fable segment is the last peer of line 2).
    - The "Percentages turn yellow…" sentence: extend the hidden-segments list with "no Fable usage data (no OAuth login)" (FAB-04 hide-over-placeholder).
    - Legend: add a row after the `15%/1w (3d:5h:57m)` row: token `` `Fable 74%/1w (2d:4h:30m)` `` — "Fable-specific weekly usage and time to reset, read from the Claude Code OAuth usage data; needs the Claude Code OAuth login (Max/Pro), hidden otherwise".
    - New section `## Fable weekly segment` placed after "Install on the host" and before "Install in Docker Sandboxes", one short paragraph plus a 4-5 item bullet list, covering exactly: the token is read from `~/.claude/.credentials.json` first, then the macOS Keychain item `Claude Code-credentials` (via the `security` CLI) — read-only, never refreshed, never printed (FAB-02, D-60, D-62); one request every 5 minutes to `https://api.anthropic.com/api/oauth/usage` with a 2-second timeout, cached 0600 in `~/.claude/statusline-usage-cache.json`, the last value kept for up to an hour if a refresh fails, then the segment hides (FAB-03, D-56..D-59); kill switch `STATUSLINE_NO_FABLE=1` in the shell or via the `"env"` key of `~/.claude/settings.json` (D-64); macOS may show one Keychain dialog the first time — click "Always Allow" once (D-61); Docker Sandboxes: the segment renders when `/home/agent/.claude/.credentials.json` exists in the sandbox (created by the sbx `anthropic` secret or by `/login` inside the sandbox) and is hidden otherwise — the kit never copies or forwards the host token (D-63).
    - Verify command block: add a comment line after the expected output: `# plus "· Fable NN%/1w (…)" at the end of line 2 when your OAuth credentials are available`.
    - Requirements bullet: `bash 3.2+ (macOS /bin/bash is fine), jq, git, curl`.
    - Do not mention the superseded in-segment notation anywhere in README.md; do not add token-handling instructions beyond naming the two sources (prohibition 3).
    Check: `grep -c 'f(' README.md` prints 0; `git diff --stat -- README.md` lists README.md and `git diff --quiet -- project-brief.md .planning/REQUIREMENTS.md .planning/PROJECT.md` succeeds (this task edits nothing else — scoped pathspecs because 04-03 shares the working tree; never assert whole-tree cleanliness, flagged assumption 4); `/bin/bash tests/run.sh` still green (docs only).
  </action>
  <verify>
    <automated>[ "$(grep -c 'f(' README.md)" = 0 ] && grep -qF '· Fable 74%/1w (2d:4h:30m)' README.md && grep -qF 'STATUSLINE_NO_FABLE=1' README.md && grep -qF 'Claude Code-credentials' README.md && grep -qF '/home/agent/.claude/.credentials.json' README.md && grep -qF 'Always Allow' README.md && grep -qF 'statusline-usage-cache.json' README.md && grep -q '^## Fable weekly segment' README.md && grep -qF 'plus "· Fable NN%/1w' README.md && grep -q 'jq.*git.*curl' README.md && [ "$(grep -ciE 'sk-ant|paste (your|the|a) token|TOKEN=' README.md)" = 0 ] && git diff --quiet -- project-brief.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'f(' README.md` outputs 0
    - `grep -cF '· Fable 74%/1w (2d:4h:30m)' README.md` outputs at least 2 (example block + legend); `grep -c '^## Fable weekly segment' README.md` outputs 1; README.md contains each of: `~/.claude/.credentials.json`, `Claude Code-credentials`, `api.anthropic.com/api/oauth/usage`, `statusline-usage-cache.json`, `STATUSLINE_NO_FABLE=1`, `Always Allow`, `/home/agent/.claude/.credentials.json`, `"env"`; `grep -c 'jq.*git.*curl' README.md` outputs at least 1; `grep -cF 'plus "· Fable NN%/1w' README.md` outputs 1
    - `grep -ciE 'sk-ant|paste (your|the|a) token|TOKEN=' README.md` outputs 0 (no pasted token, no paste-your-token or export-TOKEN= instructions — prohibition 3; the prescribed wording only names the two read-only sources)
    - The new section sits between `## Install on the host` and `## Install in Docker Sandboxes` (compare `grep -n '^## ' README.md` line numbers)
    - `git diff --stat -- README.md` lists README.md (the only file this task edits); `git diff --quiet -- project-brief.md .planning/REQUIREMENTS.md .planning/PROJECT.md` succeeds (those are untouched by this task); no whole-tree `git diff --stat` / `git status --porcelain` assertion — 04-03's uncommitted files may be present in the same wave (flagged assumption 4); `/bin/bash tests/run.sh` exits 0
  </acceptance_criteria>
  <done>The README shows the Fable segment in the example and legend and tells users where the token comes from, how the cache behaves, how to switch the segment off, what the Keychain prompt is, and what to expect in sandboxes — without any token-handling instructions.</done>
</task>

<task type="auto">
  <name>Task 2: D-52 reconciliation of REQUIREMENTS.md, PROJECT.md and ROADMAP.md with the D-51 separate-segment layout (scoped edits only)</name>
  <files>.planning/REQUIREMENTS.md, .planning/PROJECT.md, .planning/ROADMAP.md</files>
  <read_first>.planning/REQUIREMENTS.md (lines 36-42 FAB block, traceability rows 117-120), .planning/PROJECT.md (lines 10-25 target layout + example + Phase 2 note, 35-39 segment definitions, 44-62 validated/active checklists, 85-92 decision rows), .planning/ROADMAP.md (the Phase 4 block: goal, research flag, success criteria 1-4, plans list — do not touch the plans list), .planning/phases/04-fable-weekly-f-segment/04-RESEARCH.md (§Docs Reconciliation table — exact lines; §Open Questions (RESOLVED) 3 — ROADMAP is reconciled by this task), .planning/phases/04-fable-weekly-f-segment/04-CONTEXT.md (D-51, D-52; the user's target line-2 example)</read_first>
  <action>
    Use scoped edits only (never rewrite a whole planning file); keep every other phase/section byte-identical.

    1. .planning/REQUIREMENTS.md (D-52): FAB-01 → "Status line shows the Fable 5-specific weekly usage as a separate `Fable pct/1w (countdown)` segment rendered last on line 2, with its own reset countdown (e.g. `15%/1w (3d:5h:57m) · Fable 74%/1w (2d:4h:30m)`) — layout correction (Phase 4): supersedes the brief's in-segment notation"; FAB-04 → replace the in-segment reference with "the Fable segment is hidden". Leave the checkboxes and traceability table untouched (the orchestrator ticks them at phase completion).

    2. .planning/PROJECT.md (D-52): target layout line 15 → `context_usage_pct/context_usage_tokens/window_size · usage_pct/5h (when_reset) · usage_pct/1w (when_reset) · Fable fable_pct/1w (fable_reset)`; whole-example line 22 → `10%/100k/1M · 50%/5h (2h:50m) · 15%/1w (3d:5h:57m) · Fable 74%/1w (2d:4h:30m)` (this also fixes the `50%/1w` typo); add after the Phase 2 note (line 24): `> Layout correction (Phase 4): the Fable weekly value is a separate last segment with its own countdown — \`· Fable 74%/1w (2d:4h:30m)\` — instead of the in-segment form the original brief described.`; segment definition line 39 → "- Weekly limit: `used%/1w (reset countdown)`" plus a new bullet "- Fable weekly: `Fable used%/1w (reset countdown)` — the Fable 5-specific weekly limit, rendered last on line 2 with its own countdown (Phase 4)"; validated line 48 → "(Fable weekly segment added in Phase 4)"; active line 59 → "- [ ] Line 2 additionally renders the Fable 5 weekly percent and reset countdown as a separate `Fable pct/1w (countdown)` segment, last on line 2"; decision row 89 → "only the Fable weekly segment needs the OAuth endpoint (Phase 4)". project-brief.md stays untouched as the historical brief (CONTEXT D-52 discretion; record the choice in the SUMMARY).

    3. .planning/ROADMAP.md Phase 4 block only (scoped edits; do not touch the phase title, the overview paragraph, the Plans list or the Progress table). Shared-resource rule (flagged assumption 4): the orchestrator's post-04-03 Plans-list checkbox tick (`- [x] 04-03-PLAN.md`) may interleave with this task because 04-03 runs in the same wave — it touches different lines — so re-read `.planning/ROADMAP.md` immediately before making this edit (not from an earlier cached read), keep every hunk confined to the Goal line and the Success Criteria lines of the Phase 4 block, and leave whatever checkbox state you find exactly as it is (never revert `[x]` to `[ ]`, never tick anything). Goal → "Line 2 ends with a separate Fable weekly segment `Fable pct/1w (countdown)`, fetched from the OAuth usage data and cached without ever risking or delaying the rest of the line"; criterion 1 → "With valid OAuth credentials on the macOS host (Keychain), line 2 ends like `15%/1w (3d:5h:57m) · Fable 60%/1w (2d:4h:30m)` — the Fable segment is a separate last peer with its own countdown"; criterion 2 → "…and the Fable segment renders the same way"; criterion 3 → "…the Fable segment silently disappears while every other segment renders normally"; add criterion 5: "Layout correction (D-51): the brief's in-segment notation is superseded by the separate `Fable pct/1w (countdown)` segment rendered last on line 2 — REQUIREMENTS.md, PROJECT.md and README reconciled". Leave the Plans list alone (the orchestrator maintains it) — RESEARCH Open Question 3 is resolved this way.

    4. Check: `grep -c 'f(' .planning/REQUIREMENTS.md .planning/PROJECT.md` prints 0 for each; `git status --porcelain -- .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/ROADMAP.md` lists exactly those three files as modified (` M`) and nothing else under that pathspec (README.md was Task 1's and is already committed; whole-tree cleanliness is not asserted — 04-03's files may be dirty in the same wave); `git diff --quiet -- project-brief.md` succeeds; `/bin/bash tests/run.sh` still green (docs only).
  </action>
  <verify>
    <automated>[ "$(grep -c 'f(' .planning/REQUIREMENTS.md)" = 0 ] && [ "$(grep -c 'f(' .planning/PROJECT.md)" = 0 ] && grep -qF 'Fable pct/1w (countdown)' .planning/REQUIREMENTS.md && grep -q 'FAB-04.*Fable segment' .planning/REQUIREMENTS.md && grep -qF 'Layout correction (Phase 4)' .planning/PROJECT.md && grep -qF '· Fable fable_pct/1w (fable_reset)' .planning/PROJECT.md && grep -qF 'Fable 60%/1w (2d:4h:30m)' .planning/ROADMAP.md && grep -qF '5. Layout correction (D-51)' .planning/ROADMAP.md && grep -q '### Phase 4: Fable Weekly f() Segment' .planning/ROADMAP.md && grep -q 'Layout correction (Phase 2)' .planning/PROJECT.md && git diff --quiet -- project-brief.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'f(' .planning/REQUIREMENTS.md` outputs 0; `grep -c 'f(' .planning/PROJECT.md` outputs 0
    - `grep -cF 'Fable pct/1w (countdown)' .planning/REQUIREMENTS.md` outputs at least 1 on the FAB-01 line; `grep -c 'FAB-04.*Fable segment' .planning/REQUIREMENTS.md` outputs 1; the `- [ ] **FAB-0` checkboxes are unchanged (still unticked) and the Traceability table is byte-identical
    - `.planning/PROJECT.md`: `grep -cF '· Fable fable_pct/1w (fable_reset)' .planning/PROJECT.md` outputs 1; `grep -cF '10%/100k/1M · 50%/5h (2h:50m) · 15%/1w (3d:5h:57m) · Fable 74%/1w (2d:4h:30m)' .planning/PROJECT.md` outputs 1; `grep -cF '> Layout correction (Phase 4)' .planning/PROJECT.md` outputs 1; `grep -cF 'Fable weekly: `Fable used%/1w (reset countdown)`' .planning/PROJECT.md` outputs 1; `grep -c 'Layout correction (Phase 2)' .planning/PROJECT.md` still outputs 1
    - `.planning/ROADMAP.md`: `grep -c '### Phase 4: Fable Weekly f() Segment' .planning/ROADMAP.md` outputs 1 (title untouched); `grep -cF 'Fable 60%/1w (2d:4h:30m)' .planning/ROADMAP.md` outputs 1; `grep -cF '5. Layout correction (D-51)' .planning/ROADMAP.md` outputs 1; `grep -c '04-04-PLAN.md' .planning/ROADMAP.md` still outputs 1 and `grep -c '^\*\*Plans\*\*: 4 plans' .planning/ROADMAP.md` outputs 1 (plan list untouched); the Phase 1-3 sections and the Progress table are byte-identical to before as far as this task's edits go (`git diff -- .planning/ROADMAP.md` hunks authored by this task touch only the Goal and Success Criteria lines inside the Phase 4 block; a `- [x] 04-03-PLAN.md` Plans-list tick or a Progress-table count bump made by the orchestrator's post-04-03 bookkeeping may also be present, is not this task's edit, and is excluded from the judgement — flagged assumption 4); ROADMAP.md was re-read immediately before the edit and no checkbox state was changed by this task
    - `git status --porcelain -- .planning/REQUIREMENTS.md .planning/PROJECT.md .planning/ROADMAP.md` prints exactly three ` M` lines (those three files) and nothing else under the pathspec; `project-brief.md` is unchanged (`git diff --quiet -- project-brief.md`); no whole-tree `git diff --stat` / `git status --porcelain` assertion (04-03 shares the working tree in this wave); `/bin/bash tests/run.sh` exits 0
  </acceptance_criteria>
  <done>Every planning description of the layout — REQUIREMENTS FAB-01/FAB-04, PROJECT layout/example/definitions/checklists/decision row, ROADMAP Phase 4 goal and criteria — describes the separate last `Fable pct/1w (countdown)` segment, with a recorded layout-correction note and every other section byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README → users' shells and settings.json | Documents the kill switch env var and the two credential sources the script reads |
| executor → planning documents | Scoped edits to REQUIREMENTS/PROJECT/ROADMAP; a whole-file rewrite would destroy other phases' entries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-15 | Information Disclosure | README instructing users to paste tokens or disable safety | low | mitigate | README names the sources the script reads and the kill switch only; acceptance grep forbids a pasted `sk-ant` token, "paste your token" and `TOKEN=` export instructions; Keychain "Always Allow" is scoped to the `security` CLI item Claude Code created |
| T-04-16 | Tampering | whole-file rewrite of REQUIREMENTS.md / PROJECT.md / ROADMAP.md losing other phases' content | medium | mitigate | scoped edits only (prohibition 2); acceptance asserts the Phase 1-3 ROADMAP sections, Plans list, Progress table, REQUIREMENTS checkboxes and traceability table, PROJECT Phase 2 note and project-brief.md are byte-identical via pathspec-scoped `git diff -- <file>` |
| T-04-18 | Tampering | lost update on `.planning/ROADMAP.md` — shared with the orchestrator's post-04-03 Plans-list tick in the same wave | low | mitigate | Task 2 re-reads ROADMAP.md immediately before its scoped Phase-4-block edit, confines hunks to the Goal/criteria lines, never changes a checkbox state; all git assertions are pathspec-scoped so 04-03's dirty files cannot fail them (flagged assumption 4) |
| T-04-17 | Repudiation | docs claiming behaviour the script does not have (wrong TTL, wrong sources, wrong path) | low | mitigate | Task 1 reads the script header's Env inputs block and get_token first and copies names/defaults from it; the README numbers (5 min, 2 s, 1 h, 0600, cache path) equal FAB_TTL/FAB_MAXTIME/FAB_GRACE/mktemp mode/FAB_CACHE in the script |
| T-04-SC | Tampering | package installs | low | accept | none — docs only |
</threat_model>

<verification>
- README: no in-segment notation; example + legend + `## Fable weekly segment` section + curl in Requirements + verify-command comment present; no token-handling instructions
- REQUIREMENTS FAB-01/FAB-04, PROJECT layout/example/definitions/checklists/decision row + layout-correction note, ROADMAP Phase 4 goal + criteria 1-3 reworded + criterion 5 added; Plans list, Progress table, checkboxes, traceability table, Phase 1-3 sections and project-brief.md byte-identical
- `/bin/bash tests/run.sh` green (docs only); nothing under the host ~/.claude touched
</verification>

<success_criteria>
- D-66 delivered: README example/legend/note documents the feature for both environments
- D-52 delivered: REQUIREMENTS, PROJECT, ROADMAP speak the D-51 layout; the verifier judges FAB-01 against one consistent form
- The plan is docs-only and parallel to 04-03 on disjoint plan-owned files; the shared `.planning/ROADMAP.md` is handled by re-read-before-edit and pathspec-scoped git assertions (no dependency on 04-03's Docker gate)
</success_criteria>

<output>
Create `.planning/phases/04-fable-weekly-f-segment/04-04-SUMMARY.md` when done.
</output>
