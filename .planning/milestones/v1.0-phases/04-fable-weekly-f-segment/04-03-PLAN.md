---
phase: 04-fable-weekly-f-segment
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - tests/sandbox.sh
  - kit/spec.yaml
autonomous: false
requirements: [FAB-01, FAB-02, FAB-04]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "tests/sandbox.sh gains a §5.13 probe that, without ever reading or printing the file's content, records whether /home/agent/.claude/.credentials.json exists in the kit-created sandbox, renders tests/fixtures/full.json through the kit-delivered script INSIDE the sandbox with the Fable path enabled and no URL/credentials override (the sandbox's own token and HTTPS_PROXY egress), strips ANSI locally, and asserts with a fixed check name: credentials present → line 2 ends with '· Fable NN%/1w (…)' (check 'Fable segment renders in sandbox (FAB-02, D-63)'); credentials absent → line 2 contains no 'Fable' (check 'Fable hidden in sandbox without credentials (D-63)') plus an INFO line saying hidden is the correct outcome; the elapsed whole seconds and the sandbox cache file's ls -l mode line are recorded as INFO (FAB-02, D-63)"
    - "The live run of /bin/bash tests/sandbox.sh (sandboxd reachable) writes tests/out/sandbox/EVIDENCE.txt whose last line is 'N checks, 0 failures', whose in-sandbox harness line reports the plan-02 suite green (more than 126 checks, 0 failures, i.e. the Fable probes pass under the sandbox bash/jq/curl too), whose PORT-01 line reports 8 fixtures byte-identical host vs sandbox, and which contains the §5.13 Fable line as PASS — pasted into the SUMMARY (FAB-02 sandbox half, PORT-01 regression)"
    - "kit/spec.yaml's D-33 install guard also checks curl (no-op on the claude-code base image) and 'sbx kit validate kit' still exits 0; tests/sandbox.sh --help still exits 0 before any sbx call"
    - "End-of-phase live check (human): a live claude session on the host and one in the kit sandbox both show the two-line status line with '· Fable NN%/1w (Nd:Nh:Nm)' as the last segment of line 2 (dim label, coloured number), rendering instantly on subsequent messages; where the sandbox has no credentials the line renders normally without it (roadmap SC1/SC2, D-63)"
    - "The plan halts for the human, never fakes evidence: Task 2 is a blocking human-action checkpoint (start Docker Desktop so `sbx ls` exits 0) and Task 3's precondition re-asserts `sbx ls` exits 0 before any sandbox work — when sandboxd is unreachable the executor returns to the checkpoint instead of writing EVIDENCE.txt (Phase 3 precedent 03-03; D-46 carry-over)"
    - "Nothing under the host ~/.claude is created, modified, or re-pointed by the sandbox run (D-46 carry-over)"
  artifacts:
    - path: "tests/sandbox.sh"
      provides: "§5.13 credentials-presence INFO + live in-sandbox Fable render probe with fixed check names; local ESC/strip_ansi helpers"
      contains: "Fable segment renders in sandbox (FAB-02, D-63)"
    - path: "kit/spec.yaml"
      provides: "install guard covering jq, git and curl (D-33)"
      contains: "command -v curl"
  key_links:
    - from: "tests/sandbox.sh §5.13"
      to: "kit/files/home/.claude/statusline.sh inside the sandbox"
      via: "sx \"$NAME\" /bin/bash -c with the kill switch unset and tests/fixtures/full.json on stdin (repo mounted at the same absolute path)"
      pattern: "credentials file:"
    - from: "tests/sandbox.sh §5.7"
      to: "tests/run.sh §11 inside the sandbox"
      via: "the full harness (now including the Fable probes) runs in the sandbox; its summary line is asserted"
      pattern: "harness in sandbox"
  prohibitions:
    - statement: "MUST NOT read, cat, print, copy, or forward the sandbox's or the host's credentials file or token anywhere — the sandbox probe tests presence only (test -f) and the kit ships nothing credential-related (D-63)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT claim sandbox evidence without a live sandbox run — EVIDENCE.txt is produced by tests/sandbox.sh from real sbx exec results with fixed check names; when sandboxd is unreachable the task halts on its precondition instead of faking evidence"
      status: resolved
      verification: judgment
    - statement: "MUST NOT write to, replace, or re-point anything under the host ~/.claude during the sandbox run; starting Docker Desktop / sandboxd is the user's action (the Task 2 human-action checkpoint), never the executor's (D-46 carry-over)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT edit README.md or any planning document in this plan — those files are owned by plan 04 (same wave, disjoint files); the observed sandbox credentials branch is recorded in this plan's SUMMARY only (D-52, D-66)"
      status: resolved
      verification: judgment
---

<objective>
**As a** Claude Code Max subscriber who also works inside Docker Sandboxes, **I want to** see the Fable segment in the sandbox exactly as on the host, proven by re-runnable evidence, **so that** the feature is trustworthy in both environments.

This plan delivers the sandbox half of FAB-02 as re-runnable evidence: `tests/sandbox.sh` §5.13 (credentials-presence + live in-sandbox render), the kit's curl guard, a blocking human-action gate for Docker Desktop (the executor never starts host services), the live sandbox evidence run, and the hand-off of the end-of-phase human check in both environments. The README and planning-doc reconciliation (D-52, D-66) lives in plan 04 — same wave, disjoint files — so this live-evidence plan stays small, re-runnable, and is the only plan gated on Docker Desktop (`autonomous: false`, Phase 3 precedent 03-03).

Purpose: roadmap SC2 is the last live item; after this plan the verifier has EVIDENCE.txt and a live-check script for the human.
Output: `tests/sandbox.sh` (+ §5.13), `kit/spec.yaml` (+ curl guard), `tests/out/sandbox/EVIDENCE.txt` (gitignored evidence).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-fable-weekly-f-segment/04-CONTEXT.md
@.planning/phases/04-fable-weekly-f-segment/04-RESEARCH.md
@.planning/phases/04-fable-weekly-f-segment/04-PATTERNS.md
@.planning/phases/04-fable-weekly-f-segment/04-01-SUMMARY.md
@.planning/phases/04-fable-weekly-f-segment/04-02-SUMMARY.md
@.planning/phases/03-install-dual-environment-validation/03-03-SUMMARY.md
@.claude/CLAUDE.md
@tests/sandbox.sh
@kit/spec.yaml
</context>

## Artifacts this phase produces

See 04-01-PLAN.md "Artifacts this phase produces" for the full Phase 4 list. This plan creates:

- `tests/sandbox.sh` — §5.13 with `INFO credentials file: present|absent`, check names `Fable segment renders in sandbox (FAB-02, D-63)` / `Fable hidden in sandbox without credentials (D-63)`, `INFO sandbox Fable render: <seconds>s`, `INFO sandbox cache: <ls -l line>`; local `ESC` and `strip_ansi` helpers
- `kit/spec.yaml` — `curl` in the D-33 guard and apt fallback, description updated
- `tests/out/sandbox/EVIDENCE.txt`, `tests/out/sandbox/run.log`, `tests/out/{host,sandbox}/repo/*.out` (8 fixtures) — gitignored evidence
- (README.md and the planning-doc reconciliation are plan 04's artifacts — see 04-04-PLAN.md)

## Flagged assumptions (spec-less edge probe — carried from 04-01)

1. FAB-01 stays an explicit assumption until plan 04 (same wave, disjoint files) lands the D-52 wording; the verifier reads the reconciled ROADMAP criteria. This plan never edits README.md or the planning docs — it records the observed sandbox credentials branch in its SUMMARY; plan 04's README Docker sentence already covers both branches.
2. The sandbox credentials file was present (0600, `agent`-owned, proxy-scoped 26-char token, endpoint reachable through HTTPS_PROXY in 408 ms) when research probed `statusline-kit-test` on 2026-08-22; `tests/sandbox.sh` recreates that sandbox from the user's sbx configuration (global `anthropic` secret), so "present → PASS" is the expected branch. If the file is absent at run time the hidden branch is the correct outcome (D-63) and the README sentence already covers it.
3. `sbx` talks to the local sandboxd unix socket and pulls the claude-code image over the network — per the project memory notes both need the executor's command to run outside this agent's dev sandbox (`dangerouslyDisableSandbox`); inside the dev sandbox `sbx ls` returns rc=1 even when Docker Desktop is up, so the reachability check itself must run outside it. Docker Desktop is started by the user, never by the executor — hence `autonomous: false`: Task 2 is a `checkpoint:human-action` (`gate="blocking-human"`) asking the user to start Docker Desktop and confirm `sbx ls` exits 0, and Task 3 re-asserts that fact as a `<precondition>` and halts back to the checkpoint rather than faking evidence (prohibition 2; Phase 3 precedent 03-03). Task 1 never needs the daemon: its verify calls `tests/sandbox.sh --help` (returns before any sbx call) and `sbx kit validate kit` (works with the daemon stopped — Phase 3).

<tasks>

<task type="auto">
  <name>Task 1: tests/sandbox.sh §5.13 — credentials presence + live in-sandbox Fable render probe; curl in the kit's install guard</name>
  <files>tests/sandbox.sh, kit/spec.yaml</files>
  <read_first>tests/sandbox.sh (whole file: header 1-30, preamble 60-70 with EXPECT_SL/SBX_SL, emit/check_eq/check_ok 76-103, sx 105-107, §5.4 test -x probe 171-173, §5.6 INFO line 180-182, §5.7 harness run 185-189, §5.8 sx -e idiom 191-195, §5.10 n_fix loop 203-213, §5.12 end / §6 start 246-269 — the probe goes between them), tests/run.sh (lines 17-20 ESC/strip_ansi to copy; §11 check names), kit/spec.yaml (line 21-22 the D-33 guard), .planning/phases/04-fable-weekly-f-segment/04-RESEARCH.md (§Verified Facts 4 — sandbox credentials/proxy facts, curl 8.18, `nc` absent; §Testability Plan last paragraph — the sandbox additions; §Standard Stack Installation note), .planning/phases/04-fable-weekly-f-segment/04-PATTERNS.md (§tests/sandbox.sh, §kit/spec.yaml), .planning/phases/04-fable-weekly-f-segment/04-CONTEXT.md (D-63)</read_first>
  <action>
    1. tests/sandbox.sh preamble: add `ESC=$(printf '\033')` and the `strip_ansi()` sed helper copied from tests/run.sh lines 17-20 (BSD/GNU-portable on a real escape byte) next to the other helpers.

    2. New `# 5.13 Fable weekly in the sandbox (FAB-02, D-63)` block after §5.12 and before `# --- 6.` (the primary sandbox is still running there; §5.12 only touches the add-sandbox):
       - Presence, never content: `if sx "$NAME" test -f /home/agent/.claude/.credentials.json; then CREDS=present; else CREDS=absent; fi` then `emit "INFO credentials file: $CREDS (presence only — never read)"`.
       - Live render inside the sandbox through the kit-delivered script with the Fable path ON and no overrides (the sandbox's own token, proxy egress and default cache path `/home/agent/.claude/statusline-usage-cache.json` — that is the sandbox's home, not the host's): `t0=$(date +%s)`; `FAB_RAW=$(sx "$NAME" /bin/bash -c "unset STATUSLINE_NO_FABLE; /bin/bash /home/agent/.claude/statusline.sh < $PWD/tests/fixtures/full.json" 2>/dev/null)`; `t1=$(date +%s)`; `FAB_L2=$(printf '%s\n' "$FAB_RAW" | strip_ansi | sed -n 2p)`; `emit "INFO sandbox Fable render: $(( t1 - t0 ))s  line 2: $FAB_L2"`.
       - Conditional assertion (one counted check either way): when `CREDS=present` → `case "$FAB_L2" in *"· Fable "*"%/1w"*) r=0 ;; *) r=1 ;; esac` and `check_ok "Fable segment renders in sandbox (FAB-02, D-63)" $r`; when absent → `case "$FAB_L2" in *Fable*) r=1 ;; *) r=0 ;; esac`, `check_ok "Fable hidden in sandbox without credentials (D-63)" $r`, and `emit "INFO credentials absent — a hidden Fable segment is the correct outcome (D-63)"`.
       - Cache evidence (mode only): `emit "INFO sandbox cache: $(sx "$NAME" ls -l /home/agent/.claude/statusline-usage-cache.json 2>&1)"` — expected `-rw-------` owned by `agent` when present.
       - Never `cat`, `jq`, `head`, or otherwise read the credentials file; never print tokens; nothing here touches the host `~/.claude`.
       Update the header comment's feature list (line 2-8) to mention the Fable probe, and keep the usage text unchanged.

    3. kit/spec.yaml line 21-22 (D-33): extend the guard to `command -v jq >/dev/null 2>&1 && command -v git >/dev/null 2>&1 && command -v curl >/dev/null 2>&1 || (apt-get update && apt-get install -y --no-install-recommends jq git curl)` and the description to name curl ("Guard the script's dependencies (jq, git, curl) — no-op on the claude-code base image (D-33)"). The kit ships nothing credential-related (D-63).

    4. Verify offline (no daemon needed): `/bin/bash -n tests/sandbox.sh`; `/bin/bash tests/sandbox.sh --help` exits 0 (arg parsing still precedes every sbx call); `sbx kit validate kit` exits 0 (works with the daemon stopped — Phase 3); shellcheck advisory over tests/sandbox.sh is non-failing; `grep -c 'cat .*credentials' tests/sandbox.sh` is 0.
  </action>
  <verify>
    <automated>/bin/bash -n tests/sandbox.sh && /bin/bash tests/sandbox.sh --help > /dev/null && sbx kit validate kit > /dev/null 2>&1 && grep -qF 'Fable segment renders in sandbox (FAB-02, D-63)' tests/sandbox.sh && grep -qF 'Fable hidden in sandbox without credentials (D-63)' tests/sandbox.sh && grep -qF 'test -f /home/agent/.claude/.credentials.json' tests/sandbox.sh && grep -qF 'command -v curl' kit/spec.yaml && [ "$(grep -c 'cat [^|]*credentials' tests/sandbox.sh)" = 0 ] && grep -q '^strip_ansi()' tests/sandbox.sh</automated>
  </verify>
  <acceptance_criteria>
    - `/bin/bash -n tests/sandbox.sh` exits 0; `/bin/bash tests/sandbox.sh --help` exits 0 and prints the usage line; `test -x tests/sandbox.sh` succeeds
    - `grep -c '^strip_ansi()' tests/sandbox.sh` outputs 1; `grep -cF 'test -f /home/agent/.claude/.credentials.json' tests/sandbox.sh` outputs 1; `grep -cF 'INFO credentials file:' tests/sandbox.sh` outputs 1
    - `grep -cF 'Fable segment renders in sandbox (FAB-02, D-63)' tests/sandbox.sh` outputs 1; `grep -cF 'Fable hidden in sandbox without credentials (D-63)' tests/sandbox.sh` outputs 1; `grep -cF 'unset STATUSLINE_NO_FABLE' tests/sandbox.sh` outputs 1; `grep -cF 'statusline-usage-cache.json' tests/sandbox.sh` outputs at least 1
    - `grep -c 'cat [^|]*credentials' tests/sandbox.sh` outputs 0 and `grep -cE 'jq [^|]*credentials|head [^|]*credentials' tests/sandbox.sh` outputs 0 (presence only, never content)
    - `grep -cE 'date -d|readlink -f|sed -i|mapfile|declare -A' tests/sandbox.sh` outputs 0; the `--help` case still appears on a lower line number than the first `sbx ls` call
    - `grep -cF 'command -v curl >/dev/null 2>&1' kit/spec.yaml` outputs 1; `grep -cF 'jq git curl' kit/spec.yaml` outputs 1; `sbx kit validate kit` exits 0; `grep -ci 'credential\|token' kit/spec.yaml` outputs 0
  </acceptance_criteria>
  <done>tests/sandbox.sh carries a credentials-presence + live in-sandbox Fable render probe with fixed check names (never reading the token), the kit guards curl, and both still validate offline.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Start Docker Desktop so sandboxd is reachable for the live sandbox run</name>
  <action>Start Docker Desktop (or otherwise bring sandboxd up) on the macOS host so that `sbx ls` exits 0 — a host-service start the executor must not perform itself (D-46 carry-over; Phase 3 precedent 03-03 Task 2)</action>
  <instructions>
    Task 1 wrote tests/sandbox.sh §5.13 and proved it handles `--help` without contacting the daemon; nothing further in this plan can run until sandboxd answers. `sbx ls` returns rc=1 from inside this agent's dev sandbox even when Docker Desktop is up (the sandboxd socket is blocked there — RESEARCH §Environment Availability), so the executor checks reachability outside it (`dangerouslyDisableSandbox`), and the rule for this phase is that the executor never starts Docker or any other host service.
    The ONE thing to do: make sure Docker Desktop is running (Applications → Docker, or Spotlight) and wait until it reports running; then, from this repo's root, run `sbx ls` and confirm it exits 0 (it auto-starts sandboxd; `sbx daemon start` is the explicit form). If you reach this checkpoint with Docker Desktop already running and `sbx ls` already succeeding, reply immediately.
    Nothing under ~/.claude is touched by this step (D-46).
  </instructions>
  <verification>`sbx ls` exits 0 when the executor runs it from the repo root outside the dev sandbox (read-only; re-asserted as Task 3's precondition before any sandbox work)</verification>
  <resume-signal>Type "done" once Docker Desktop is running and `sbx ls` exits 0</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Live sandbox evidence run (tests/sandbox.sh) and the end-of-phase live-check hand-off</name>
  <precondition>sandboxd is reachable: `sbx ls` exits 0 (Docker Desktop running) — read-only check, run outside this agent's dev sandbox (`dangerouslyDisableSandbox`) because the sandboxd socket is blocked inside it; if it fails, halt and return to the Task 2 checkpoint (ask the user to start Docker Desktop — never start services yourself, never write EVIDENCE.txt by hand); also `git diff --quiet -- kit/files/home/.claude/statusline.sh` succeeds (plan 02 left the script pristine)</precondition>
  <files>tests/out/sandbox/EVIDENCE.txt, tests/out/sandbox/run.log (gitignored evidence written by tests/sandbox.sh — this task changes no tracked file)</files>
  <read_first>tests/sandbox.sh (post-Task-1 §5.13 and the §5.7/§5.10 lines whose evidence is asserted), .planning/phases/03-install-dual-environment-validation/03-03-SUMMARY.md (how the Phase 3 live run was executed and recorded: tool timeout, background + poll pattern, evidence pasted; the `sbx run --name statusline-kit-test` live-check hand-off), .planning/phases/04-fable-weekly-f-segment/04-RESEARCH.md (§Verified Facts 4 — expected sandbox branch; §Environment Availability — sbx/Docker), .planning/phases/04-fable-weekly-f-segment/04-CONTEXT.md (D-63, Integration Points: sbx and commit signing need dangerouslyDisableSandbox; Docker must be running)</read_first>
  <action>
    1. With the Task 2 checkpoint answered and the precondition re-asserted, run `/bin/bash tests/sandbox.sh` from the repo root with the maximum tool timeout (the `sbx create` may pull the claude-code image; if it risks exceeding the limit run it in the background and poll `tests/out/sandbox/EVIDENCE.txt` until the summary line appears). sbx talks to the local sandboxd socket and needs network for the image pull — run it outside this agent's dev sandbox (`dangerouslyDisableSandbox: true`). Do not pass `--rm` (the sandbox stays for the human live check). Do not touch anything under the host `~/.claude` (record `ls -l ~/.claude/statusline.sh` and `jq -c .statusLine ~/.claude/settings.json` before and after — identical, D-46 style).

    2. Assert the evidence: `tail -n 1 tests/out/sandbox/EVIDENCE.txt` matches `^[0-9]+ checks, 0 failures$`; the file contains `PASS harness in sandbox:` with a count greater than 126 and `0 failures` (the plan-02 Fable probes passed under the sandbox bash 5 / jq 1.8 / curl 8.18 — including the file:// fixtures and the proxy-mediated timeout probe); `PASS PORT-01: host vs sandbox fixture renders byte-identical (8 fixtures)`; `INFO credentials file: present` (expected) followed by `PASS Fable segment renders in sandbox (FAB-02, D-63)` and an `INFO sandbox Fable render:` line whose line 2 ends with `· Fable NN%/1w (…)`, and `INFO sandbox cache:` showing `-rw-------` — OR, if credentials were absent, `PASS Fable hidden in sandbox without credentials (D-63)` plus the INFO explaining hidden is correct. Paste the full EVIDENCE.txt into the SUMMARY (it contains percentages and modes only, no tokens).

    3. Record which credentials branch was observed (present → rendered, or absent → hidden) in the SUMMARY. README.md is owned by plan 04 (same wave, disjoint files) and its Docker sentence already covers both branches (the segment renders when `/home/agent/.claude/.credentials.json` exists via the sbx `anthropic` secret or `/login`, and is hidden otherwise) — do NOT edit README.md or any planning document here; if the observation contradicts that sentence, state it in the SUMMARY as a follow-up for the verifier (D-63, D-66).

    4. Hand off the end-of-phase live check (human_verify_mode=end-of-phase — no checkpoint task): the `<human-check>` below is the script; do not attempt to drive `claude` yourself.
  </action>
  <verify>
    <automated>tail -n 1 tests/out/sandbox/EVIDENCE.txt | grep -Eq '^[0-9]+ checks, 0 failures$' && grep -qF 'PASS PORT-01: host vs sandbox fixture renders byte-identical (8 fixtures)' tests/out/sandbox/EVIDENCE.txt && grep -E '^PASS harness in sandbox: [0-9]+ checks, 0 failures' tests/out/sandbox/EVIDENCE.txt | awk '{ if ($5+0 > 126) ok = 1 } END { exit !ok }' && grep -qE '^PASS (Fable segment renders in sandbox \(FAB-02, D-63\)|Fable hidden in sandbox without credentials \(D-63\))' tests/out/sandbox/EVIDENCE.txt && grep -qF 'INFO credentials file:' tests/out/sandbox/EVIDENCE.txt && [ -z "$(git status --porcelain tests/out)" ]</automated>
    <human-check>
      <test>Host: confirm `ls -l ~/.claude/statusline.sh` points at this repo's `kit/files/home/.claude/statusline.sh` (re-run the README `ln -sf` line if it is a stale copy), start `claude` in this repo, send one short message, and look at line 2: it must end with `· Fable NN%/1w (Nd:Nh:Nm)` — dim `Fable`, the number green/yellow/red by threshold, `/1w` and the countdown plain — and keep rendering instantly on later messages (cached; one fetch per 5 minutes). Sandbox: with Docker Desktop running, `sbx run --name statusline-kit-test` (the sandbox tests/sandbox.sh left in place; recreate with `sbx run claude . --kit "$PWD/kit"` if gone), accept any trust prompt, send one short message, and confirm the same Fable segment appears at the end of line 2 (the sandbox has its own `~/.claude/.credentials.json`) — or, if the sandbox has no credentials, that line 2 renders normally without it.</test>
      <expected>Both environments show the two-line status line with the Fable segment last on line 2 (same layout, glyphs, colours as the rest), no lag on subsequent renders, no blank line; the host `~/.claude/settings.json` statusLine object is unchanged.</expected>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `tests/out/sandbox/EVIDENCE.txt` exists; its last line matches `^[0-9]+ checks, 0 failures$`; it contains `PASS sandbox created with kit: statusline-kit-test`, `PASS harness in sandbox:` with a count greater than 126 and `0 failures`, `PASS render dump in sandbox: installed == repo (PORT-04 sandbox half)`, `PASS PORT-01: host vs sandbox fixture renders byte-identical (8 fixtures)`, `INFO credentials file: present` or `absent`, and exactly one of `PASS Fable segment renders in sandbox (FAB-02, D-63)` / `PASS Fable hidden in sandbox without credentials (D-63)`
    - When credentials were present, EVIDENCE.txt's `INFO sandbox Fable render:` line 2 ends with `· Fable ` followed by a number, `%/1w (` and a countdown, and `INFO sandbox cache:` shows `-rw-------`
    - `diff -r tests/out/host/repo tests/out/sandbox/repo` exits 0 and both directories hold 8 `.out` files
    - The SUMMARY pastes EVIDENCE.txt in full, states which credentials branch was observed, records the D-46 before/after lines for `~/.claude/statusline.sh` and the `.statusLine` object (identical), and names the sandbox left running for the live check
    - `git status --porcelain tests/out` prints nothing (gitignored); `git diff --quiet -- kit/files/home/.claude/statusline.sh tests/run.sh tests/sandbox.sh kit/spec.yaml` succeeds after the run (the run changes no tracked file; README.md and the planning docs are plan 04's and are never touched here)
  </acceptance_criteria>
  <done>Re-runnable live evidence proves the Fable segment renders inside the kit sandbox from the sandbox's own credentials (or hides cleanly without them), the plan-02 harness is green under the sandbox toolchain, host/sandbox renders stay byte-identical for all 8 fixtures, and the human live check in both environments is scripted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host script → sandboxd | tests/sandbox.sh creates, execs into, stops and removes sandboxes by name through the local daemon |
| sandbox ~/.claude/.credentials.json → in-sandbox statusline.sh → proxy egress | The sandbox's proxy-scoped token reaches the endpoint through HTTPS_PROXY; the host script only tests the file's presence |
| kit/spec.yaml → sandbox root shell | The install guard runs apt as root at creation |
| evidence files → repo | EVIDENCE.txt and render dumps are gitignored but pasted into the SUMMARY |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-02 | Information Disclosure | sandbox probe printing the credentials file or token | high | mitigate | presence-only `test -f`; acceptance greps forbid cat/jq/head on the file; evidence lines carry percentages and `ls -l` modes only |
| T-04-08 | Tampering | proxy bypass inside the sandbox | medium | mitigate | the in-sandbox render uses the script's defaults (curl honours HTTPS_PROXY); no proxy flags anywhere |
| T-04-10 | Repudiation | evidence claimed without a live sandbox | medium | mitigate | the Task 2 human-action checkpoint and Task 3's precondition both halt when `sbx ls` fails (plan is `autonomous: false`); EVIDENCE.txt is written by the script with fixed check names and pasted into the SUMMARY; prohibition 2 |
| T-04-14 | Tampering | sandbox lifecycle damaging other sandboxes | low | mitigate | tests/sandbox.sh removes only its two named sandboxes (Phase 3 T-03-10 gate stands) |
| T-04-SC | Tampering | `apt-get install jq git curl` in setup.install | low | accept | `command -v` guarded (no-op on the base image); Ubuntu official repos only; no npm/pip/cargo installs anywhere in this phase |
</threat_model>

<verification>
- `/bin/bash tests/sandbox.sh --help` exits 0 offline; `sbx kit validate kit` exits 0; `/bin/bash -n tests/sandbox.sh` exits 0
- Task 2 checkpoint answered (Docker Desktop up, `sbx ls` exits 0) before any sandbox work; Task 3 precondition re-asserts it
- Live run: EVIDENCE.txt ends `N checks, 0 failures`, harness-in-sandbox > 126 checks / 0 failures, PORT-01 8 fixtures identical, Fable probe PASS (present branch expected); pasted into the SUMMARY with the observed credentials branch
- Human live check scripted for host + sandbox; nothing under the host ~/.claude written by the executor; README.md and the planning docs untouched by this plan (plan 04 owns them)
</verification>

<success_criteria>
- Roadmap SC2 evidenced: the token is discovered from `~/.claude/.credentials.json` inside the kit sandbox and the Fable segment renders there (or hides cleanly without credentials — D-63)
- The live-evidence plan is small and re-runnable, gated only on the Docker Desktop human action (`autonomous: false`, Phase 3 precedent); D-52/D-66 docs are delivered in parallel by plan 04
- Phase 4 is re-verifiable: `tests/run.sh` (host + sandbox), `tests/sandbox.sh`, and a two-environment human check
</success_criteria>

<output>
Create `.planning/phases/04-fable-weekly-f-segment/04-03-SUMMARY.md` when done.
</output>
