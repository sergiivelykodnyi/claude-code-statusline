---
phase: 02-git-segment
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [tests/run.sh]
autonomous: true
requirements: [GIT-06, PORT-02]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "tests/run.sh proves the git-state matrix against real temp repos: not-a-repo, clean in-sync, full dirty form (⎇ main* ≡ ↓2 ↑3 #2), boundary-ones (↓1 ↑1 #1), no-upstream, detached, unborn — each by exact ANSI-stripped line-1 equality (GIT-06)"
    - "A clean in-sync repo with zero stashes renders exactly '⎇ main ≡' — zero-value counters and the dirty star provably absent; at counts of exactly one, the segment renders the ones-tokens (GIT-04 and GIT-05 boundary edges: 0 hidden, 1 shown)"
    - "A branch named feature/x-1 renders verbatim in the segment (GIT-02 encoding edge: branch labels are byte-verbatim porcelain output, no truncation or normalization)"
    - "Semantic marker colors are byte-asserted: magenta branch span, yellow star, green has-upstream glyph, red no-upstream glyph, yellow behind token, green ahead token, dim stash token (D-17, D-18, D-19, D-20)"
    - "Ten sequential full renders against a real repo complete within 2 wall-clock seconds measured with date +%s — average ≤200ms per render, well under the ~300ms debounce, with git lookups uncached (PORT-02, D-28, D-29)"
    - "The full harness exits 0 with zero failures and zero stderr bytes from any render"
  artifacts:
    - path: "tests/run.sh"
      provides: "git-state regression matrix, temp-repo helpers, timed latency assertion, git-render palette-purity pass"
      contains: "latency"
  key_links:
    - from: "tests/run.sh"
      to: "statusline.sh"
      via: "jq --arg d mutation of tests/fixtures/full.json pointing workspace.current_dir at each temp repo, piped into the script"
      pattern: "workspace.current_dir"
    - from: "tests/run.sh temp repos"
      to: "git"
      via: "isolated-config git wrapper (tgit) building each repo state under TESTTMP"
      pattern: "GIT_CONFIG_GLOBAL"
---

<objective>
Turn the phase's git states into a permanent regression net in tests/run.sh — real temp repos for every edge state (GIT-06), exact byte assertions for the semantic marker colors (D-17..D-20), the zero/one counter boundary (D-27), and the timed render-latency budget (PORT-02, D-28).

Purpose: Phases 3-4 re-run this harness before touching the render path; the git states and the latency budget must be regression-proof, not one-shot tracer checks.
Output: tests/run.sh extended with temp-repo helpers, seven git-state scenarios, a git-render palette-purity pass, and the latency assertion.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-git-segment/02-CONTEXT.md
@.planning/phases/02-git-segment/02-PATTERNS.md
@tests/run.sh
@statusline.sh
</context>

## Artifacts this phase produces

New symbols this plan creates in tests/run.sh (the source-grounding pass excludes these from drift verification; plan 01 created `seg_git()`, `MAGENTA`, `git_seg` in statusline.sh):

- `tgit()` — git wrapper exporting GIT_CONFIG_GLOBAL=/dev/null and GIT_CONFIG_SYSTEM=/dev/null for hermetic repo building
- `mk_repo()` — creates an isolated repo under $TESTTMP (init, symbolic-ref to main, identity config), echoes its path
- `git_render()` — pipes the dir-mutated full.json payload through statusline.sh, capturing stdout, exit code, and stderr bytes
- `git_line1()` — ANSI-stripped line 1 for a given directory
- `TESTTMP` — mktemp -d root for all temp repos, cleaned by the existing EXIT trap
- New files: none.

## Flagged assumptions (spec-less edge probe)

Unclassified probe rows surfaced as explicit assumptions (never silently dropped):

1. **GIT-06 (unclassified):** Assumed the edge-state universe is the five research-Pitfall-7 states plus the boundary-ones scenario. Bare repos and submodule/worktree exotica are not separately tested — a bare repo makes the porcelain call fail exactly like not-a-repo, so it is covered structurally by the hidden-segment path; flagged for verifier review.
2. **PORT-02 (unclassified):** Assumed the "well under ~300ms" budget is operationalized as 10 sequential renders ≤ 2 whole-clock seconds (average ≤200ms). Per-render sub-millisecond timing is deliberately not measured — GNU date +%s%N is not portable to BSD userland, and the whole-second bound stays flake-proof on both targets (D-28 discretion); flagged for verifier review.

<tasks>

<task type="auto">
  <name>Task 1: Git-state regression matrix with real temp repos</name>
  <files>tests/run.sh</files>
  <read_first>tests/run.sh (check_eq/check_ok helpers, ERRTMP trap lifecycle, run_fixture pattern, strip_ansi, the case-substring raw-byte pattern in sections 5-6, jq --arg mutation pattern), statusline.sh (seg_git byte composition as landed by plan 02-01 — the byte assertions below must match it exactly), .planning/phases/02-git-segment/02-PATTERNS.md (temp-repo and fixture-mutation guidance)</read_first>
  <action>
    Add a git-state matrix section to tests/run.sh between the injection probe and the shellcheck advisory. Every assertion goes through check_eq/check_ok so the CHECKS/FAILS counters stay authoritative.

    Infrastructure first:
    - TESTTMP=$(mktemp -d "${TMPDIR:-/tmp}/statusline-git.XXXXXX") and extend the existing EXIT trap to also rm -rf "$TESTTMP".
    - tgit(): runs git with GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_SYSTEM=/dev/null so host config (gpgsign, hooks, init.defaultBranch) cannot break repo building. Repo-building only — the rendered script itself keeps calling plain git, as it does in production.
    - mk_repo NAME: creates "$TESTTMP/NAME", tgit init -q, tgit symbolic-ref HEAD refs/heads/main (portable main-branch pin, no init -b version dependency), sets user.email/user.name, echoes the path.
    - git_render DIR: builds the payload with jq --arg d "$DIR" '.workspace.current_dir = $d' tests/fixtures/full.json, pipes it into /bin/bash "$SL" with stderr to "$ERRTMP", and records stdout, exit code, and stderr byte count in globals (mirror run_fixture's capture discipline).
    - git_line1 DIR: git_render then strip_ansi, first line.

    Scenarios (exact expected line-1 strings; BASE is each repo's basename so expectations are computed at runtime):
    1. not-a-repo: plain mkdir under $TESTTMP → line 1 equals "Opus 5 (high) · BASE" (GIT-01 hidden segment, no stray space; exit 0, zero stderr bytes).
    2. Progressive repo (one repo named w, advanced through three states — cheap and deterministic):
       a. clean in-sync: bare remote under $TESTTMP, initial commit of a tracked file f, push -q -u origin main → "Opus 5 (high) · w ⎇ main ≡" (proves no dirty star, no counter tokens at zero — GIT-04/GIT-05 boundary low side, D-27).
       b. boundary ones: clone the remote to w2 (own identity config), one empty commit pushed from w2, tgit fetch in w → behind 1; one local empty commit in w → ahead 1; modify f and stash → one stash, tree clean again → "Opus 5 (high) · w ⎇ main ≡ ↓1 ↑1 #1" (GIT-04/GIT-05 boundary high side: exactly one renders).
       c. full form: second pushed commit from w2 plus fetch → behind 2; two more local commits → ahead 3; modify f and stash again → two stashes; touch an untracked file → dirty → "Opus 5 (high) · w ⎇ main* ≡ ↓2 ↑3 #2" (roadmap criterion 1). On this render also assert exit 0 and zero stderr bytes.
    3. no-upstream with verbatim branch name: fresh mk_repo, tgit checkout -q -b feature/x-1, one commit → "Opus 5 (high) · BASE ⎇ feature/x-1 ≢" (GIT-03 red-glyph state; GIT-02 encoding edge — the slashed name must render byte-verbatim).
    4. detached: fresh mk_repo, two commits, tgit checkout -q --detach HEAD~1, SHA=$(tgit rev-parse --short HEAD) → "Opus 5 (high) · BASE ⎇ $SHA" (D-24; exact equality proves no sync glyph and no star, D-25).
    5. unborn: fresh mk_repo with one untracked file → "Opus 5 (high) · BASE ⎇ main* ≢" (D-26).

    Byte assertions on the full-form raw render (case-substring pattern from section 5, using the existing ESC variable) — these pin D-17..D-20 and must match seg_git's composition from plan 02-01 exactly:
    - "${ESC}[35m⎇ main${ESC}[0m${ESC}[33m*${ESC}[0m ${ESC}[32m≡${ESC}[0m ${ESC}[33m↓2${ESC}[0m ${ESC}[32m↑3${ESC}[0m ${ESC}[2m#2${ESC}[0m" present in the raw full-form output (magenta branch span incl. glyph, yellow star, green has-upstream glyph, whole-token yellow behind / green ahead / dim stash).
    - "${ESC}[31m≢${ESC}[0m" present in the raw no-upstream render (red no-upstream glyph, D-19 — the user's explicit choice).

    Palette-purity pass over the raw full-form render: reuse the section-6 loop shape asserting every SGR sequence is in the whitelist including 35m (the section-6 check runs on a non-repo fixture and never sees magenta — this pass is what actually exercises the git colors).
  </action>
  <verify>
    <automated>/bin/bash tests/run.sh</automated>
  </verify>
  <acceptance_criteria>
    - /bin/bash tests/run.sh exits 0 and its final summary line reports 0 failures with a total check count strictly greater than 68 (66 Phase-1 checks + plan-01 additions)
    - The harness output contains a PASS line for each scenario: not-a-repo, clean in-sync, boundary ones, full form, no-upstream verbatim branch, detached, unborn
    - The full-form scenario asserts line 1 equality against "Opus 5 (high) · w ⎇ main* ≡ ↓2 ↑3 #2", plus exit code 0 and 0 stderr bytes for that render
    - The raw-byte checks for the D-17..D-20 color spans and the red no-upstream glyph pass
    - tests/run.sh contains tgit, mk_repo, git_render, and git_line1 definitions: `grep -c 'mk_repo()' tests/run.sh` outputs 1
    - The EXIT trap removes $TESTTMP: `grep 'trap' tests/run.sh | grep -c 'TESTTMP'` outputs 1
  </acceptance_criteria>
  <done>All seven git states are permanent harness checks with exact-equality and byte-level color proofs, temp repos are hermetic (isolated git config) and cleaned on exit, and the whole harness is green.</done>
</task>

<task type="auto">
  <name>Task 2: Timed render-latency budget (D-28)</name>
  <files>tests/run.sh</files>
  <read_first>tests/run.sh (git-state section as landed by Task 1 — reuse the full-form repo and payload; check_ok helper), .planning/phases/02-git-segment/02-CONTEXT.md (D-28: timed assertion requirements, D-29: git stays uncached)</read_first>
  <action>
    Add a latency section after the git-state matrix, before the shellcheck advisory (PORT-02, D-28).

    - Materialize the payload once: jq --arg d "<full-form repo path>" '.workspace.current_dir = $d' tests/fixtures/full.json into a file under $TESTTMP (so jq cost is not measured per iteration — the measured unit is exactly one full render: 1 jq pass inside the script + the git calls).
    - Timing mechanism (portable to BSD and GNU userland — no date +%s%N, no GNU-only flags): t0=$(date +%s); run 10 sequential /bin/bash "$SL" < payload > /dev/null 2>&1 invocations in a plain loop; t1=$(date +%s); elapsed=$((t1 - t0)).
    - Assertion via check_ok with the elapsed value in the check name (e.g. "latency: 10 full renders in ${elapsed}s (budget 2s)"): pass when elapsed is less than or equal to 2. Rationale recorded in a comment: 10 renders ≤ 2s bounds the average at ≤200ms — well under the ~300ms debounce (roadmap criterion 4) — while whole-second granularity plus an expected ~0.5s actual keeps the check flake-proof on slow CI; each render performs real uncached git work per D-29, so this is regression-proof as Phases 3-4 grow the script.
    - Do not add any cache, warm-up render, or timing-dependent skip logic — a genuinely slow script must fail this check.
  </action>
  <verify>
    <automated>/bin/bash tests/run.sh && /bin/bash tests/run.sh | grep -c 'PASS latency' | grep -qx '1' && echo LATENCY-OK</automated>
  </verify>
  <acceptance_criteria>
    - /bin/bash tests/run.sh exits 0 with a PASS line whose name starts with "latency:" and reports the measured elapsed seconds
    - The latency check runs 10 sequential renders against the real full-form temp repo and asserts elapsed ≤ 2 whole seconds via date +%s arithmetic only (no GNU-only date flags anywhere in tests/run.sh)
    - The final harness summary reports 0 failures
  </acceptance_criteria>
  <done>The render-latency budget is a permanent, portable harness check: 10 uncached full renders within 2 wall-clock seconds, failing loudly if the render path ever slows toward the debounce limit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared temp directory → harness | Temp repos and payload files live under a world-writable temp root |
| harness → statusline.sh | The harness feeds constructed payloads into the script under test |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Tampering | temp repo root under /tmp | low | mitigate | mktemp -d creates a mode-700 unpredictable directory; all repos and payloads live inside it; removed via the EXIT trap |
| T-02-06 | Denial of Service | latency check flakiness blocking the harness | low | mitigate | Whole-second bound with ~4x headroom over expected duration; no network, no external services |

No package-manager installs occur in this plan — the package-legitimacy gate does not apply. Test-only changes: statusline.sh is not modified.
</threat_model>

<verification>
- /bin/bash tests/run.sh exits 0: all Phase-1 checks, the seven git-state scenarios, the color-byte checks, the git-render palette-purity pass, and the latency budget all PASS
- Roadmap criteria 1-3 are now regression-proven (not just tracer-proven), and criterion 4 has its timed proof (PORT-02, D-28)
</verification>

<success_criteria>
- Git-state matrix: 7 states asserted by exact line equality against real temp repos, hermetic and self-cleaning
- Zero/one counter boundary proven on both sides (D-27); marker colors byte-proven (D-17..D-20)
- Latency: 10 uncached renders ≤ 2s via portable epoch arithmetic (D-28, D-29)
- Harness remains the single one-command regression net for Phases 3-4
</success_criteria>

<output>
Create `.planning/phases/02-git-segment/02-02-SUMMARY.md` when done.
</output>
