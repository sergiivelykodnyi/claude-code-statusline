---
phase: 04-fable-weekly-f-segment
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/run.sh
  - tests/fixtures/usage/no-bucket.json
autonomous: true
requirements: [FAB-01, FAB-02, FAB-03, FAB-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "/bin/bash tests/run.sh exits 0, its last line reports more than 126 checks and 0 failures, and its output contains every fixed 'PASS fable: …' and 'PASS iso_to_epoch …' line named in this plan's acceptance criteria; the harness stays hermetic — the kill switch is exported globally and every Fable probe passes explicit STATUSLINE_USAGE_URL (file:// fixture or a blackhole address), STATUSLINE_CREDENTIALS_FILE (synthetic file under $TESTTMP or an absent path) and STATUSLINE_USAGE_CACHE (under $TESTTMP), so no probe reads the Keychain, reaches the network, or writes under ~/.claude (D-64, D-65, RESEARCH Pitfall 1)"
    - "Functional probes render the Fable segment through the real script from tests/fixtures/usage/fable.json and assert: the exact line 2 '10%/100k/1M · 50%/5h (now) · 15%/1w (now) · Fable 74%/1w (now)', the dim label and threshold-colour bytes on the number only (69→32m, 70→33m, 89→33m, 90→31m), palette purity, cache mode -rw-------, cache hit without fetch (URL switched to a 42% fixture still renders 74%), stale-grace serve (fetched_at now-400, URL unreachable), past-grace hide (fetched_at now-4000), negative cache for a bucket-less answer and that it suppresses the next fetch, no-credentials hide with no cache write, expired-token hide with no cache write, kill switch over a warm cache, stdin-first with no cache write, Fable alone on line 2 with no-rate-limits.json, missing resets_at → no parens, and a blackhole endpoint hidden within 3 wall-clock seconds with 0 stderr bytes (FAB-01, FAB-03, FAB-04, D-49, D-53..D-60, D-62, D-64)"
    - "Security probes run under /bin/bash (host 3.2.57): injection strings and array payloads on rate_limits.model_scoped (the array itself, display_name, utilization, resets_at) and hostile cache / endpoint bodies (garbage, x[$(touch …)] strings, arrays, 1e100, non-array limits) all exit 0, leave tests/.pwned absent, print 0 stderr bytes, and hide the segment — all 12 @sh-ingested stdin fields are now probed (T-04-03)"
    - "The iso_to_epoch unit table in §2 covers the 17 RESEARCH Pattern 3 rows (UTC, fractional seconds, ±HH:MM and ±HHMM offsets, leap day 2000-02-29, 2028-02-29, century boundary 2100-03-01, epoch 0, year-end, garbage, month 13, empty, date-only, injected string → empty) and passes under /bin/bash"
    - "Every new probe family was shown to bite (Phase 2/3 convention, D-65): the SUMMARY records, for each of the eight listed script mutations, the exact FAIL line(s) the harness printed against the mutated copy, and that the real script was restored (git diff --quiet -- kit/files/home/.claude/statusline.sh succeeds) and the harness is green again"
    - "FAB-04 concurrency (fallback edge probe, explicit): the hostile-cache probes prove that a torn/garbage/hostile cache file hides the segment with exit 0 and 0 stderr and never reaches the arithmetic sinks"
  artifacts:
    - path: "tests/run.sh"
      provides: "iso_to_epoch table (§2), fable_render helper, §11 Fable weekly functional + security probes, updated field-count comment"
      contains: "fable_render"
    - path: "tests/fixtures/usage/no-bucket.json"
      provides: "sanitized OAuth usage response WITHOUT a weekly_scoped entry — the D-49 negative-cache case"
      contains: "weekly_all"
  key_links:
    - from: "tests/run.sh §11"
      to: "kit/files/home/.claude/statusline.sh"
      via: "fable_render CACHE URL CREDS FIXTURE — runs /bin/bash $SL with STATUSLINE_NO_FABLE= and the three overrides, fills FB_OUT/FB_RC/FB_ERRBYTES/FB_L2"
      pattern: "STATUSLINE_USAGE_URL="
    - from: "tests/run.sh §11"
      to: "tests/fixtures/usage/fable.json"
      via: "file://$PWD/tests/fixtures/usage/fable.json as the fake endpoint"
      pattern: "file://\\$PWD/tests/fixtures/usage/fable.json"
    - from: "tests/run.sh §2"
      to: "iso_to_epoch"
      via: "sourced helper table, | -delimited specs"
      pattern: "iso_to_epoch"
  prohibitions:
    - statement: "MUST NOT let any harness probe read the host Keychain, reach the network, or write under the user's real ~/.claude — every Fable probe sets STATUSLINE_USAGE_URL, STATUSLINE_CREDENTIALS_FILE and STATUSLINE_USAGE_CACHE explicitly and the kill switch stays exported for everything else; the single deliberate exception is the executor-time bite run for the explicit-credentials-file rule, which reads the Keychain once by design and may be skipped if a dialog would be disruptive (D-64, D-65)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT commit a real token, a real response with identifiers, or account data into fixtures — synthetic credentials files are generated into $TESTTMP at run time and endpoint fixtures carry numbers only (D-65)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT weaken, rename, or delete any pre-existing check in tests/run.sh — the suite only grows (Phase 2 convention; earlier verifications reference the existing names)"
      status: resolved
      verification: judgment
---

<objective>
**As a** Claude Code Max subscriber, **I want to** trust that the Fable segment renders the right number, caches correctly, and disappears cleanly on every failure, **so that** a change to the script, the endpoint shape, or the cache can never silently slow, blank, or corrupt my status line.

This plan turns the tracer's behaviours into permanent, bite-proven regression checks in `tests/run.sh`: the `iso_to_epoch` table, the functional Fable probes served from `file://` fixtures (cold/warm/grace/negative/kill-switch/stdin-first/timeout/colour), and the security probes (hostile stdin `model_scoped`, hostile cache, hostile endpoint body) under the host `/bin/bash` 3.2 — each family shown to FAIL against a deliberately mutated script and PASS against the real one.

Purpose: D-65 demands that every branch of the adapter is asserted and that every probe bites; this is also what plan 03 runs inside the sandbox to prove cross-environment behaviour.
Output: `tests/run.sh` (+ §2 table, `fable_render`, §11 block, comment update), `tests/fixtures/usage/no-bucket.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-fable-weekly-f-segment/04-CONTEXT.md
@.planning/phases/04-fable-weekly-f-segment/04-RESEARCH.md
@.planning/phases/04-fable-weekly-f-segment/04-PATTERNS.md
@.planning/phases/04-fable-weekly-f-segment/04-01-SUMMARY.md
@.claude/CLAUDE.md
@tests/run.sh
@kit/files/home/.claude/statusline.sh
@tests/fixtures/usage/fable.json
@tests/fixtures/fable-stdin.json
@tests/fixtures/no-rate-limits.json
</context>

## Artifacts this phase produces

See 04-01-PLAN.md "Artifacts this phase produces" for the full Phase 4 list. This plan creates:

- `tests/run.sh` — §2 `iso_to_epoch` table (check names prefixed `iso_to_epoch `); helper `fable_render CACHE URL CREDS FIXTURE` setting globals `FB_OUT`, `FB_RC`, `FB_ERRBYTES`, `FB_L2`; §11 "Fable weekly" block (check names prefixed `fable: `); synthetic files generated at run time under `$TESTTMP`: `creds.json`, `creds-expired.json`, `usage.json` (cache), mutated endpoint fixtures `fable-42.json`, `fable-p<N>.json`, `fable-norst.json`, hostile bodies; updated §7.4 comment (12 fields, pointer to §11)
- `tests/fixtures/usage/no-bucket.json`
- Executor-time only (not committed): the eight mutated script copies used for the bite proofs

## Flagged assumptions (spec-less edge probe — carried from 04-01)

FAB-01 stays an explicit assumption (REQUIREMENTS wording superseded by D-51/D-52; plan 04 reconciles it). The blackhole/timeout probe asserts only the upper wall-clock bound (≤ 3 s) because the failure mode differs per environment: on the host curl waits for `--max-time`, inside the Docker sandbox the egress proxy answers with an error immediately, and inside this agent's dev sandbox the address is simply blocked — all three hide the segment (RESEARCH Pitfall 6).

<tasks>

<task type="auto">
  <name>Task 1: iso_to_epoch table, fable_render helper, and the functional Fable probe block (cold/warm/grace/negative/credentials/kill-switch/stdin/alone/no-parens/timeout/colour)</name>
  <files>tests/run.sh, tests/fixtures/usage/no-bucket.json</files>
  <read_first>tests/run.sh (whole file: §2 tables lines 66-89 — the `|`-delimited spec idiom is at §7.5 lines 236-250; run_fixture 95-105; §4/§5 jq fixture mutation 124-140; §6 palette purity 144-152; §7.3 stderr-bytes 188-195; git_render 276-288 as the helper template; §9 wall-clock bound 403-412; the final Result lines), kit/files/home/.claude/statusline.sh (post-plan-01: get_token's explicit-file rule and expiresAt pre-check, read_cache/write_cache shape, get_fable_weekly order, seg_fable bytes), .planning/phases/04-fable-weekly-f-segment/04-RESEARCH.md (§Pattern 3 — the 17-row iso_to_epoch table with expected epochs; §Testability Plan — the probe table; §Common Pitfalls 1, 2, 6, 7), .planning/phases/04-fable-weekly-f-segment/04-PATTERNS.md (§tests/run.sh — every assertion idiom with line references), tests/fixtures/usage/fable.json, tests/fixtures/fable-stdin.json, tests/fixtures/no-rate-limits.json, .planning/phases/04-fable-weekly-f-segment/04-01-SUMMARY.md</read_first>
  <action>
    Everything below runs under `/bin/bash` (the harness is already `#!/bin/bash` and invokes `/bin/bash "$SL"`); keep bash 3.2 / BSD-GNU-neutral syntax; reuse `check_eq`/`check_ok`, `strip_ansi`, `$ERRTMP`, `$TESTTMP`, the `|`-delimited spec loops, and the `case "$raw" in *"$want"*)` raw-byte idiom. Never weaken or rename an existing check; the suite only grows.

    1. Fixture `tests/fixtures/usage/no-bucket.json`: copy of tests/fixtures/usage/fable.json with the `weekly_scoped` entry removed from `limits` (keep `session` and `weekly_all`); valid JSON; numbers only.

    2. §2 helper tables — add the `iso_to_epoch` table after the `fmt_duration` loop, using a `|`-delimited spec list (ISO strings contain `:`), covering exactly the RESEARCH Pattern 3 rows with their expected epochs: `2026-08-24T18:00:00.097816+00:00|1787594400`, `2026-08-24T17:59:59.939927+00:00|1787594399`, `2026-08-24T18:00:00Z|1787594400`, `2026-08-24T18:00:00+02:00|1787587200`, `2026-08-24T18:00:00-05:30|1787614200`, `2026-08-24T18:00:00+0200|1787587200`, `2000-02-29T00:00:00Z|951782400`, `1970-01-01T00:00:00Z|0`, `2100-03-01T12:34:56.5Z|4107587696`, `2026-02-28T23:59:59Z|1772323199`, `2028-02-29T00:00:00Z|1835395200`, `2026-12-31T23:59:59Z|1798761599`, plus the unparseable rows expecting the empty string: `garbage`, `2026-13-01T00:00:00Z`, the empty string, date-only `2026-08-24`, and the injected string `2026-08-24T18:00:00x[$(touch tests/.pwned)]` (single-quoted in the list; after the loop assert `[ ! -e tests/.pwned ]` as its own check_ok and `rm -f tests/.pwned`). Check names: `iso_to_epoch <iso> -> <want>` (use `<empty>` in the name for the empty expectation).

    3. Helper `fable_render CACHE URL CREDS FIXTURE` (place it at the top of the new §11, mirroring git_render): runs `STATUSLINE_NO_FABLE= STATUSLINE_USAGE_CACHE="$1" STATUSLINE_USAGE_URL="$2" STATUSLINE_CREDENTIALS_FILE="$3" /bin/bash "$SL" < "$4" 2>"$ERRTMP"` and fills `FB_OUT` (raw), `FB_RC`, `FB_ERRBYTES` (wc -c of $ERRTMP, whitespace-trimmed), `FB_L2` (ANSI-stripped line 2). Every probe below goes through it unless stated. Set `FIX_URL="file://$PWD/tests/fixtures/usage/fable.json"`, `NOB_URL="file://$PWD/tests/fixtures/usage/no-bucket.json"`, `CACHE="$TESTTMP/usage.json"`, and write `CREDS="$TESTTMP/creds.json"` = `{"claudeAiOauth":{"accessToken":"test-token","expiresAt":9999999999999}}` and `CREDS_EXP="$TESTTMP/creds-expired.json"` = `{"claudeAiOauth":{"accessToken":"test-token","expiresAt":1000}}` (synthetic, generated at run time — never committed); `NOCREDS="$TESTTMP/none.json"` (never created). "Fresh" below means `rm -f "$CACHE"` first.

    4. New `# --- 11. Fable weekly (FAB-01..04, D-47..D-65) ---` section placed after §10 and before the Result lines (so shellcheck stays last-but-one; the Result summary line stays LAST). Probes, in this order, with these exact check names:
       - Cold render (fresh, FIX_URL, CREDS, stdin tests/fixtures/full.json): `fable: cold render exit code` = 0; `fable: cold render stderr bytes` = 0; `fable: cold render line 2` = `10%/100k/1M · 50%/5h (now) · 15%/1w (now) · Fable 74%/1w (now)`; `fable: cache file mode -rw-------` (`ls -l "$CACHE" | cut -c1-10`); `fable: cache content [true,74,0]` (`jq -c '[.fetched_at > 0, .pct, .resets_at]' "$CACHE"`).
       - Bytes on the cold raw render: `fable: label+threshold bytes pct 74` — raw contains `${ESC}[2mFable${ESC}[0m ${ESC}[33m74%${ESC}[0m/1w`; `fable: palette purity` — §6 idiom over FB_OUT (only 0m/2m/31m..36m).
       - Threshold quartet: for `69:32 70:33 89:33 90:31` write a mutated fixture `$TESTTMP/fable-p$p.json` with `jq --argjson p "$p" '(.limits[] | select(.kind=="weekly_scoped") | .percent) = $p' tests/fixtures/usage/fable.json`, fresh cache, render full.json via `file://$TESTTMP/fable-p$p.json`, assert raw contains `${ESC}[2mFable${ESC}[0m ${ESC}[${code}m${p}%${ESC}[0m/1w` → `fable: threshold bytes pct <p> -> SGR <code>m`.
       - Cache hit: warm the cache with a fresh cold render from FIX_URL (74), then write `$TESTTMP/fable-42.json` (percent 42 via the same jq) and render with `file://$TESTTMP/fable-42.json` → `fable: cache hit (no fetch)`: FB_L2 still contains `Fable 74%/1w (now)` (a fetch would have produced 42).
       - Stale grace: `jq --argjson now "$(date +%s)" '.fetched_at = $now - 400' "$CACHE" > "$CACHE.new" && mv "$CACHE.new" "$CACHE"`, render with URL `file:///nonexistent` → `fable: stale-grace serve`: FB_L2 contains `Fable 74%/1w (now)`. Then `.fetched_at = $now - 4000`, same URL → `fable: past-grace hide`: FB_L2 = `10%/100k/1M · 50%/5h (now) · 15%/1w (now)`; `fable: past-grace stderr bytes` = 0.
       - No resets_at: `$TESTTMP/fable-norst.json` with the Fable bucket's `.resets_at = null`, fresh, render → `fable: no resets_at -> no parens`: FB_L2 = `10%/100k/1M · 50%/5h (now) · 15%/1w (now) · Fable 74%/1w`.
       - No bucket (fresh, NOB_URL): `fable: no bucket hidden`: FB_L2 = `10%/100k/1M · 50%/5h (now) · 15%/1w (now)`; `fable: negative cache written`: `jq -c .pct "$CACHE"` = `null`; then render again with FIX_URL (the VALID fixture) → `fable: negative cache hit (no fetch)`: FB_L2 still without `Fable` (a fetch would have rendered 74).
       - No credentials (fresh, FIX_URL, NOCREDS): `fable: no credentials hidden` (no `Fable` in FB_L2, line 2 equals the three-segment form) and `fable: no credentials no cache` (`test ! -e "$CACHE"`).
       - Expired token (fresh, FIX_URL, CREDS_EXP): `fable: expired token hidden`; `fable: expired token no cache`.
       - Kill switch: warm the cache (fresh cold render, FIX_URL, CREDS), then run `STATUSLINE_NO_FABLE=1 STATUSLINE_USAGE_CACHE="$CACHE" STATUSLINE_USAGE_URL="$FIX_URL" STATUSLINE_CREDENTIALS_FILE="$CREDS" /bin/bash "$SL" < tests/fixtures/full.json` → `fable: kill switch hidden`: stripped line 2 = `10%/100k/1M · 50%/5h (now) · 15%/1w (now)`.
       - Stdin first (fresh, URL `file:///nonexistent`, NOCREDS, stdin tests/fixtures/fable-stdin.json): `fable: stdin-first line 2` = `10%/100k/1M · 50%/5h (now) · 15%/1w (now) · Fable 33%/1w (now)`; `fable: stdin-first no cache` (`test ! -e "$CACHE"`).
       - Alone on line 2 (fresh, FIX_URL, CREDS, stdin tests/fixtures/no-rate-limits.json): `fable: alone on line 2` = `10%/100k/1M · Fable 74%/1w (now)` (D-55 peer rule).
       - Timeout/offline (fresh, CREDS): `t0=$(date +%s)`; run `STATUSLINE_NO_FABLE= STATUSLINE_CURL_MAX_TIME=1 STATUSLINE_USAGE_CACHE="$CACHE" STATUSLINE_USAGE_URL=http://192.0.2.1/ STATUSLINE_CREDENTIALS_FILE="$CREDS" /bin/bash "$SL" < tests/fixtures/full.json 2>"$ERRTMP"`; `t1=$(date +%s)` → `fable: timeout exit code` = 0; `fable: timeout hidden` (line 2 = three-segment form); `fable: timeout stderr bytes` = 0; `fable: timeout wall <= 3s` (`[ $(( t1 - t0 )) -le 3 ]`, upper bound only — RESEARCH Pitfall 6).
       Leave `$CACHE` removed at the end of the block.

    5. Update the tests/run.sh header comment (line 4-6 sentence) to mention the Fable weekly adapter probes (fixture-served endpoint, cache TTL/grace, kill switch, stdin probe). Run `/bin/bash tests/run.sh`: exit 0, more than 126 checks, 0 failures, every name above present as a PASS line, `git status --porcelain` shows only tests/run.sh and the new fixture, nothing under `~/.claude` touched, `test ! -e tests/.pwned`.
  </action>
  <verify>
    <automated>OUT=$(/bin/bash tests/run.sh) && printf '%s\n' "$OUT" | tail -1 | awk '$1 > 126 && $3 == 0 { ok = 1 } END { exit !ok }' && for n in 'fable: cold render line 2' 'fable: cache file mode -rw-------' 'fable: label+threshold bytes pct 74' 'fable: threshold bytes pct 90 -> SGR 31m' 'fable: palette purity' 'fable: cache hit (no fetch)' 'fable: stale-grace serve' 'fable: past-grace hide' 'fable: no resets_at -> no parens' 'fable: no bucket hidden' 'fable: negative cache written' 'fable: negative cache hit (no fetch)' 'fable: no credentials hidden' 'fable: no credentials no cache' 'fable: expired token hidden' 'fable: kill switch hidden' 'fable: stdin-first line 2' 'fable: stdin-first no cache' 'fable: alone on line 2' 'fable: timeout hidden' 'fable: timeout wall <= 3s' 'iso_to_epoch 2026-08-24T18:00:00.097816+00:00 -> 1787594400' 'iso_to_epoch 1970-01-01T00:00:00Z -> 0'; do printf '%s\n' "$OUT" | grep -qF -- "PASS $n" || { echo "missing: $n"; exit 1; }; done && [ "$(printf '%s\n' "$OUT" | grep -c '^PASS iso_to_epoch ')" -ge 17 ] && test ! -e tests/.pwned && jq -e '[.limits[] | select(.kind=="weekly_scoped")] | length == 0' tests/fixtures/usage/no-bucket.json > /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `/bin/bash tests/run.sh` exits 0; its last line reports more than 126 checks and `0 failures`
    - The harness OUTPUT contains each of these exact lines (checked with `grep -qF -- 'PASS <name>'`): `fable: cold render exit code`, `fable: cold render stderr bytes`, `fable: cold render line 2`, `fable: cache file mode -rw-------`, `fable: cache content [true,74,0]`, `fable: label+threshold bytes pct 74`, `fable: threshold bytes pct 69 -> SGR 32m`, `fable: threshold bytes pct 70 -> SGR 33m`, `fable: threshold bytes pct 89 -> SGR 33m`, `fable: threshold bytes pct 90 -> SGR 31m`, `fable: palette purity`, `fable: cache hit (no fetch)`, `fable: stale-grace serve`, `fable: past-grace hide`, `fable: past-grace stderr bytes`, `fable: no resets_at -> no parens`, `fable: no bucket hidden`, `fable: negative cache written`, `fable: negative cache hit (no fetch)`, `fable: no credentials hidden`, `fable: no credentials no cache`, `fable: expired token hidden`, `fable: expired token no cache`, `fable: kill switch hidden`, `fable: stdin-first line 2`, `fable: stdin-first no cache`, `fable: alone on line 2`, `fable: timeout exit code`, `fable: timeout hidden`, `fable: timeout stderr bytes`, `fable: timeout wall <= 3s`
    - `printf '%s\n' "$OUT" | grep -c '^PASS iso_to_epoch '` outputs at least 17, including `PASS iso_to_epoch 2026-08-24T18:00:00.097816+00:00 -> 1787594400`, `PASS iso_to_epoch 2000-02-29T00:00:00Z -> 951782400`, `PASS iso_to_epoch 1970-01-01T00:00:00Z -> 0`, `PASS iso_to_epoch 2026-08-24T18:00:00-05:30 -> 1787614200`, and a row for the injected string expecting `<empty>`; `test ! -e tests/.pwned` succeeds after the run
    - `grep -c '^fable_render()' tests/run.sh` outputs 1; `grep -c 'STATUSLINE_NO_FABLE= STATUSLINE_USAGE_CACHE=' tests/run.sh` outputs at least 1; `grep -c 'file://\$PWD/tests/fixtures/usage/fable.json' tests/run.sh` outputs 1; `grep -c 'file://\$PWD/tests/fixtures/usage/no-bucket.json' tests/run.sh` outputs 1; `grep -c 'http://192.0.2.1/' tests/run.sh` outputs 1; `grep -c 'STATUSLINE_CURL_MAX_TIME=1' tests/run.sh` outputs 1
    - `grep -c 'accessToken' tests/run.sh` outputs at least 1 (synthetic credentials generated under $TESTTMP) and `grep -rc 'accessToken' tests/fixtures/` outputs 0 for every file (no credentials file is committed)
    - `jq -e '[.limits[] | select(.kind=="weekly_scoped")] | length == 0' tests/fixtures/usage/no-bucket.json` exits 0 and `jq -e '[.limits[] | select(.kind=="weekly_all")] | length == 1' tests/fixtures/usage/no-bucket.json` exits 0
    - The Result summary line is still the LAST line of the harness output; every pre-existing check name from before this plan still appears as PASS
    - No file under the user's `~/.claude` was created or modified by the harness (`ls -l ~/.claude/statusline-usage-cache.json` unchanged before/after, recorded in the SUMMARY)
  </acceptance_criteria>
  <done>The harness asserts every functional branch of the Fable adapter (render shape and bytes, cache lifecycle, negative cache, credentials rules, kill switch, stdin-first, peer rule, bounded timeout) plus the iso_to_epoch contract, all from local fixtures, and stays green and hermetic.</done>
</task>

<task type="auto">
  <name>Task 2: Security probes (hostile stdin model_scoped, hostile cache, hostile endpoint body) under /bin/bash 3.2, 12-field comment update, and the prove-it-bites mutation runs</name>
  <files>tests/run.sh</files>
  <read_first>tests/run.sh (post-Task-1: the §7.2 comment 165-169 and §7.4 comment 197-205 explaining why probes run under /bin/bash; the §7.2/§7.4 loops as the shape to copy; the new §11 block and fable_render), kit/files/home/.claude/statusline.sh (the jq pass with FAB_SI_* assignments, read_cache's uint guard, fetch_usage's parse, get_fable_weekly's order, the kill-switch test line, the FAB_TTL/FAB_GRACE constants — you will mutate copies of these lines), .planning/phases/04-fable-weekly-f-segment/04-RESEARCH.md (§Verified Facts 6 rows 9-10 hostile cases; §Pattern 1 and 2 stress sets; §Security Domain V5), .planning/phases/02-git-segment/02-04-PLAN.md (lines 255-285 — the prove-it-bites convention: swap in a broken variant, run the harness, name the FAIL lines, restore, confirm git status clean)</read_first>
  <action>
    1. Security probes appended to §11 (all through `fable_render` or the same env set, all under /bin/bash, each followed by `rm -f tests/.pwned`). Quoting forms are prescribed so the static acceptance counts below are deterministic:
       - Stdin injection (numeric fields reaching arithmetic — CR-01 regression): for `utilization:.rate_limits.model_scoped[0].utilization` and `resets_at:.rate_limits.model_scoped[0].resets_at`, copy the §7.2 loop idiom verbatim — a `|`/`:`-split spec list and the double-quoted jq filter with escaped inner quotes, `jq "$path = \"x[\$(touch tests/.pwned)]\"" tests/fixtures/fable-stdin.json` — piped into the script with the kill switch empty, URL `file:///nonexistent`, NOCREDS, fresh cache → `fable: stdin injection <label> exit code` = 0, `fable: stdin injection <label> tests/.pwned not created`, `fable: stdin injection <label> stderr bytes` = 0.
       - Stdin arrays (CR-02 regression): for `model_scoped:.rate_limits.model_scoped`, `display_name:.rate_limits.model_scoped[0].display_name`, `utilization:.rate_limits.model_scoped[0].utilization`, `resets_at:.rate_limits.model_scoped[0].resets_at`, copy the §7.4 loop idiom verbatim — `jq "$path = [\"\",\"touch\",\"tests/.pwned\"]" tests/fixtures/fable-stdin.json` — the same way → `fable: stdin array <label> exit code` = 0, `fable: stdin array <label> tests/.pwned not created`, `fable: stdin array <label> hidden` (no `Fable` in line 2 — the guard empties the field, so the stdin branch is skipped and, with no credentials, the segment hides).
       - Hostile cache: write the literal `garbage` into `$CACHE`, render with URL `file:///nonexistent`, CREDS → `fable: hostile cache garbage hidden`, `fable: hostile cache garbage stderr bytes` = 0, `fable: hostile cache garbage exit code` = 0. Then write the JSON `{"fetched_at":"x[$(touch tests/.pwned)]","pct":["","touch","tests/.pwned"],"resets_at":1e100}` into `$CACHE` as ONE literal line — `printf '%s\n' '<that JSON>' > "$CACHE"` with the JSON single-quoted (or a `<<'EOF'` heredoc) so the shell never expands it and the payload appears verbatim in run.sh — render the same way → `fable: hostile cache injection exit code` = 0, `fable: hostile cache injection tests/.pwned not created`, `fable: hostile cache injection stderr bytes` = 0, `fable: hostile cache injection hidden`.
       - Hostile endpoint body: write into `$TESTTMP` the bodies `not json at all` (label `non-json`), fable.json with `.limits = "x"` (label `limits string`), with the Fable bucket's `.percent = "x[$(touch tests/.pwned)]"` (label `percent injection`), with `.percent = ["","touch","tests/.pwned"]` (label `percent array`), and with `.scope.model.display_name = ["","touch","tests/.pwned"]` (label `display_name array`); generate the jq-derived bodies with SINGLE-quoted jq filters over tests/fixtures/usage/fable.json (e.g. `jq '(.limits[] | select(.kind=="weekly_scoped") | .percent) = "x[$(touch tests/.pwned)]"' tests/fixtures/usage/fable.json > "$TESTTMP/body-pinj.json"`), one jq line per body, so each payload is a literal in run.sh; for each: fresh cache, CREDS, URL `file://$TESTTMP/<file>` → `fable: hostile body <label> exit code` = 0, `fable: hostile body <label> tests/.pwned not created`, `fable: hostile body <label> stderr bytes` = 0, `fable: hostile body <label> hidden`.
       Update the §7.4 comment so it reads that all 12 @sh-ingested fields are probed — the 10 original ones here and the two `model_scoped` fields in §11 (which need the kill switch off to reach the arithmetic sinks). Write the phrase `12 @sh-ingested` on ONE physical comment line (the current comment wraps "10" and "@sh-ingested" across two lines — re-wrap the sentence so `12 @sh-ingested fields` sits unbroken on a single line) so the positive grep in the acceptance criteria is deterministic.

    2. Run `/bin/bash tests/run.sh` → exit 0, more than 126 checks, 0 failures, `test ! -e tests/.pwned`.

    3. Prove the probes bite (Phase 2/3 convention): save the real script (`cp kit/files/home/.claude/statusline.sh "$TMP/sl.orig"`, keep its mode), then for EACH mutation below: apply it with `sed` (or a one-line edit) to kit/files/home/.claude/statusline.sh, run `/bin/bash tests/run.sh > "$TMP/m<N>.log" 2>&1; tail -1`, confirm the named FAIL line(s) appear (`grep -F 'FAIL <name>'`), then restore with `cp "$TMP/sl.orig" kit/files/home/.claude/statusline.sh` and confirm `git diff --quiet -- kit/files/home/.claude/statusline.sh` succeeds before the next mutation. Mutations and the FAIL lines that MUST appear:
       - M1 — delete the line in get_fable_weekly that tests `STATUSLINE_NO_FABLE` → `FAIL fable: kill switch hidden`.
       - M2 — make the `write_cache` call in get_fable_weekly run only when `FAB_PCT` is non-empty (prefix it with `[ -n "$FAB_PCT" ] &&`) → `FAIL fable: negative cache written` and `FAIL fable: negative cache hit (no fetch)`.
       - M3 — `FAB_GRACE=3600` → `FAB_GRACE=0` → `FAIL fable: stale-grace serve`.
       - M4 — `FAB_GRACE=3600` → `FAB_GRACE=999999` → `FAIL fable: past-grace hide`.
       - M5 — `FAB_TTL=300` → `FAB_TTL=0` → `FAIL fable: cache hit (no fetch)` (the 42% fixture gets fetched).
       - M6 — in get_token, remove the early return of the explicit `STATUSLINE_CREDENTIALS_FILE` branch so an empty result falls through to the default file and the Keychain → on this host (Keychain item present) `FAIL fable: no credentials hidden` and `FAIL fable: no credentials no cache` (the fixture URL is local, so this reads the Keychain once but never touches the network; if a Keychain dialog appears, click Allow once per D-61 — or record this mutation as skipped with the reason; inside a sandbox it cannot bite and is recorded as such).
       - M7 — delete the `FAB_SI_PCT=` assignment line from the jq program in main → `FAIL fable: stdin-first line 2`.
       - M8 — in read_cache, drop the `| uint` guard from the `.fetched_at` extraction → under /bin/bash 3.2.57 `FAIL fable: hostile cache injection tests/.pwned not created` (the injected subscript string reaches `$(( NOW - C_AT ))`); `rm -f tests/.pwned` afterwards.
       Record a table (mutation → observed FAIL lines → restored) in the SUMMARY. After the last restore: `git diff --quiet -- kit/files/home/.claude/statusline.sh` succeeds, `test -x kit/files/home/.claude/statusline.sh`, `/bin/bash tests/run.sh` is green again, `test ! -e tests/.pwned`, and `git status --porcelain` lists only tests/run.sh (and the Task 1 fixture if not yet committed).
  </action>
  <verify>
    <automated>OUT=$(/bin/bash tests/run.sh) && printf '%s\n' "$OUT" | tail -1 | awk '$1 > 126 && $3 == 0 { ok = 1 } END { exit !ok }' && for n in 'fable: stdin injection utilization tests/.pwned not created' 'fable: stdin injection resets_at tests/.pwned not created' 'fable: stdin array model_scoped tests/.pwned not created' 'fable: stdin array display_name tests/.pwned not created' 'fable: stdin array utilization hidden' 'fable: stdin array resets_at tests/.pwned not created' 'fable: hostile cache garbage hidden' 'fable: hostile cache injection tests/.pwned not created' 'fable: hostile cache injection stderr bytes' 'fable: hostile body non-json hidden' 'fable: hostile body limits string hidden' 'fable: hostile body percent injection tests/.pwned not created' 'fable: hostile body percent array hidden' 'fable: hostile body display_name array tests/.pwned not created'; do printf '%s\n' "$OUT" | grep -qF -- "PASS $n" || { echo "missing: $n"; exit 1; }; done && test ! -e tests/.pwned && git diff --quiet -- kit/files/home/.claude/statusline.sh && test -x kit/files/home/.claude/statusline.sh</automated>
  </verify>
  <acceptance_criteria>
    - `/bin/bash tests/run.sh` exits 0 with more than 126 checks and `0 failures`; `test ! -e tests/.pwned` succeeds afterwards
    - The harness OUTPUT contains each of these exact PASS lines: `fable: stdin injection utilization exit code`, `fable: stdin injection utilization tests/.pwned not created`, `fable: stdin injection resets_at tests/.pwned not created`, `fable: stdin array model_scoped tests/.pwned not created`, `fable: stdin array display_name tests/.pwned not created`, `fable: stdin array utilization tests/.pwned not created`, `fable: stdin array utilization hidden`, `fable: stdin array resets_at tests/.pwned not created`, `fable: hostile cache garbage hidden`, `fable: hostile cache garbage stderr bytes`, `fable: hostile cache injection exit code`, `fable: hostile cache injection tests/.pwned not created`, `fable: hostile cache injection stderr bytes`, `fable: hostile cache injection hidden`, `fable: hostile body non-json hidden`, `fable: hostile body limits string hidden`, `fable: hostile body percent injection tests/.pwned not created`, `fable: hostile body percent array hidden`, `fable: hostile body display_name array tests/.pwned not created`
    - `grep -c '12 @sh-ingested' tests/run.sh` outputs at least 1 (§7.4 comment updated with the phrase written on one physical line — the only load-bearing check for this item; the former negative baseline grep was dropped because the old comment wraps its count across two lines and that grep was already 0 before any change, so it carried no evidence); the §7.4 comment, read as prose, names 12 fields and points to §11 for the two `model_scoped` ones
    - `grep -v '^[[:space:]]*#' tests/run.sh | grep -c 'touch tests/.pwned'` outputs at least 6 — measured baseline at planning time: 2 code lines (§7.1 line 157 `$(touch tests/.pwned)` and §7.2 line 177 `\$(touch tests/.pwned)`); this plan adds at least 4 more code lines carrying the `$(touch tests/.pwned)` payload (the §2 iso row, the §11 stdin-injection jq line, the hostile-cache JSON literal, the hostile-body percent-injection jq line); the substring matches both the escaped `\$(touch …)` and the single-quoted `$(touch …)` forms, so the count does not depend on quoting style
    - `grep -v '^[[:space:]]*#' tests/run.sh | grep -cE 'touch\\?",\\?"tests/\.pwned'` outputs at least 4 — measured baseline at planning time: 1 code line (§7.4 line 218 `[\"\",\"touch\",\"tests/.pwned\"]`; the §7.4 comment on line 198 is filtered out); this plan adds at least 3 more code lines carrying the `["","touch","tests/.pwned"]` payload (the §11 stdin-array jq line, the hostile-cache JSON literal, the hostile-body array jq lines); the optional backslashes make the regex accept both the escaped and the literal form
    - Prove-it-bites: the SUMMARY records the M1-M8 table with, at minimum, these observed FAIL lines — M1 `FAIL fable: kill switch hidden`; M2 `FAIL fable: negative cache written` and `FAIL fable: negative cache hit (no fetch)`; M3 `FAIL fable: stale-grace serve`; M4 `FAIL fable: past-grace hide`; M5 `FAIL fable: cache hit (no fetch)`; M6 `FAIL fable: no credentials hidden` (or an explicit "skipped: <reason>" entry); M7 `FAIL fable: stdin-first line 2`; M8 `FAIL fable: hostile cache injection tests/.pwned not created`
    - After the bite runs: `git diff --quiet -- kit/files/home/.claude/statusline.sh` succeeds (the real script is byte-identical to HEAD), `test -x kit/files/home/.claude/statusline.sh` succeeds, `git ls-files -s kit/files/home/.claude/statusline.sh` starts with `100755`, and `/bin/bash tests/run.sh` is green
    - `git status --porcelain` lists only tests/run.sh and tests/fixtures/usage/no-bucket.json before the task's commit; nothing under the user's `~/.claude` was created or modified
  </acceptance_criteria>
  <done>Hostile stdin, cache, and endpoint inputs are pinned as regression probes under the production interpreter, the 12-field eval boundary is fully covered, and every Fable probe family is proven to bite against a mutated script while the real script is restored byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → script under test | Probes feed hostile stdin, cache files and endpoint bodies into the real script under /bin/bash 3.2 |
| harness → host environment | The harness runs with the user's real $HOME; without the exported kill switch and per-probe overrides it would reach the Keychain, the network and ~/.claude |
| executor bite runs → working tree | Mutated copies of the script are swapped in temporarily; a leftover mutation would ship a broken script |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering / Elevation of Privilege | eval / arithmetic sinks fed by model_scoped, cache, endpoint body | high | mitigate | §11 injection/array/hostile probes under /bin/bash 3.2.57 assert exit 0, no tests/.pwned, 0 stderr, hidden; M8 bite proves the cache uint guard is load-bearing |
| T-04-09 | Tampering | harness reading the Keychain / network / writing ~/.claude | high | mitigate | kill switch exported; fable_render always sets URL, credentials-file and cache overrides into $TESTTMP; blackhole address only for the timeout probe; the only Keychain-reading run is the explicit M6 bite (no network, local fixture) |
| T-04-11 | Tampering | mutated script left in the working tree after a bite run | medium | mitigate | save/restore from a pristine copy after EVERY mutation; `git diff --quiet` on the script is part of verify; exec bit re-checked |
| T-04-12 | Information Disclosure | synthetic credentials or real data in committed fixtures | medium | mitigate | credentials files are generated under $TESTTMP at run time; `grep -rc accessToken tests/fixtures/` must be 0; endpoint fixtures are the sanitized CONTEXT JSON |
| T-04-13 | Denial of Service | a probe that blocks the harness (network wait) | low | mitigate | timeout probe uses `STATUSLINE_CURL_MAX_TIME=1` and asserts only the ≤ 3 s upper bound; every other probe is file:// or a missing path |
| T-04-SC | Tampering | package installs | low | accept | none |
</threat_model>

<verification>
- `/bin/bash tests/run.sh` → more than 126 checks, 0 failures, every named `PASS fable: …` / `PASS iso_to_epoch …` line present, `tests/.pwned` absent
- Bite table M1-M8 recorded in the SUMMARY with the named FAIL lines; script restored byte-identical (git diff --quiet) with mode 100755
- `tests/fixtures/usage/no-bucket.json` valid, no weekly_scoped entry; no credentials-shaped data under tests/fixtures/
- Nothing under the user's ~/.claude created or modified
</verification>

<success_criteria>
- D-65 satisfied: render shape/colours, cache hit, stale-grace serve/hide, timeout bounded with no stderr, ISO→epoch parse, kill switch, missing-bucket hide, credentials rules, stdin-first — all asserted and all bite-proven
- The Phase 2 eval-boundary discipline is extended to the two new stdin fields, the cache file, and the endpoint body
- The harness remains hermetic and is what plan 03 runs inside the sandbox
</success_criteria>

<output>
Create `.planning/phases/04-fable-weekly-f-segment/04-02-SUMMARY.md` when done.
</output>
