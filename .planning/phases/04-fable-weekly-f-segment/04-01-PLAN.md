---
phase: 04-fable-weekly-f-segment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kit/files/home/.claude/statusline.sh
  - tests/fixtures/usage/fable.json
  - tests/fixtures/fable-stdin.json
  - tests/run.sh
  - tests/render-fixtures.sh
autonomous: true
requirements: [FAB-01, FAB-02, FAB-03, FAB-04]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Piping tests/fixtures/full.json into kit/files/home/.claude/statusline.sh with STATUSLINE_NO_FABLE unset, STATUSLINE_USAGE_URL=file://<repo>/tests/fixtures/usage/fable.json, STATUSLINE_CREDENTIALS_FILE pointing at a synthetic credentials file and STATUSLINE_USAGE_CACHE at an empty temp path renders line 2 exactly '10%/100k/1M · 50%/5h (now) · 15%/1w (now) · Fable 74%/1w (now)' — the Fable datum is a separate LAST peer segment joined with the dim ' · ', the literal label 'Fable' rendered dim (SGR 2), the number threshold-coloured via pct_color, '/1w' and the countdown plain, exit 0, zero stderr bytes (FAB-01, D-51, D-53, D-54, D-55)"
    - "That cold render writes the cache file at STATUSLINE_USAGE_CACHE with mode -rw------- (mktemp template in the cache's own directory + mv -f) and content {fetched_at:<epoch>,pct:74,resets_at:0}; a second render within the 300 s TTL with STATUSLINE_USAGE_URL pointing at a DIFFERENT fixture still renders 74% — cache hit, no fetch (FAB-03, D-56, D-57)"
    - "With STATUSLINE_NO_FABLE=1, or no credentials, or a failed fetch past the 3600 s grace window, or an endpoint answer without a Fable bucket, or a hostile/garbage cache, the Fable segment and its separator are absent and every other segment renders unchanged — exit 0, zero stderr (FAB-04, D-49, D-55, D-58, D-64)"
    - "The token is discovered in one ordered list for both environments: $STATUSLINE_CREDENTIALS_FILE (when set: this file only, no Keychain fallback), else $HOME/.claude/.credentials.json, else 'security find-generic-password -s \"Claude Code-credentials\" -w' when 'security' exists — read only when a fetch is actually needed, handed to curl as '-K -' config on stdin (never argv), never printed, never refreshed (FAB-02, D-60, D-62)"
    - "A stdin payload carrying rate_limits.model_scoped[] whose display_name starts with 'fable' (case-insensitive, first hit) renders 'Fable <utilization>%/1w (<countdown>)' from stdin with no cache read, no cache write and no network (D-48, D-50)"
    - "/bin/bash tests/run.sh and /bin/bash tests/render-fixtures.sh export STATUSLINE_NO_FABLE=1 so every pre-existing check and dump stays network-free, Keychain-free and byte-identical: the harness still ends with at least 126 checks and 0 failures, and the host dump still passes installed == repo (D-64, RESEARCH Pitfall 1)"
    - "One live render on the macOS host with the real Keychain token and the production endpoint (no URL/credentials override, STATUSLINE_USAGE_CACHE pointed at a temp path so nothing is written under ~/.claude) ends line 2 with '· Fable NN%/1w (Nd:Nh:Nm)' — the stripped line is recorded in the SUMMARY (roadmap SC1, FAB-02)"
    - "FAB-02 concurrency (fallback edge probe, explicit): concurrent or cancelled renders never write to, refresh, or modify ~/.claude/.credentials.json or the Keychain item — the script has no write path to either store (grep: no 'security add-', no redirect into .credentials.json); an interrupted or failed read yields an empty token and the segment hides"
    - "FAB-03 concurrency (fallback edge probe, explicit): a render cancelled mid-fetch or racing another render can never leave a torn or partial cache — the body is fetched into a shell variable first, then written through mktemp (0600) + mv -f inside the cache's own directory, so the temp file lives for microseconds and every reader sees either the old or the new complete JSON; at a TTL edge N concurrent sessions cost at most N duplicate fetches, last mv wins, no lock required (flock is absent on macOS)"
    - "FAB-04 concurrency (fallback edge probe, explicit): a garbage, truncated or hostile cache file (non-JSON, wrong types, injected strings, 1e100) parses to empty values through the guarded second jq call and hides the segment with exit 0 and zero stderr; the next render past TTL simply refetches and overwrites it"
  artifacts:
    - path: "kit/files/home/.claude/statusline.sh"
      provides: "iso_to_epoch helper, Fable weekly adapter (get_token, read_cache, write_cache, fetch_usage, get_fable_weekly), seg_fable renderer, stdin model_scoped probe, line-2 join with the Fable segment last, header documentation of the STATUSLINE_* env inputs"
      contains: "seg_fable"
      min_lines: 330
    - path: "tests/fixtures/usage/fable.json"
      provides: "sanitized OAuth usage response with a weekly_scoped Fable bucket (percent 74, resets_at 1970-01-01T00:00:00Z) — served via file:// as the fake endpoint"
      contains: "weekly_scoped"
    - path: "tests/fixtures/fable-stdin.json"
      provides: "stdin fixture carrying rate_limits.model_scoped[] with a Fable entry (utilization 33, resets_at 1970-01-01T00:00:00Z) for the D-48 stdin-first path"
      contains: "model_scoped"
    - path: "tests/run.sh"
      provides: "global kill-switch export so the existing harness never reads the Keychain or hits the network"
      contains: "export STATUSLINE_NO_FABLE=1"
    - path: "tests/render-fixtures.sh"
      provides: "global kill-switch export so the byte-for-byte dumps stay deterministic across environments"
      contains: "export STATUSLINE_NO_FABLE=1"
  key_links:
    - from: "kit/files/home/.claude/statusline.sh main()"
      to: "get_fable_weekly"
      via: "direct call in main's shell (not inside a command substitution) after NOW is set and before the line-2 join, so FAB_PCT/FAB_RST are visible to seg_fable"
      pattern: "^  get_fable_weekly"
    - from: "kit/files/home/.claude/statusline.sh main() line-2 join"
      to: "seg_fable"
      via: "appended as the fourth (last) argument of the line-2 join_segments call"
      pattern: "seg_1w\\)\" \"\\$\\(seg_fable\\)\""
    - from: "fetch_usage"
      to: "GET https://api.anthropic.com/api/oauth/usage"
      via: "curl -s -f --max-time N -K - with the three headers fed on stdin by a builtin printf; URL from STATUSLINE_USAGE_URL or the production default"
      pattern: "curl -s -f --max-time"
    - from: "write_cache"
      to: "$STATUSLINE_USAGE_CACHE (default ~/.claude/statusline-usage-cache.json)"
      via: "mktemp template in the cache directory, printf JSON, mv -f (atomic, 0600)"
      pattern: "mktemp \"\\$\\{FAB_CACHE%/\\*\\}/"
    - from: "single jq pass in main()"
      to: "get_fable_weekly stdin branch"
      via: "FAB_SI_PCT / FAB_SI_RST assignments emitted by the @sh program from rate_limits.model_scoped[] (uint / strings guarded)"
      pattern: "FAB_SI_PCT="
    - from: "seg_fable"
      to: "pct_color / fmt_duration / DIM"
      via: "same helpers as seg_1w; colour on the number only, dim literal label, optional countdown from FAB_RST - NOW"
      pattern: "Fable\\$\\{RESET\\}"
  prohibitions:
    - statement: "MUST NOT refresh, rotate, re-save, or otherwise mutate the Claude Code OAuth token or either credential store (~/.claude/.credentials.json, Keychain item 'Claude Code-credentials') — the token is read-only input; an expired or rejected token hides the segment (D-62); and no real token or account identifier may be committed in fixtures, evidence, or planning docs"
      status: resolved
      verification: judgment
    - statement: "MUST NOT let the Fable path block, slow, or blank the rest of the line beyond the single bounded curl --max-time on a cold/stale render — no background processes, detached fetches, daemons, retries with backoff, or sleeps; every failure path ends in an empty FAB_PCT and a normal render of every other segment (D-58, D-59, FAB-03, FAB-04, REQUIREMENTS out-of-scope)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT show any proxy value in place of the Fable bucket — no extra_usage, spend, seven_day_opus/sonnet, or the all-models weekly number; when the Fable bucket is absent the segment is hidden (D-47, D-49)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT let the test harness, the render dumper, or any executor verification read the host Keychain, hit the network, or write under the user's real ~/.claude — the kill switch is exported globally, every live-path probe sets STATUSLINE_CREDENTIALS_FILE, STATUSLINE_USAGE_URL and STATUSLINE_USAGE_CACHE explicitly, and the one live host render points its cache at a temp path (D-46 carry-over, D-64, RESEARCH Pitfall 1)"
      status: resolved
      verification: judgment
    - statement: "MUST NOT ship, copy, or forward the host token into the sandbox through the kit or any test — the sandbox uses only its own ~/.claude/.credentials.json (D-63)"
      status: resolved
      verification: judgment
---

<objective>
**As a** Claude Code Max subscriber working on the macOS host and inside Docker Sandboxes, **I want to** see my Fable-specific weekly usage and its reset countdown as the last segment of line 2 (`… · 15%/1w (3d:5h:57m) · Fable 74%/1w (2d:4h:30m)`), **so that** one glance tells me how much of the Fable cap is left — and never at the price of a slower or blank status line.

This plan is the tracer slice: the complete Fable weekly adapter in `kit/files/home/.claude/statusline.sh` — pure-bash `iso_to_epoch`, the collector (`get_fable_weekly` with kill switch → stdin-first → TTL cache → `curl -K -` fetch with the token from the credentials file or the Keychain → atomic 0600 cache write, negative results cached, bounded stale-while-error grace → hidden), `seg_fable` as a dim-labelled peer of `seg_1w`, and the line-2 join with the Fable segment last — proven end-to-end on the host with a `file://` fixture standing in for the endpoint, then with the real Keychain token against the production endpoint. It also exports the kill switch in the harness and the render dumper so nothing that already exists goes live.

Purpose: after this plan the feature works in production shape on the host (FAB-01..04 behaviours exist); plan 02 proves every branch bites in the harness, plan 03 proves the sandbox half, reconciles the docs, and hands off the live check.
Output: `kit/files/home/.claude/statusline.sh` (+ ~130 lines), `tests/fixtures/usage/fable.json`, `tests/fixtures/fable-stdin.json`, `tests/run.sh` (+1 export line), `tests/render-fixtures.sh` (+1 export line, determinism note).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-fable-weekly-f-segment/04-CONTEXT.md
@.planning/phases/04-fable-weekly-f-segment/04-RESEARCH.md
@.planning/phases/04-fable-weekly-f-segment/04-PATTERNS.md
@.claude/CLAUDE.md
@kit/files/home/.claude/statusline.sh
@tests/run.sh
@tests/render-fixtures.sh
@tests/fixtures/full.json
</context>

## Artifacts this phase produces

New symbols and files created across Phase 4 (plans 01-03). The source-grounding pass excludes these from drift verification:

- `kit/files/home/.claude/statusline.sh` — new functions `iso_to_epoch` (pure helper, after `fmt_duration`), `seg_fable` (after `seg_1w`), and the "Fable weekly adapter" section: `get_token`, `read_cache`, `write_cache`, `fetch_usage`, `get_fable_weekly`; new constants `FAB_TTL` (300), `FAB_GRACE` (3600), `FAB_URL`, `FAB_CACHE`, `FAB_MAXTIME`; new variables `FAB_PCT`, `FAB_RST` (collector outputs), `FAB_SI_PCT`, `FAB_SI_RST` (stdin probe, emitted by the jq pass), `C_AT`/`C_PCT`/`C_RST` (cache read), `F_PCT`/`F_RST_ISO` (fetch parse); new header "Env inputs" block (this plan)
- Env vars / knobs: `STATUSLINE_NO_FABLE` (kill switch, D-64), `STATUSLINE_USAGE_URL` (endpoint override, default `https://api.anthropic.com/api/oauth/usage`), `STATUSLINE_CREDENTIALS_FILE` (credentials-file override; when set = this file only), `STATUSLINE_USAGE_CACHE` (cache path override, default `$HOME/.claude/statusline-usage-cache.json`), `STATUSLINE_CURL_MAX_TIME` (test knob, default 2) (this plan)
- Cache file `~/.claude/statusline-usage-cache.json`, format `{"fetched_at":<epoch>,"pct":<0-100|null>,"resets_at":<epoch|null>}`, written via temp template `.usage.XXXXXX` in the same directory (this plan)
- Fixtures: `tests/fixtures/usage/fable.json`, `tests/fixtures/fable-stdin.json` (this plan); `tests/fixtures/usage/no-bucket.json` (plan 02)
- `tests/run.sh` — `export STATUSLINE_NO_FABLE=1` (this plan); `iso_to_epoch` table in §2, helper `fable_render` (globals `FB_OUT`/`FB_RC`/`FB_ERRBYTES`/`FB_L2`), new §11 "Fable weekly" block with check names prefixed `fable: `, new §7.2/§7.4 rows for `model_scoped` (plan 02)
- `tests/render-fixtures.sh` — `export STATUSLINE_NO_FABLE=1` + determinism sentence (this plan); fixture count wording (plan 02)
- `tests/sandbox.sh` — new §5.13: `INFO credentials file: present|absent`, check `Fable segment renders in sandbox (FAB-02, D-63)` / `Fable hidden in sandbox without credentials (D-63)`, `INFO sandbox cache:` line, local `ESC`/`strip_ansi` helpers (plan 03)
- `kit/spec.yaml` — `curl` added to the D-33 install guard (plan 03)
- `README.md` — example line with `· Fable 74%/1w (2d:4h:30m)`, legend row, new "Fable weekly segment" note, `curl` in Requirements (plan 03)
- `.planning/REQUIREMENTS.md` FAB-01/FAB-04 wording, `.planning/PROJECT.md` target layout + decision rows, `.planning/ROADMAP.md` Phase 4 goal/SC1 layout-correction note (plan 03, D-52)

## Flagged assumptions (spec-less edge probe — no silent drops)

No SPEC.md exists for this phase. The deterministic edge probe returned FAB-02/03/04 as `concurrency` probes ("if interrupted or run in parallel, what is guaranteed?") — each is authored above as an explicit `must_haves.truths` entry (atomic mktemp+mv, fetch-then-write, read-only credential access, guarded cache parse). FAB-01 came back `unclassified → unresolved` and is surfaced here as an explicit assumption:

1. **FAB-01 (unclassified):** REQUIREMENTS.md still words FAB-01 as `f(pct)` "inside the weekly segment". Per D-51/D-52 the phase delivers the Fable value as a **separate last peer segment** `Fable pct/1w (countdown)` and plan 03 reconciles the REQUIREMENTS/PROJECT/README/ROADMAP wording. The verifier should judge FAB-01 against the D-51 form (roadmap SC1 "weekly segment followed by the Fable segment"), not the superseded in-segment form.
2. **Live host proof (Task 2 step 5)** needs network access to `api.anthropic.com`, which this agent's dev sandbox blocks — the executor runs that one command outside the sandbox (`dangerouslyDisableSandbox`), never prints the token, and points the cache at a temp path. If the network is unavailable at execution time the step is recorded as INFO and the end-of-phase human check (plan 03) covers SC1.
3. **Research A1** (`settings.json` `env` entries reach the statusline subprocess) affects only README wording for the kill switch (plan 03); the shell-export form always works.

Canon referral (not minted as prohibitions): token in `ps` argv, hostile JSON reaching `eval`/arithmetic, TLS verification — canon security, handled in `<threat_model>` (T-04-01..T-04-04) and by the Phase 2 harness discipline extended in plan 02.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — Fable weekly adapter + seg_fable end-to-end through a file:// endpoint fixture, kill switch exported in the harness and dumper</name>
  <reversibility rating="costly">Env-var names (D-64/D-65) and the cache path + JSON shape (D-57) become a small public contract shared by the script header, README, tests/run.sh, tests/sandbox.sh and users' shell/settings.json env — renaming later is a coordinated multi-file change (no data migration: a stale cache self-heals on the next fetch).</reversibility>
  <files>kit/files/home/.claude/statusline.sh, tests/fixtures/usage/fable.json, tests/run.sh, tests/render-fixtures.sh</files>
  <read_first>kit/files/home/.claude/statusline.sh (whole file: palette lines 7-15, fmt_duration 36-46, pct_color 48-54, join_segments 56-64, seg_model_effort 69-75 dim-label idiom, seg_git 89-128 external-call discipline, seg_1w 151-159 template, main 163-220: local line 179, jq pass 181-195, NOW 197, line-2 join 210, source guard 222-226), .planning/phases/04-fable-weekly-f-segment/04-RESEARCH.md (§Summary; §Verified Facts 2-6; §Architecture Patterns 2-6 — copy Pattern 3 iso_to_epoch verbatim, Patterns 4-6 are the bash-3.2-prototyped bodies; §Anti-Patterns; §Common Pitfalls 1-8; §Security Domain), .planning/phases/04-fable-weekly-f-segment/04-PATTERNS.md (every statusline.sh row; the kill-switch export placement for run.sh and render-fixtures.sh), .planning/phases/04-fable-weekly-f-segment/04-CONTEXT.md (D-47..D-66; the sanitized live response JSON in <specifics> = the fixture content), .claude/CLAUDE.md (bash 3.2 / BSD-GNU portability table, jq patterns, What NOT to Use), tests/run.sh (lines 12-13 SL assignment, 44-46 temp dirs, 95-105 run_fixture), tests/render-fixtures.sh (lines 24-33 determinism paragraph + SL/INSTALLED idiom), tests/fixtures/full.json</read_first>
  <action>
    All script code is bash 3.2 / BSD-GNU-neutral (`#!/bin/bash`, no `set -e/-u`, every new external call guarded with `2>/dev/null` and falling through to "hidden"; comments cite the decision they serve — house style). Follow RESEARCH Patterns 3-6 and the PATTERNS.md placements; do not invent a different control flow.

    1. Header (lines 1-5): extend the comment with an "Env inputs:" block in the style of tests/render-fixtures.sh lines 15-19 listing, with defaults: `STATUSLINE_NO_FABLE` (non-empty = kill switch: no credential read, no network, Fable segment hidden — D-64), `STATUSLINE_USAGE_URL` (default `https://api.anthropic.com/api/oauth/usage`), `STATUSLINE_CREDENTIALS_FILE` (default: `$HOME/.claude/.credentials.json` then the macOS Keychain item "Claude Code-credentials"; when set explicitly it is the ONLY source — no Keychain fallback, so tests are deterministic on macOS), `STATUSLINE_USAGE_CACHE` (default `$HOME/.claude/statusline-usage-cache.json`), `STATUSLINE_CURL_MAX_TIME` (default 2, seconds; test knob). Note the cache TTL 300 s and grace 3600 s there too (D-56, D-58).

    2. `iso_to_epoch` (D-54): insert directly after `fmt_duration`, copying RESEARCH Pattern 3 verbatim (comment header `# iso_to_epoch ISO -> epoch seconds, or "" when unparseable (D-54)`; accepts `YYYY-MM-DDTHH:MM:SS[.frac][Z|±HH:MM|±HHMM|none=UTC]`; days-from-civil arithmetic; `10#` on every two-digit field; `case` bracket patterns without spaces; prints nothing and returns 0 on any unparseable input). The 17-row verified table in RESEARCH Pattern 3 is the contract (e.g. `2026-08-24T18:00:00.097816+00:00` → 1787594400, `1970-01-01T00:00:00Z` → 0, `2000-02-29T00:00:00Z` → 951782400, garbage/date-only/injected strings → empty).

    3. New section `# --- Fable weekly adapter (FAB-01..04, D-47..D-65) ---` placed after the segment renderers and before `# --- Main ---`, containing, in this order:
       - Constants: `FAB_TTL=300` (D-56), `FAB_GRACE=3600` (D-58), `FAB_MAXTIME=${STATUSLINE_CURL_MAX_TIME:-2}` (D-59), `FAB_URL=${STATUSLINE_USAGE_URL:-https://api.anthropic.com/api/oauth/usage}`, `FAB_CACHE=${STATUSLINE_USAGE_CACHE:-$HOME/.claude/statusline-usage-cache.json}` (D-57, D-65; the cache path is expected to be absolute).
       - `get_token` (D-60, D-62): prints the access token or nothing. Source order: if `STATUSLINE_CREDENTIALS_FILE` is set → that file only; else `$HOME/.claude/.credentials.json`; if that yields nothing and `command -v security` succeeds → `security find-generic-password -s "Claude Code-credentials" -w 2>/dev/null` piped into jq. Each source goes through ONE guarded jq program (`jq -r … 2>/dev/null`, with `--argjson now "$NOW"`) that prints `.claudeAiOauth.accessToken` only when it is a non-empty string AND — the RESEARCH OQ2 pre-check, consistent with D-62 — when `.claudeAiOauth.expiresAt`, if it is a number (epoch milliseconds), divided by 1000 is still greater than `$now`; a missing or non-numeric `expiresAt` never blocks (use the `// ($now + 1)` idiom so a non-number falls through as "valid"). Any non-empty string is a token (RESEARCH Pitfall 8: the sandbox token is a 26-char proxy credential — no length/prefix validation). The token is never echoed, logged, or stored.
       - `read_cache` (D-57): pre-clears `C_AT=""; C_PCT=""; C_RST=""`; returns 0 silently unless `[ -r "$FAB_CACHE" ]`; parses the file with a separate guarded jq program that re-declares `def uint: (numbers | floor | select(. >= 0 and . < 1e15)) // "";` and emits `@sh "C_AT=\(.fetched_at // "" | uint) C_PCT=\(.pct // "" | uint) C_RST=\(.resets_at // "" | uint)"`, then `eval`s the (possibly empty) output — non-JSON leaves all three empty (RESEARCH Pattern 5).
       - `write_cache PCT RST` (D-57): `mkdir -p "${FAB_CACHE%/*}" 2>/dev/null`; `tmp=$(mktemp "${FAB_CACHE%/*}/.usage.XXXXXX" 2>/dev/null) || return 1` (0600 by default on BSD and GNU — no umask dance); `printf '{"fetched_at":%s,"pct":%s,"resets_at":%s}\n' "$NOW" "${1:-null}" "${2:-null}" > "$tmp" && mv -f "$tmp" "$FAB_CACHE" 2>/dev/null || rm -f "$tmp" 2>/dev/null`. Empty PCT/RST become JSON `null` (negative result, D-49).
       - `fetch_usage` (D-59, D-62, FAB-02): returns 1 on any failure, sets `F_PCT` / `F_RST_ISO` on success (both may be empty = "no Fable bucket", still success). Steps: `command -v curl >/dev/null 2>&1 || return 1`; `tok=$(get_token)`; `[ -n "$tok" ] || return 1`; build the curl config with a builtin `printf` of three `header = "…"` lines (`Authorization: Bearer <tok>`, `anthropic-beta: oauth-2025-04-20`, `Content-Type: application/json` — the live-verified set) piped into `curl -s -f --max-time "$FAB_MAXTIME" -K - "$FAB_URL" 2>/dev/null`, capturing the body into a variable (`|| return 1`; empty body → return 1). Never put the token on the curl command line, never disable TLS verification, never bypass the proxy environment (the sandbox egress and auth injection ride HTTPS_PROXY — RESEARCH Verified Facts 4). Parse the body with the RESEARCH Pattern 2 jq program (own `def uint`; `.limits? // [] | arrays[]? | objects | select(.kind == "weekly_scoped" and ((.scope.model.display_name? // "" | strings // "") | ascii_downcase | startswith("fable")))`, `first // {}`, `@sh "F_PCT=\($b.percent // "" | uint) F_RST_ISO=\($b.resets_at // "" | strings // "")"`) — D-47/D-50; jq producing no output (non-JSON body) is a failure (return 1); otherwise pre-clear `F_PCT=""; F_RST_ISO=""` and `eval` the single-line output.
       - `get_fable_weekly` (D-48, D-55, D-58, D-59, D-64) — RESEARCH Pattern 6 verbatim in spirit: `FAB_PCT=""; FAB_RST=""`; if `STATUSLINE_NO_FABLE` is non-empty → return (nothing else runs: no file, no Keychain, no network); if `FAB_SI_PCT` is non-empty → `FAB_PCT=$FAB_SI_PCT; FAB_RST=$(iso_to_epoch "$FAB_SI_RST")`; return (stdin first — Task 2 wires the ingestion; until then the variable is simply empty); `read_cache`; if `C_AT` non-empty and `NOW - C_AT < FAB_TTL` → serve `C_PCT`/`C_RST` (a fresh negative cache serves empty → hidden, no network) and return; if `fetch_usage` succeeds → `FAB_PCT=$F_PCT; FAB_RST=$(iso_to_epoch "$F_RST_ISO")`; `write_cache "$FAB_PCT" "$FAB_RST"` (negative results cached too — RESEARCH Pitfall 2 / SC4) and return; else if `C_AT` non-empty and `NOW - C_AT < FAB_GRACE` → serve `C_PCT`/`C_RST` (stale-while-error, D-58); else leave empty (hidden). Guard every arithmetic input for emptiness first (the `uint` guard already guarantees integer-or-empty).
       - `seg_fable` placed directly after `seg_1w` (D-51, D-53, D-54, D-55, FAB-01): `[ -n "$FAB_PCT" ] || return 0`; `local pct=${FAB_PCT%.*} out`; `[ -z "$pct" ] && pct=0`; `out="${DIM}Fable${RESET} $(pct_color "$pct")${pct}%${RESET}/1w"`; when `FAB_RST` is non-empty append ` ($(fmt_duration $(( FAB_RST - NOW ))))`; `printf '%s' "$out"`. Colour on the number only; label dim; `/1w` and the parens plain; ANSI-16 only (D-02).

    4. `main()`: add `FAB_PCT FAB_RST` to the `local` declaration on line 179; immediately after `NOW=$(date +%s)` call `get_fable_weekly` DIRECTLY in main's shell (not inside `$(…)` — it must set FAB_PCT/FAB_RST where seg_fable, which runs in a command substitution, can see them by dynamic scope); change the line-2 join to `join_segments "$sep" "$(seg_context)" "$(seg_5h)" "$(seg_1w)" "$(seg_fable)"` — Fable last; `join_segments` already drops empties and their separators (D-55 / PRES-04). Nothing else in main changes.

    5. Fixture `tests/fixtures/usage/fable.json` (RESEARCH Pitfall 7 — sub-directory on purpose: tests/render-fixtures.sh globs `tests/fixtures/*.json` non-recursively, so endpoint fixtures never get piped in as stdin): the sanitized live response from 04-CONTEXT.md `<specifics>` (numbers only, no identifiers), with the `weekly_scoped` Fable bucket's `resets_at` changed to `"1970-01-01T00:00:00Z"` so the countdown is the deterministic `(now)`; keep `percent: 74`, `scope.model.display_name: "Fable"`, the `session` and `weekly_all` entries, `seven_day_opus`/`seven_day_sonnet` null, `extra_usage`, `spend`. Valid JSON (`jq . tests/fixtures/usage/fable.json` exits 0).

    6. Kill switch in the existing tooling (RESEARCH Pitfall 1 — without it every `run_fixture`/dump call on the host would read the Keychain and hit the network): in tests/run.sh add `export STATUSLINE_NO_FABLE=1` on the line after `SL=kit/files/home/.claude/statusline.sh` with a one-line comment (D-64; plan 02's Fable block overrides it per command); in tests/render-fixtures.sh add the same export after the `SL=` line and extend the "Determinism:" paragraph with one sentence saying the Fable path is disabled so the dumps never depend on network or credentials (D-64).

    7. Prove the tracer end-to-end on the host, all under `/bin/bash`: create a temp dir and a synthetic credentials file `{"claudeAiOauth":{"accessToken":"test-token","expiresAt":9999999999999}}`; render `tests/fixtures/full.json` with `STATUSLINE_NO_FABLE=` (empty), `STATUSLINE_USAGE_URL=file://$PWD/tests/fixtures/usage/fable.json`, `STATUSLINE_CREDENTIALS_FILE=<that file>`, `STATUSLINE_USAGE_CACHE=<temp>/usage.json` — stripped line 2 must be exactly `10%/100k/1M · 50%/5h (now) · 15%/1w (now) · Fable 74%/1w (now)`, exit 0, zero stderr bytes; the cache file must exist with mode `-rw-------` (`ls -l | cut -c1-10` — portable, no stat) and `jq -c '[.fetched_at > 0, .pct, .resets_at]'` = `[true,74,0]`; render again with `STATUSLINE_USAGE_URL=file:///nonexistent` and the same cache → identical line 2 (cache hit); render with `STATUSLINE_NO_FABLE=1` and the same cache → line 2 `10%/100k/1M · 50%/5h (now) · 15%/1w (now)`; render with an absent credentials file and an empty cache dir → hidden and no cache file created; run `/bin/bash tests/run.sh` → last line at least 126 checks, 0 failures (unchanged count — the kill switch keeps everything hermetic); run `/bin/bash tests/render-fixtures.sh tests/out/host` → still PASS installed == repo; confirm `git status --porcelain tests/out` is empty and nothing under `~/.claude` was created or modified (`ls -l ~/.claude/statusline-usage-cache.json` must say no such file unless the user's own Claude Code session created it — never write it yourself). Do not run the Fable path without the four overrides anywhere in this task.
  </action>
  <verify>
    <automated>T=$(mktemp -d "${TMPDIR:-/tmp}/fab.XXXXXX") && printf '{"claudeAiOauth":{"accessToken":"test-token","expiresAt":9999999999999}}' > "$T/creds.json" && [ "$(STATUSLINE_NO_FABLE= STATUSLINE_USAGE_URL="file://$PWD/tests/fixtures/usage/fable.json" STATUSLINE_CREDENTIALS_FILE="$T/creds.json" STATUSLINE_USAGE_CACHE="$T/usage.json" /bin/bash kit/files/home/.claude/statusline.sh < tests/fixtures/full.json 2>&1 | sed "s/$(printf '\033')\[[0-9;]*m//g" | sed -n 2p)" = "10%/100k/1M · 50%/5h (now) · 15%/1w (now) · Fable 74%/1w (now)" ] && [ "$(ls -l "$T/usage.json" | cut -c1-10)" = "-rw-------" ] && [ "$(jq -c '[.fetched_at > 0, .pct, .resets_at]' "$T/usage.json")" = "[true,74,0]" ] && [ "$(STATUSLINE_NO_FABLE= STATUSLINE_USAGE_URL=file:///nonexistent STATUSLINE_CREDENTIALS_FILE="$T/creds.json" STATUSLINE_USAGE_CACHE="$T/usage.json" /bin/bash kit/files/home/.claude/statusline.sh < tests/fixtures/full.json 2>&1 | sed "s/$(printf '\033')\[[0-9;]*m//g" | sed -n 2p)" = "10%/100k/1M · 50%/5h (now) · 15%/1w (now) · Fable 74%/1w (now)" ] && [ "$(STATUSLINE_NO_FABLE=1 STATUSLINE_USAGE_URL=file:///nonexistent STATUSLINE_CREDENTIALS_FILE="$T/creds.json" STATUSLINE_USAGE_CACHE="$T/usage.json" /bin/bash kit/files/home/.claude/statusline.sh < tests/fixtures/full.json 2>&1 | sed "s/$(printf '\033')\[[0-9;]*m//g" | sed -n 2p)" = "10%/100k/1M · 50%/5h (now) · 15%/1w (now)" ] && /bin/bash tests/run.sh | tail -1 | awk '$1 >= 126 && $3 == 0 { ok = 1 } END { exit !ok }' && rm -rf "$T"</automated>
  </verify>
  <acceptance_criteria>
    - `/bin/bash -n kit/files/home/.claude/statusline.sh` exits 0 and `test -x kit/files/home/.claude/statusline.sh` succeeds
    - `grep -c '^iso_to_epoch()' kit/files/home/.claude/statusline.sh` outputs 1; `grep -c '^seg_fable()' kit/files/home/.claude/statusline.sh` outputs 1; `grep -c '^get_fable_weekly()' kit/files/home/.claude/statusline.sh` outputs 1; `grep -c '^get_token()' kit/files/home/.claude/statusline.sh` outputs 1; `grep -c '^read_cache()' kit/files/home/.claude/statusline.sh` outputs 1; `grep -c '^write_cache()' kit/files/home/.claude/statusline.sh` outputs 1; `grep -c '^fetch_usage()' kit/files/home/.claude/statusline.sh` outputs 1
    - `grep -c 'FAB_TTL=300' kit/files/home/.claude/statusline.sh` outputs 1; `grep -c 'FAB_GRACE=3600' kit/files/home/.claude/statusline.sh` outputs 1; `grep -c 'STATUSLINE_CURL_MAX_TIME:-2' kit/files/home/.claude/statusline.sh` outputs 1; `grep -c 'STATUSLINE_USAGE_URL:-https://api.anthropic.com/api/oauth/usage' kit/files/home/.claude/statusline.sh` outputs 1; `grep -c 'STATUSLINE_USAGE_CACHE:-\$HOME/.claude/statusline-usage-cache.json' kit/files/home/.claude/statusline.sh` outputs 1; `grep -c 'STATUSLINE_NO_FABLE' kit/files/home/.claude/statusline.sh` outputs at least 2 (header + collector)
    - `grep -c 'security find-generic-password -s "Claude Code-credentials" -w' kit/files/home/.claude/statusline.sh` outputs 1; `grep -c 'claudeAiOauth' kit/files/home/.claude/statusline.sh` outputs at least 1; `grep -c 'anthropic-beta: oauth-2025-04-20' kit/files/home/.claude/statusline.sh` outputs 1; `grep -c -- '-K -' kit/files/home/.claude/statusline.sh` outputs at least 1; `grep -c 'curl -s -f --max-time' kit/files/home/.claude/statusline.sh` outputs 1
    - `grep -v '^[[:space:]]*#' kit/files/home/.claude/statusline.sh | grep -cE -- '(^|[[:space:]])-H[[:space:]]|--header|--noproxy|--insecure|[[:space:]]-k[[:space:]]|security add-|security delete-|> *"?\$HOME/.claude/.credentials'` outputs 0 (code lines only: token never in argv, TLS and proxy never bypassed, no credential-store writes)
    - `grep -v '^[[:space:]]*#' kit/files/home/.claude/statusline.sh | grep -cE 'date -d|date -r|date -j|flock|stat -c|stat -f|declare -A|mapfile|readlink -f|sed -i|echo -e'` outputs 0 (code lines only — CLAUDE.md portability table)
    - `grep -c 'weekly_scoped' kit/files/home/.claude/statusline.sh` outputs 1; `grep -c 'startswith("fable")' kit/files/home/.claude/statusline.sh` outputs at least 1; `grep -c 'mktemp "\${FAB_CACHE%/\*}/.usage.XXXXXX"' kit/files/home/.claude/statusline.sh` outputs 1; `grep -c 'mv -f "\$tmp" "\$FAB_CACHE"' kit/files/home/.claude/statusline.sh` outputs 1
    - `grep -cE '^  get_fable_weekly( |$)' kit/files/home/.claude/statusline.sh` outputs 1 (direct call in main, not inside a command substitution); `grep -Fc '"$(seg_1w)" "$(seg_fable)")' kit/files/home/.claude/statusline.sh` outputs 1
    - `grep -c 'Fable\${RESET}' kit/files/home/.claude/statusline.sh` outputs 1 and that line also contains `${DIM}Fable` and `pct_color "$pct"` (dim literal label, colour on the number only)
    - `jq . tests/fixtures/usage/fable.json` exits 0; `jq -r '.limits[] | select(.kind=="weekly_scoped") | [.scope.model.display_name, .percent, .resets_at] | @tsv' tests/fixtures/usage/fable.json` prints `Fable	74	1970-01-01T00:00:00Z`; `grep -ciE 'sk-ant|accessToken|token' tests/fixtures/usage/fable.json` outputs 0
    - `grep -c '^export STATUSLINE_NO_FABLE=1' tests/run.sh` outputs 1 and it appears on a line number greater than the `^SL=` line; `grep -c '^export STATUSLINE_NO_FABLE=1' tests/render-fixtures.sh` outputs 1
    - The cold file:// render of tests/fixtures/full.json prints stripped line 2 exactly `10%/100k/1M · 50%/5h (now) · 15%/1w (now) · Fable 74%/1w (now)` with exit 0 and 0 stderr bytes; the raw render contains the byte sequence `ESC[2mFable ESC[0m ESC[33m74%ESC[0m/1w` (ESC = 0x1b; the space after the dim label is outside both spans)
    - After the cold render: `ls -l <cache> | cut -c1-10` prints `-rw-------`; `jq -c '[.fetched_at > 0, .pct, .resets_at]' <cache>` prints `[true,74,0]`
    - Second render with `STATUSLINE_USAGE_URL=file:///nonexistent` and the warm cache prints the same line 2 (cache hit); render with `STATUSLINE_NO_FABLE=1` prints `10%/100k/1M · 50%/5h (now) · 15%/1w (now)`; render with an absent credentials file and no cache prints `10%/100k/1M · 50%/5h (now) · 15%/1w (now)` and creates no cache file
    - `/bin/bash tests/run.sh` exits 0 and its last line reports at least 126 checks and `0 failures`; `/bin/bash tests/render-fixtures.sh tests/out/host` exits 0 with a PASS line for installed == repo; `git status --porcelain tests/out` prints nothing
    - No file under the user's `~/.claude` was created or modified by this task (the SUMMARY records `ls -l ~/.claude/statusline-usage-cache.json` output, expected "No such file" unless the user's own live session wrote it)
  </acceptance_criteria>
  <done>The Fable weekly adapter renders `Fable 74%/1w (now)` as the last line-2 segment from a file:// endpoint fixture, caches atomically at 0600, serves from cache without fetching, hides under the kill switch / no credentials, and the existing 126-check harness and the render dumper stay green and hermetic — the architecture is proven end-to-end on the host and committed.</done>
</task>

<task type="auto">
  <name>Task 2: Stdin-first source (D-48) folded into the single jq pass + stdin fixture, then the live host render against the production endpoint (roadmap SC1)</name>
  <files>kit/files/home/.claude/statusline.sh, tests/fixtures/fable-stdin.json, tests/render-fixtures.sh</files>
  <read_first>kit/files/home/.claude/statusline.sh (post-Task-1: the ingestion comment block lines ~164-178 and the jq program lines ~181-194 — the `def uint` line and the `@sh "` opening; get_fable_weekly's stdin branch), .planning/phases/04-fable-weekly-f-segment/04-RESEARCH.md (§Verified Facts 1 — why stdin carries no model_scoped today and what its shape will be; §Pattern 1 — the exact `as $ms` binding and the two assignment lines; §Verified Facts 3 — host Keychain facts; §Environment Availability — network to api.anthropic.com is blocked inside this agent's dev sandbox), .planning/phases/04-fable-weekly-f-segment/04-PATTERNS.md (§stdin model_scoped probe, §fixtures), tests/fixtures/full.json, tests/render-fixtures.sh (line 24 "all 7 fixtures" wording)</read_first>
  <action>
    1. Stdin probe (D-48, D-50 — forward-compatible, zero cost today): inside the existing jq program of main(), between the `def uint: …;` line and the `@sh "` opening, add the RESEARCH Pattern 1 binding — `( [ .rate_limits.model_scoped? // [] | arrays[]? | objects | select((.display_name? // "" | strings // "") | ascii_downcase | startswith("fable")) ] | first // {} ) as $ms |` — and append two assignment lines inside the @sh string after `P7_RST=…`: `FAB_SI_PCT=\($ms.utilization // "" | uint)` and `FAB_SI_RST=\($ms.resets_at // "" | strings // "")` (utilization IS the endpoint's percent, 0-100, no scaling — RESEARCH Verified Facts 1; resets_at is an ISO string, converted by iso_to_epoch in the collector's stdin branch written in Task 1). The binding must come before the @sh string because @sh applies to the whole program output. Update the ingestion comment block so the counts read 4 string fields and 8 numeric fields (12 @sh-ingested fields) and mention the two new names. Confirm against a hostile payload under /bin/bash that `model_scoped` as a string, as an array of strings, and with array-valued fields all yield empty FAB_SI_* (one quoted word each) — the §7 probes in plan 02 pin this permanently.

    2. Fixture `tests/fixtures/fable-stdin.json`: tests/fixtures/full.json plus `"model_scoped":[{"display_name":"Fable","utilization":33,"resets_at":"1970-01-01T00:00:00Z"}]` inside `rate_limits` (session_id `fix-fable-stdin`). It lives in tests/fixtures/ (not usage/) on purpose: it is a stdin payload; with the exported kill switch the dumper renders it like full.json, so the cross-environment byte diff stays deterministic. In tests/render-fixtures.sh change the determinism sentence "all 7 fixtures" to "all 8 fixtures" and mention that `fable-stdin.json` renders without the Fable segment because the dumper exports the kill switch.

    3. Prove the stdin path under /bin/bash: render `tests/fixtures/fable-stdin.json` with `STATUSLINE_NO_FABLE=` (empty), `STATUSLINE_USAGE_URL=file:///nonexistent`, `STATUSLINE_CREDENTIALS_FILE=<absent path>`, `STATUSLINE_USAGE_CACHE=<empty temp dir>/usage.json` → stripped line 2 exactly `10%/100k/1M · 50%/5h (now) · 15%/1w (now) · Fable 33%/1w (now)`, exit 0, zero stderr, and NO cache file created (stdin wins before the cache is touched — D-48); the same payload with `STATUSLINE_NO_FABLE=1` → `10%/100k/1M · 50%/5h (now) · 15%/1w (now)` (D-64 disables the stdin path too). Re-run `/bin/bash tests/run.sh` (still at least 126 checks, 0 failures) and `/bin/bash tests/render-fixtures.sh tests/out/host` (8 fixtures, PASS installed == repo).

    4. Live host render (roadmap SC1, FAB-02 — the only step that needs the real Keychain and the real endpoint): from the repo root run, with `STATUSLINE_NO_FABLE` unset, NO `STATUSLINE_USAGE_URL` and NO `STATUSLINE_CREDENTIALS_FILE` override (so get_token walks the D-60 order: `~/.claude/.credentials.json` is absent on this host, the Keychain item answers), and `STATUSLINE_USAGE_CACHE=<temp dir>/live-usage.json` (never the real `~/.claude/` path), `/bin/bash kit/files/home/.claude/statusline.sh < tests/fixtures/full.json`. This needs network access to `api.anthropic.com`, which this agent's dev sandbox blocks — run that single command outside the sandbox (`dangerouslyDisableSandbox: true`), and nothing else. Expected: stripped line 2 = `10%/100k/1M · 50%/5h (now) · 15%/1w (now) · Fable NN%/1w (Nd:Nh:Nm)` (NN = the account's live Fable percentage, ~75 at research time; the countdown is live), exit 0, 0 stderr; the temp cache file exists with mode `-rw-------` and a `pct` equal to NN; the wall time of that cold render is under 2 s (measured with `date +%s` before/after, whole seconds). Record the stripped line 2, the cache mode line (`ls -l`, no content beyond `jq -c .pct`), and the elapsed seconds in the SUMMARY. NEVER print, echo, or store the token; if a Keychain dialog appears, click "Always Allow" once (D-61) and note it. If the network is unavailable or the endpoint answers non-2xx, record the stripped line 2 (segment hidden, everything else normal) as INFO in the SUMMARY — that is itself FAB-04 evidence — and state that SC1 is then covered by the plan 03 end-of-phase human check. Delete the temp cache afterwards; confirm `ls -l ~/.claude/statusline-usage-cache.json` was not created by you.
  </action>
  <verify>
    <automated>T=$(mktemp -d "${TMPDIR:-/tmp}/fab2.XXXXXX") && [ "$(STATUSLINE_NO_FABLE= STATUSLINE_USAGE_URL=file:///nonexistent STATUSLINE_CREDENTIALS_FILE="$T/none.json" STATUSLINE_USAGE_CACHE="$T/usage.json" /bin/bash kit/files/home/.claude/statusline.sh < tests/fixtures/fable-stdin.json 2>&1 | sed "s/$(printf '\033')\[[0-9;]*m//g" | sed -n 2p)" = "10%/100k/1M · 50%/5h (now) · 15%/1w (now) · Fable 33%/1w (now)" ] && test ! -e "$T/usage.json" && [ "$(STATUSLINE_NO_FABLE=1 /bin/bash kit/files/home/.claude/statusline.sh < tests/fixtures/fable-stdin.json 2>&1 | sed "s/$(printf '\033')\[[0-9;]*m//g" | sed -n 2p)" = "10%/100k/1M · 50%/5h (now) · 15%/1w (now)" ] && grep -c 'FAB_SI_PCT=' kit/files/home/.claude/statusline.sh | grep -qx 1 && /bin/bash tests/run.sh | tail -1 | awk '$1 >= 126 && $3 == 0 { ok = 1 } END { exit !ok }' && /bin/bash tests/render-fixtures.sh tests/out/host > /dev/null && [ "$(ls tests/out/host/repo/*.out | wc -l | tr -d ' ')" = 8 ] && rm -rf "$T"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'model_scoped' kit/files/home/.claude/statusline.sh` outputs at least 1; `grep -c 'FAB_SI_PCT=\\(\$ms.utilization // "" | uint)' kit/files/home/.claude/statusline.sh` outputs 1; `grep -c 'FAB_SI_RST=\\(\$ms.resets_at // "" | strings // "")' kit/files/home/.claude/statusline.sh` outputs 1; the `as $ms` binding line number is smaller than the `@sh "` line number inside main()
    - `grep -c '12 @sh-ingested\|12 fields\|12 ingested' kit/files/home/.claude/statusline.sh` outputs at least 1 (ingestion comment counts updated)
    - `jq -r '.rate_limits.model_scoped[0] | [.display_name, .utilization, .resets_at] | @tsv' tests/fixtures/fable-stdin.json` prints `Fable	33	1970-01-01T00:00:00Z`
    - Rendering tests/fixtures/fable-stdin.json with the kill switch empty, URL `file:///nonexistent`, an absent credentials file and an empty cache dir prints stripped line 2 exactly `10%/100k/1M · 50%/5h (now) · 15%/1w (now) · Fable 33%/1w (now)`, exit 0, 0 stderr bytes, and creates no cache file; with `STATUSLINE_NO_FABLE=1` it prints `10%/100k/1M · 50%/5h (now) · 15%/1w (now)`
    - Under /bin/bash, `jq '.rate_limits.model_scoped = "x"' tests/fixtures/fable-stdin.json | STATUSLINE_NO_FABLE= STATUSLINE_USAGE_URL=file:///nonexistent STATUSLINE_CREDENTIALS_FILE=/nonexistent STATUSLINE_USAGE_CACHE=<tmp>/c.json /bin/bash kit/files/home/.claude/statusline.sh` exits 0 with 0 stderr bytes and line 2 `10%/100k/1M · 50%/5h (now) · 15%/1w (now)`; the same with `.rate_limits.model_scoped[0].utilization = ["","touch","tests/.pwned"]` exits 0 and `test ! -e tests/.pwned` succeeds
    - `grep -c 'all 8 fixtures' tests/render-fixtures.sh` outputs 1 and `grep -c 'all 7 fixtures' tests/render-fixtures.sh` outputs 0
    - `/bin/bash tests/run.sh` exits 0 with at least 126 checks and `0 failures`; `/bin/bash tests/render-fixtures.sh tests/out/host` exits 0, `ls tests/out/host/repo/*.out | wc -l` prints 8, and `git status --porcelain tests/out` prints nothing
    - The SUMMARY records the live host render: the stripped line 2 ending in `· Fable NN%/1w (…)` (or, if the network was unavailable, the hidden-segment line plus an INFO note deferring SC1 to the plan 03 human check), the cache `ls -l` mode `-rw-------`, the elapsed whole seconds (expected 0-1), and the statement that no token was printed and nothing under `~/.claude` was written
  </acceptance_criteria>
  <done>The adapter's two sources are wired behind the same seam — stdin `model_scoped` (fixture-proven, no network, no cache) and the OAuth endpoint (file:// fixture-proven and live-proven on the host with the Keychain token) — with the harness and dumper still green and hermetic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| endpoint → script | Untrusted JSON body from an undocumented HTTPS endpoint reaches a jq parse whose output is `eval`ed |
| cache file → script | A world-invisible but user-writable file under ~/.claude is parsed and its numbers reach `$(( ))` |
| credentials file / Keychain → script → curl | The OAuth bearer token crosses from the OS store into a child process |
| stdin payload → script | `rate_limits.model_scoped[]` (untrusted, forward-compat) joins the existing @sh/eval choke point |
| env overrides → script | `STATUSLINE_USAGE_URL` / `STATUSLINE_CREDENTIALS_FILE` / `STATUSLINE_USAGE_CACHE` redirect the fetch, the token source and the cache path — same trust level as the user's shell |
| harness / dumper → host ~/.claude, Keychain, network | Every existing render call would go live without the kill switch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Information Disclosure | fetch_usage curl invocation | medium | mitigate | headers fed via `curl -K -` from a builtin `printf` pipe — the token never enters any process argv (ps-visible); acceptance grep forbids header arguments on the curl command line |
| T-04-02 | Information Disclosure | token / body / account data in stdout, stderr, cache, fixtures, evidence | high | mitigate | `2>/dev/null` on every call; cache holds only `fetched_at/pct/resets_at`; fixtures are the sanitized CONTEXT JSON with no identifiers; harness asserts 0 stderr bytes; SUMMARY records modes/lines only, never token or body |
| T-04-03 | Tampering / Elevation of Privilege | hostile endpoint body, cache file, credentials JSON or stdin `model_scoped` reaching `eval` or `$(( ))` | high | mitigate | every parse is a separate guarded jq program (`uint`/`strings`/`arrays`/`objects`) emitting `@sh` single words; non-JSON → empty output → failure path; `iso_to_epoch` is `case`-pattern-gated before any arithmetic; plan 02 adds the hostile-cache/body/stdin probes under /bin/bash 3.2 |
| T-04-04 | Tampering | torn, partial or symlinked cache file | medium | mitigate | fetch-then-write; `mktemp` template in the cache's own directory + `mv -f` (atomic rename; replaces a symlink rather than following it); any unparseable content → hidden |
| T-04-05 | Information Disclosure | world-readable usage data in a predictable /tmp path | medium | mitigate | default path under `~/.claude/`, created 0600 by `mktemp` (BSD and GNU); never keyed by `$$`; never /tmp |
| T-04-06 | Spoofing | `STATUSLINE_USAGE_URL=https://evil/` exfiltrating the bearer token | medium | accept | same trust boundary as the user's shell / settings.json env; no remote or config-file source for the override; documented only in the script header and tests; default is the HTTPS production URL with TLS verification on |
| T-04-07 | Denial of Service (UX) | blocking network / Keychain in the render path | medium | mitigate | `--max-time ${STATUSLINE_CURL_MAX_TIME:-2}`, 300 s TTL cache, negative results cached, 3600 s grace, kill switch, token read only when a fetch is needed, `expiresAt` pre-check skips guaranteed-401 calls |
| T-04-08 | Tampering | sandbox proxy bypass / TLS downgrade | medium | mitigate | never `--noproxy`/`--proxy ''`/`-k`; curl honours HTTPS_PROXY automatically (verified in the kit sandbox) |
| T-04-09 | Tampering | harness / dumper reading the host Keychain, hitting the network, or writing the real ~/.claude cache | high | mitigate | `export STATUSLINE_NO_FABLE=1` at the top of tests/run.sh and tests/render-fixtures.sh; every live-path probe/verify sets URL, credentials-file and cache overrides into a temp dir; the live host render overrides the cache path |
| T-04-10 | Elevation of Privilege | script refreshing / rewriting the OAuth token and racing Claude Code's own refresh | high | mitigate | read-only token use; no refresh flow, no writes to either store (acceptance grep forbids `security add-`/`refresh`); expired or rejected token → hidden (D-62) |
| T-04-SC | Tampering | package installs | low | accept | none in this plan (bash/jq/curl/security/mktemp are OS-provided); package-legitimacy gate not applicable |
</threat_model>

<verification>
- Cold file:// render of full.json → line 2 `10%/100k/1M · 50%/5h (now) · 15%/1w (now) · Fable 74%/1w (now)`, exit 0, 0 stderr; cache `-rw-------` `{fetched_at,pct:74,resets_at:0}`
- Warm render with a missing URL → same line (no fetch); kill switch → no Fable; absent credentials → no Fable, no cache
- fable-stdin.json → `… · Fable 33%/1w (now)` with no cache file; same payload under the kill switch → no Fable
- `/bin/bash tests/run.sh` ≥ 126 checks, 0 failures; `/bin/bash tests/render-fixtures.sh tests/out/host` PASS with 8 fixtures; `git status --porcelain tests/out` empty
- Live host render with the Keychain token recorded in the SUMMARY (or INFO + deferral to the plan 03 human check); nothing written under ~/.claude; token never printed
</verification>

<success_criteria>
- The Fable weekly segment exists in production shape: separate last peer of line 2, dim `Fable` label, threshold-coloured number, own countdown from the bucket's `resets_at` (FAB-01, D-51/D-53/D-54/D-55)
- Data flows stdin-first then endpoint, behind one adapter seam; token discovered file → Keychain, read-only, never in argv (FAB-02, D-48/D-60/D-62)
- 300 s TTL cache, 0600, atomic, negative results cached, 3600 s grace, 2 s curl bound; hide on every failure (FAB-03/FAB-04, D-56..D-59, D-64)
- Existing harness and dumper remain hermetic and green
</success_criteria>

<output>
Create `.planning/phases/04-fable-weekly-f-segment/04-01-SUMMARY.md` when done.
</output>
