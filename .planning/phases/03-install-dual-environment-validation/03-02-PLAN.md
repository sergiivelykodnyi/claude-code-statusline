---
phase: 03-install-dual-environment-validation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - README.md
  - .planning/research/ARCHITECTURE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [DOCS-01, DOCS-02]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "README.md exists at the repo root, is lean (50–100 lines), and has exactly five level-2 sections in this order: What it shows, Symbol legend, Install on the host, Install in Docker Sandboxes, Requirements — no contributor/development-workflow section (D-40)"
    - "The What-it-shows section is a fenced text block with the current two-line output 'Opus 5 (high) · myproject ⎇ main* ≡ ↓2 ↑3 #2' / '10%/100k/1M · 50%/5h (2h:50m) · 15%/1w (3d:5h:57m)' and the legend is one compact table covering ⎇, *, ≡/≢, ↓N, ↑N, #N, the context segment, and the 5h/1w segments — current output only, no Phase 4 weekly sub-segment anywhere in the file (D-41, DOCS-01)"
    - "Host install documents the one-liner `ln -sf \"$PWD/kit/files/home/.claude/statusline.sh\" ~/.claude/statusline.sh` run from the repo root (D-35), the optional copy variant `rm -f ~/.claude/statusline.sh && cp kit/files/home/.claude/statusline.sh ~/.claude/statusline.sh` with an explicit warning that it overwrites any existing ~/.claude/statusline.sh (D-36), and the note that the symlink install is live — editing or pulling the repo changes the status line immediately (D-42b)"
    - "README carries the statusLine settings snippet as a paste-this JSON block for ~/.claude/settings.json with exactly type command, command ~/.claude/statusline.sh, padding 0, refreshInterval 60 — so reset countdowns keep ticking while the session is idle (D-34, D-43, D-44, D-45, DOCS-02)"
    - "README carries one mock-input verify command that pipes the tests/fixtures/full.json payload into ~/.claude/statusline.sh and states the expected two lines 'Opus 5 (high) · myproject' / '10%/100k/1M · 50%/5h (now) · 15%/1w (now)', plus the claude --debug hint for a blank line — usable on the host and inside a sandbox shell (D-38)"
    - "The Docker Sandboxes section explains in one line why a kit is needed (host ~/.claude is not imported; symlinks cannot cross into the sandbox) and documents both ways to apply it: local — `sbx run claude --kit <absolute path to kit/>` for new sandboxes and `sbx kit add <sandbox> <absolute path to kit/>` for existing ones — and remote — `sbx run claude --kit \"git+https://github.com/sergiivelykodnyi/claude-code-statusline.git#dir=kit\"` with the kit.allowedSources allow-list note (D-37, D-42a)"
    - "Running the README's documented verify payload through kit/files/home/.claude/statusline.sh yields exactly the two documented lines after stripping escape sequences — the README's promise is true against the shipped script"
    - "Host UAT (human, end-of-phase): following only the README's ln -sf one-liner and settings snippet gets the status line rendering in Claude Code on the macOS host, ~/.claude/statusline.sh resolves to the kit path, and the countdowns tick while the session is idle — performed by the user, never by an agent (D-46; roadmap criteria 1 and 3)"
    - ".planning/research/ARCHITECTURE.md and .planning/PROJECT.md no longer claim that sandboxes share the host ~/.claude; both name kit/ as the canonical script location and the sbx kit as the sandbox install route"
  artifacts:
    - path: "README.md"
      provides: "what the status line shows, symbol legend, host symlink/copy install, settings snippet with refreshInterval, verify command, Docker Sandboxes kit install, requirements (DOCS-01, DOCS-02, D-35..D-38, D-40..D-45)"
      contains: "refreshInterval"
      min_lines: 50
    - path: ".planning/research/ARCHITECTURE.md"
      provides: "layout + integration notes updated for kit/ and the sbx kit sandbox route"
      contains: "kit/"
    - path: ".planning/PROJECT.md"
      provides: "installation-model bullet corrected: host symlink into the kit path; sandboxes via the kit"
      contains: "kit/files/home/.claude/statusline.sh"
  key_links:
    - from: "README.md host install one-liner"
      to: "kit/files/home/.claude/statusline.sh"
      via: "ln -sf with an absolute $PWD target from the repo root"
      pattern: "ln -sf \"\\$PWD/kit/files/home/.claude/statusline.sh\""
    - from: "README.md settings snippet"
      to: "~/.claude/statusline.sh"
      via: "statusLine.command literal path; refreshInterval 60 drives idle re-renders"
      pattern: "\"refreshInterval\": 60"
    - from: "README.md Docker Sandboxes section"
      to: "kit/spec.yaml"
      via: "--kit references (local absolute path and git+https#dir=kit) applied at sandbox creation or via sbx kit add"
      pattern: "--kit"
  prohibitions:
    - statement: "README MUST NOT document a copy command that writes through an existing symlink — the copy variant removes the destination first and carries the overwrite warning (D-36)"
    - statement: "README MUST NOT mention the Phase 4 weekly sub-segment or any not-yet-shipped feature — current output only; Phase 4 updates the README when it lands (D-41)"
    - statement: "MUST NOT write to ~/.claude/settings.json or ~/.claude/statusline.sh from any task — applying the snippet and re-pointing the symlink is the user's README-driven host UAT (D-46)"
---

<objective>
Write the README that lets anyone reproduce the install (DOCS-01, DOCS-02): what the status line shows with the example output, the symbol legend, the host symlink one-liner (plus the safe copy variant and its warning), the `statusLine` settings snippet with `refreshInterval` 60 and `padding` 0, a mock-input verify command, and the Docker Sandboxes kit install both local and remote (D-35..D-38, D-40..D-45). Then reconcile the two planning docs that still assume sandboxes share `~/.claude`.

Purpose: the README is the user's install path on the host (D-46 — the user applies it; agents never touch `~/.claude`) and the only place the kit route is explained to a reader who did not sit in the Phase 3 discussion.
Output: `README.md` (new, ~60–80 lines), `.planning/research/ARCHITECTURE.md` and `.planning/PROJECT.md` (scoped one-bullet / one-row edits).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-install-dual-environment-validation/03-CONTEXT.md
@.planning/phases/03-install-dual-environment-validation/03-RESEARCH.md
@.planning/phases/03-install-dual-environment-validation/03-PATTERNS.md
@project-brief.md
@tests/fixtures/full.json
@tests/run.sh
@kit/spec.yaml
</context>

## Artifacts this phase produces

See plan 03-01 for the full Phase 3 list. This plan creates:

- `README.md` — new file at the repo root with five level-2 sections (this plan)
- Edits: `.planning/research/ARCHITECTURE.md` (Recommended Project Structure tree + the Filesystem row of Integration Points), `.planning/PROJECT.md` (the Installation-model bullet) (this plan)
- No code symbols, no new scripts.

## Flagged assumptions (spec-less edge probe — no silent drops)

1. **DOCS-01 (unclassified):** "the `ln -s` command" is documented as `ln -sf` with an absolute `$PWD` target run from the repo root (D-35) — `-f` because the host already has a file/link at `~/.claude/statusline.sh` that must be replaced; `$PWD` so the link works from any other cwd. The example layout follows the implementation, not the brief: `↓N` = behind, `↑N` = ahead (the brief's wording is swapped), `50%/5h` on the 5-hour segment (the brief has a typo).
2. **DOCS-02 (unclassified):** the snippet is exactly D-34 with `refreshInterval` 60 (D-43) and `padding` 0 (D-44), presented as a JSON block to merge by hand into `~/.claude/settings.json` (D-45) — no host jq one-liner (deferred idea, out of scope).
3. **Repository name:** the README's remote kit reference uses the real GitHub repo `sergiivelykodnyi/claude-code-statusline` (research-verified `git remote -v`), not the CONTEXT D-37 spelling; research A6 assumes the repo is public for anonymous `git+https` access — flagged, not verifiable offline.
4. **`sbx kit add` for existing sandboxes (research Open Question 1):** documented per the v0.39.0 CLI help (container recreated with the kit appended); plan 03 verifies it live and reconciles this sentence if files do not land.
5. **`kit.allowedSources` note:** format taken from the kits docs via research Pitfall 6 (daemon was stopped, not locally verified); worded as an example.

<tasks>

<task type="auto">
  <name>Task 1: Write README.md — what it shows, legend, host install + settings snippet + verify, Docker Sandboxes kit install, requirements</name>
  <files>README.md</files>
  <read_first>.planning/phases/03-install-dual-environment-validation/03-CONTEXT.md (D-35..D-38, D-40..D-46 and §Specific Ideas — the snippet to document verbatim), .planning/phases/03-install-dual-environment-validation/03-RESEARCH.md (§Code Examples: README host install, settings snippet, verify command; §Pattern 2 host facts; §Pattern 4 sbx commands; §Common Pitfalls 5-7; §Anti-Patterns — real repo name), .planning/phases/03-install-dual-environment-validation/03-PATTERNS.md (§README.md — tone/structure analogs, legend mapping, pinned expected strings), project-brief.md (tone and the two-line example shape), tests/fixtures/full.json (the verify payload), tests/run.sh (lines 100-108 — the pinned expected line strings), kit/spec.yaml (the merged statusLine object the snippet must match byte-for-byte, D-34)</read_first>
  <reversibility rating="reversible">Documentation text; any wording change is a single-file edit.</reversibility>
  <action>
    Create `README.md` at the repo root — lean, 60–80 lines target (hard bounds 50–100), five level-2 sections in this exact order (D-40), terse tone like project-brief.md, no contributor/development-workflow section:

    1. Title `# Claude Code Status Line` + one sentence: a two-line bash status line for Claude Code showing model/effort, directory, git state, context usage and rate limits; same script on the macOS host and inside Docker Sandboxes.

    2. `## What it shows` — a fenced `text` block with exactly these two lines: `Opus 5 (high) · myproject ⎇ main* ≡ ↓2 ↑3 #2` and `10%/100k/1M · 50%/5h (2h:50m) · 15%/1w (3d:5h:57m)`. Current output only — the Phase 4 weekly sub-segment from the project brief must NOT appear in the example, the legend, or anywhere else (D-41). One sentence after the block: percentages turn yellow at 70% and red at 90%; segments with no data (no git repo, no effort, no rate-limit data yet) disappear together with their separators.

    3. `## Symbol legend` — ONE compact markdown table (Token | Meaning) with these rows, wording following the implementation (`↓` = behind, `↑` = ahead): `Opus 5 (high)` model name with suffixes like `(1M context)` stripped, plus reasoning effort (hidden when the model has none); `myproject` basename of the working directory; `⎇ main` current branch (short SHA when detached) — whole git segment hidden outside a repo; `*` working tree has changes or untracked files; `≡` / `≢` branch has / has no upstream; `↓2` commits behind upstream (hidden at 0; from the last fetch — the script never fetches); `↑3` commits ahead of upstream (hidden at 0); `#2` stash count (hidden at 0); `10%/100k/1M` context used as percent / tokens / window size; `50%/5h (2h:50m)` 5-hour rate-limit usage and time to reset; `15%/1w (3d:5h:57m)` weekly rate-limit usage and time to reset.

    4. `## Install on the host` (macOS) — in this order:
       (a) Symlink (recommended), run from the repo root, in a fenced `sh` block: `ln -sf "$PWD/kit/files/home/.claude/statusline.sh" ~/.claude/statusline.sh` (D-35). One-line note (D-42b): with the symlink, editing or pulling this repo changes the live status line immediately; use the copy variant if you want a static copy.
       (b) Copy variant (optional), fenced `sh` block: `rm -f ~/.claude/statusline.sh && cp kit/files/home/.claude/statusline.sh ~/.claude/statusline.sh` — followed by an explicit warning line (bold "Warning:") that this overwrites any existing `~/.claude/statusline.sh` (D-36). Do not document a bare copy without the preceding remove — BSD/GNU cp write through an existing symlink into the repo.
       (c) Settings: sentence "Add this to `~/.claude/settings.json` (merge into the existing object, keep your other keys):" followed by a fenced `json` block that is exactly the D-34 object pretty-printed with 2-space indent: top-level key `statusLine` with `"type": "command"`, `"command": "~/.claude/statusline.sh"`, `"padding": 0`, `"refreshInterval": 60` (D-43, D-44, D-45). One line after it: `refreshInterval` re-runs the script every 60 s so the reset countdowns keep ticking while the session is idle; the status line appears after your next interaction.
       (d) Verify, fenced `sh` block: the exact one-line `echo '<payload>' | ~/.claude/statusline.sh` where `<payload>` is the tests/fixtures/full.json object (model display_name `Opus 5 (1M context)`, effort high, workspace current_dir `/tmp/myproject`, context_window used_percentage 10.4 / total_input_tokens 100000 / context_window_size 1000000, rate_limits five_hour 50.9 / resets_at 0 and seven_day 15.2 / resets_at 0 — resets_at 0 renders "now" forever, so the output is stable), with two comment lines giving the expected (colored) output `Opus 5 (high) · myproject` and `10%/100k/1M · 50%/5h (now) · 15%/1w (now)` (D-38). One line: if the line stays blank in Claude Code, run `claude --debug` (it logs the script's exit code/stderr and workspace-trust skips) and check `chmod +x`.

    5. `## Install in Docker Sandboxes` — one-line why (D-42a): Docker Sandboxes do not import your host `~/.claude` and cannot follow symlinks to host paths, so the script is delivered by the sbx kit in `kit/` (it copies the script to `/home/agent/.claude/statusline.sh` and sets the same `statusLine` settings at every start). Then both ways, reader's choice (D-37), fenced `sh` blocks:
       - Local directory (use the ABSOLUTE path to this repo's `kit/` — other projects' sandboxes do not mount this repo): new sandbox `sbx run claude --kit /absolute/path/to/claude-code-status-line/kit`; existing sandbox `sbx kit add <sandbox-name> /absolute/path/to/claude-code-status-line/kit`.
       - Remote: `sbx run claude --kit "git+https://github.com/sergiivelykodnyi/claude-code-statusline.git#dir=kit"` — with a note that `kit.allowedSources` may need the GitHub prefix allow-listed, e.g. `sbx settings set kit.allowedSources '["docker.io/","github.com/sergiivelykodnyi/"]'`, and that `#ref=<tag-or-commit>&dir=kit` pins a version. Use the REAL repo name `claude-code-statusline` (research-verified remote), not the directory name.
       - One line: the status line shows up after the first message in the sandbox; the same verify command works inside a sandbox shell (`sbx exec <sandbox-name> ...`).

    6. `## Requirements` — short bullet list: bash 3.2+ (macOS `/bin/bash` is fine), `jq`, `git`; Claude Code ≥ 2.1.x (rate-limit fields and `refreshInterval`); for sandboxes, Docker Sandboxes `sbx` ≥ 0.39 (kit spec v2).

    Keep every command copy-pasteable and every path literal as written above; no screenshots, no badges, no Phase 4 content. After writing, count lines (`wc -l`) and trim or tighten until within 50–100 (aim 60–80). Do not touch anything under ~/.claude while working (D-46).
  </action>
  <verify>
    <automated>test -f README.md && [ "$(wc -l < README.md | tr -d ' ')" -ge 50 ] && [ "$(wc -l < README.md | tr -d ' ')" -le 100 ] && [ "$(grep -c '^## ' README.md)" = 5 ] && grep -Fq 'ln -sf "$PWD/kit/files/home/.claude/statusline.sh" ~/.claude/statusline.sh' README.md && grep -Fq '"refreshInterval": 60' README.md && grep -Fq '"padding": 0' README.md && grep -Fq 'rm -f ~/.claude/statusline.sh && cp kit/files/home/.claude/statusline.sh ~/.claude/statusline.sh' README.md && grep -Fq 'git+https://github.com/sergiivelykodnyi/claude-code-statusline.git#dir=kit' README.md && [ "$(grep -ci 'fable' README.md)" = 0 ] && [ "$(grep -Fc 'f(' README.md)" = 0 ]
    <human-check>
      <test>On the macOS host, from this repo's root, follow ONLY the README: run the `ln -sf` one-liner, merge the README's statusLine JSON into ~/.claude/settings.json (keep your other keys), then start `claude` in this repo, send one message, and look at the status line; leave the session idle for at least two minutes and watch the countdowns. Then run `ls -l ~/.claude/statusline.sh`.</test>
      <expected>The two-line status line renders: line 1 shows the model and effort, `claude-code-status-line`, and `⎇ main` with the live git markers; line 2 shows context usage and the 5h/1w segments with countdowns. While idle (no new message) the countdown values change, proving refreshInterval 60 re-renders. `ls -l` shows ~/.claude/statusline.sh is a symlink whose target ends in /kit/files/home/.claude/statusline.sh (D-35, D-46; roadmap criteria 1 and 3).</expected>
      <why_human>Requires editing files outside the repo (agents never touch ~/.claude — D-46) and observing a live Claude Code TUI over time.</why_human>
    </human-check>
  </verify>
  <acceptance_criteria>
    - `test -f README.md` succeeds; `wc -l < README.md` is between 50 and 100 inclusive; `grep -c '^## ' README.md` outputs 5
    - `grep -Fc 'Opus 5 (high) · myproject ⎇ main* ≡ ↓2 ↑3 #2' README.md` outputs 1 and `grep -Fc '10%/100k/1M · 50%/5h (2h:50m) · 15%/1w (3d:5h:57m)' README.md` outputs 1
    - `grep -Fc 'ln -sf "$PWD/kit/files/home/.claude/statusline.sh" ~/.claude/statusline.sh' README.md` outputs 1
    - `grep -Fc 'rm -f ~/.claude/statusline.sh && cp kit/files/home/.claude/statusline.sh ~/.claude/statusline.sh' README.md` outputs 1 and `grep -ci 'overwrite' README.md` outputs ≥ 1
    - `grep -Fc '"statusLine"' README.md` outputs 1; `grep -Fc '"type": "command"' README.md` outputs 1; `grep -Fc '"command": "~/.claude/statusline.sh"' README.md` outputs 1; `grep -Fc '"padding": 0' README.md` outputs 1; `grep -Fc '"refreshInterval": 60' README.md` outputs 1
    - `grep -Fc '| ~/.claude/statusline.sh' README.md` outputs ≥ 1; `grep -Fc '50%/5h (now) · 15%/1w (now)' README.md` outputs 1; `grep -Fc 'claude --debug' README.md` outputs ≥ 1
    - `grep -Fc 'sbx run claude --kit' README.md` outputs ≥ 2 (local and remote); `grep -Fc 'sbx kit add' README.md` outputs ≥ 1; `grep -Fc 'git+https://github.com/sergiivelykodnyi/claude-code-statusline.git#dir=kit' README.md` outputs 1; `grep -Fc 'kit.allowedSources' README.md` outputs ≥ 1
    - `grep -Fc 'do not import' README.md` outputs ≥ 1 (D-42a) and `grep -Fc 'immediately' README.md` outputs ≥ 1 (D-42b)
    - `grep -Fc 'bash 3.2' README.md` outputs ≥ 1; `grep -Fc 'Claude Code' README.md` outputs ≥ 1
    - `grep -ci 'fable' README.md` outputs 0; `grep -Fc 'f(' README.md` outputs 0; `grep -Fc '## Development' README.md` outputs 0
    - `ls -l ~/.claude/statusline.sh` and `jq -c .statusLine ~/.claude/settings.json` produce identical output before and after the task (both recorded in the SUMMARY — agents never edit ~/.claude, D-46)
  </acceptance_criteria>
  <done>README.md documents everything a reader needs — example output, legend, host symlink/copy install with warning, the settings snippet with refreshInterval 60, a verify command, both sandbox kit routes, requirements — within the D-40 shape, with no Phase 4 content and no agent edits outside the repo.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile planning docs with the kit layout and prove the README's verify promise against the shipped script</name>
  <files>.planning/research/ARCHITECTURE.md, .planning/PROJECT.md</files>
  <read_first>README.md (as written in Task 1 — the verify payload and expected lines), .planning/research/ARCHITECTURE.md (the "Recommended Project Structure" tree around lines 70-81 and the "Integration Points → External Services" table row for `Filesystem (~/.claude)` around line 251), .planning/PROJECT.md (the "Installation model" bullet around line 72 that says sandboxes mount/share the host ~/.claude), kit/files/home/.claude/statusline.sh (the script the README promises about)</read_first>
  <action>
    1. `.planning/research/ARCHITECTURE.md` — two scoped edits (Edit, not rewrite): in the "Recommended Project Structure" tree, replace the single `statusline.sh` root entry with the kit layout — `kit/spec.yaml` (sbx mixin kit manifest) and `kit/files/home/.claude/statusline.sh` (the canonical script; host symlink target and kit static file) — keeping the inner section list, and keep `README.md` / `tests/` lines (tests/ now also lists `run.sh`, `render-fixtures.sh`, `sandbox.sh`, `fixtures/`). In the Integration Points table, rewrite the `Filesystem (~/.claude)` row's pattern/notes to: host — `ln -sf` from `~/.claude/statusline.sh` into the kit path; Docker Sandboxes — the `kit/` sbx mixin kit copies the script to `/home/agent/.claude/statusline.sh` and merges the `statusLine` key at every start, because sandboxes do not import the host `~/.claude`; same file runs on both platforms — no build step.

    2. `.planning/PROJECT.md` — one scoped edit: rewrite the "Installation model" bullet to state that this repo is the source of truth; on the host `kit/files/home/.claude/statusline.sh` is symlinked to `~/.claude/statusline.sh`; Docker Sandboxes do not import the host `~/.claude` (not imported), so the `kit/` sbx mixin kit installs the same file there. Touch nothing else in PROJECT.md (Key Decisions table and requirements lists are managed by GSD).

    3. README contract check (no file output — evidence for the SUMMARY): extract the payload from the README's verify command (the JSON between the single quotes after `echo`), pipe it through `/bin/bash kit/files/home/.claude/statusline.sh`, strip escape sequences, and confirm line 1 is exactly `Opus 5 (high) · myproject` and line 2 is exactly `10%/100k/1M · 50%/5h (now) · 15%/1w (now)` — identical to the README's stated expectation and to the pinned strings in tests/run.sh. If they differ, fix the README (never the script). Also re-run `/bin/bash tests/run.sh` to confirm the harness is still green after the docs work (nothing in the render path changed).
  </action>
  <verify>
    <automated>grep -Fq 'kit/files/home/.claude/statusline.sh' .planning/PROJECT.md && grep -Fq 'kit/' .planning/research/ARCHITECTURE.md && grep -Fq 'not imported' .planning/PROJECT.md && P=$(grep -F "| ~/.claude/statusline.sh" README.md | head -1 | sed "s/^[^']*'//; s/'.*$//") && printf '%s' "$P" | /bin/bash kit/files/home/.claude/statusline.sh | sed "s/$(printf '\033')\[[0-9;]*m//g" | sed -n 2p | grep -Fqx '10%/100k/1M · 50%/5h (now) · 15%/1w (now)'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -Fc 'kit/files/home/.claude/statusline.sh' .planning/PROJECT.md` outputs ≥ 1 and `grep -Fc 'not imported' .planning/PROJECT.md` outputs ≥ 1
    - `grep -Fc 'kit/spec.yaml' .planning/research/ARCHITECTURE.md` outputs ≥ 1 and `grep -Fc 'kit/files/home/.claude/statusline.sh' .planning/research/ARCHITECTURE.md` outputs ≥ 1
    - The README verify payload piped through `/bin/bash kit/files/home/.claude/statusline.sh` renders (escape-stripped) line 1 `Opus 5 (high) · myproject` and line 2 `10%/100k/1M · 50%/5h (now) · 15%/1w (now)`
    - `/bin/bash tests/run.sh` exits 0 with `0 failures`
    - `git diff --stat` for this task touches only .planning/research/ARCHITECTURE.md and .planning/PROJECT.md (and README.md only if the contract check required a wording fix)
  </acceptance_criteria>
  <done>Planning docs no longer carry the false shared-~/.claude assumption and name the kit layout; the README's verify promise is proven true against the shipped script.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README reader → host filesystem | Copy-pasted commands modify ~/.claude on the reader's machine |
| README reader → remote kit source | A git+https kit reference fetches executable content from GitHub into a sandbox |
| README → Claude Code settings | The documented statusLine command is executed by Claude Code on every render |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering | README copy variant vs. an existing symlink | low | mitigate | Documented form removes the destination first (`rm -f ... && cp ...`) and carries the D-36 overwrite warning; bare copy form never shown |
| T-03-07 | Tampering (supply chain) | README remote `git+https` kit reference | medium | mitigate | Local absolute `--kit` path documented first as the primary route; `kit.allowedSources` prefix allow-list note (deny-by-default outside docker.io); `#ref=` pin note; real repo name used |
| T-03-08 | Information Disclosure | README settings snippet / verify payload | low | accept | Fixed literal path and a synthetic payload; no secrets, no user data |
| T-03-09 | Elevation of Privilege | Claude Code executes the documented command path | low | accept | Literal `~/.claude/statusline.sh` (D-34); no user-controlled interpolation; script content is repo-reviewed |

No package installs in this plan — the package-legitimacy gate does not apply.
</threat_model>

<verification>
- README.md present, 50–100 lines, five `##` sections, all D-35..D-38 / D-40..D-45 strings present, no Phase 4 content
- README verify payload renders the documented two lines through the shipped script
- ARCHITECTURE.md and PROJECT.md updated with scoped edits only; harness still green
- Host UAT (human, end-of-phase): ln -sf + snippet → status line renders on the host, countdowns tick while idle, symlink resolves to the kit path (D-46; criteria 1 and 3)
</verification>

<success_criteria>
- Anyone can reproduce the host install from the README alone (DOCS-01, DOCS-02; roadmap criterion 4)
- The settings snippet carries `refreshInterval: 60` so countdowns tick while idle (roadmap criterion 3; D-43)
- The sandbox kit route is documented both ways with the real repository name (D-37)
</success_criteria>

<output>
Create `.planning/phases/03-install-dual-environment-validation/03-02-SUMMARY.md` when done.
</output>
