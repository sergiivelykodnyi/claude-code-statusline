---
phase: 03-install-dual-environment-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kit/spec.yaml
  - kit/files/home/.claude/statusline.sh
  - statusline.sh
  - tests/run.sh
  - tests/render-fixtures.sh
  - .gitignore
autonomous: true
requirements: [PORT-01, PORT-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The canonical script lives at kit/files/home/.claude/statusline.sh with git index mode 100755 and byte-identical content to the pre-move statusline.sh; no statusline.sh and no shim remain at the repo root (D-31)"
    - "kit/ is a valid sbx mixin kit: `sbx kit validate kit` exits 0 offline; kit/spec.yaml declares schemaVersion \"2\", kind mixin, requires.agent claude, an install-time jq/git guard (D-33), and a root startup command that waits for the platform settings seed, then jq-merges ONLY the statusLine key to exactly {type:\"command\",command:\"~/.claude/statusline.sh\",padding:0,refreshInterval:60}, chmod 0755s the delivered script and hands ownership back to agent (D-30, D-32, D-34)"
    - "/bin/bash tests/run.sh runs against the relocated path, carries a new exec-bit check that is proven to bite on mode loss, and exits 0 with at least 126 checks and 0 failures"
    - "/bin/bash tests/render-fixtures.sh OUTDIR writes raw, escape-sequence-preserving renders of all 7 fixtures through the repo path into OUTDIR/repo and — when the installed ~/.claude/statusline.sh is executable — through the installed path into OUTDIR/installed, then diff -r's the two and exits non-zero on any byte difference: the per-environment half of PORT-04 and the input to the host-vs-sandbox byte diff (PORT-01, D-39)"
    - "Running tests/render-fixtures.sh tests/out/host on the host today reports the installed path byte-identical to the repo path (PORT-04 host half) without any agent write under ~/.claude (D-46); tests/out/ is gitignored so both environments can write evidence into the repo (TMPDIR-independent)"
    - "Executing kit/files/home/.claude/statusline.sh directly (shebang + exec bit) on tests/fixtures/full.json renders 'Opus 5 (high) · myproject' / '10%/100k/1M · 50%/5h (now) · 15%/1w (now)' — the relocation changed nothing about the render (PORT-01)"
  artifacts:
    - path: "kit/spec.yaml"
      provides: "sbx mixin kit manifest — static file delivery, install-time dependency guard, idempotent startup statusLine merge (D-30, D-32, D-33, D-34)"
      contains: "schemaVersion: \"2\""
    - path: "kit/files/home/.claude/statusline.sh"
      provides: "the canonical status line script, relocated unchanged (D-31)"
      contains: "Source guard"
      min_lines: 220
    - path: "tests/run.sh"
      provides: "regression harness re-pointed to the kit path with an exec-bit check (PORT-04)"
      contains: "exec bit"
    - path: "tests/render-fixtures.sh"
      provides: "raw fixture render dumper for byte-for-byte cross-environment comparison (D-39, PORT-01, PORT-04)"
      contains: "diff -r"
    - path: ".gitignore"
      provides: "tests/out/ ignored so host and sandbox evidence can live inside the repo"
      contains: "tests/out/"
  key_links:
    - from: "kit/spec.yaml"
      to: "kit/files/home/.claude/statusline.sh"
      via: "static file under files/home/ lands at /home/agent/.claude/statusline.sh at creation; startup command chmod 0755s it and points statusLine.command at ~/.claude/statusline.sh"
      pattern: "~/.claude/statusline.sh"
    - from: "tests/run.sh"
      to: "kit/files/home/.claude/statusline.sh"
      via: "SL= path every fixture, probe, and git-matrix render goes through"
      pattern: "SL=kit/files/home/.claude/statusline.sh"
    - from: "tests/render-fixtures.sh"
      to: "$HOME/.claude/statusline.sh"
      via: "installed-path render (direct execution through the symlink/copy) compared by diff -r against the repo-path render"
      pattern: "installed"
  prohibitions:
    - statement: "MUST NOT change the content of statusline.sh in this phase — the move is a pure relocation (git rename, byte-identical content, mode 100755); any render-path change belongs to a later phase (D-31, PORT-01)"
    - statement: "MUST NOT let the kit replace, truncate, or wholesale-rewrite /home/agent/.claude/settings.json — the startup command sets only the statusLine key through an atomic tmp+mv jq merge and preserves every other key (D-32)"
    - statement: "MUST NOT write to, replace, or re-point anything under the user's host ~/.claude (statusline.sh, settings.json) — executing the installed script read-only for evidence is allowed; editing it is the user's README-driven action (D-46)"
---

<objective>
**As a** Claude Code user who works on the macOS host and inside Docker Sandboxes, **I want to** install this status line once from the repo — a symlink on the host, an sbx mixin kit in sandboxes — **so that** the same script renders identically wherever Claude runs.

This plan is the tracer slice plus the measurement tooling: relocate the canonical `statusline.sh` into the kit directory (D-31), ship it as a valid `kind: mixin` sbx kit whose startup command idempotently merges the D-34 `statusLine` object (D-30, D-32, D-33), re-point the 125-check harness to the new path and add the exec-bit check, and add `tests/render-fixtures.sh` so host and sandbox renders can be diffed byte-for-byte (D-39, PORT-01, PORT-04).

Purpose: after this plan the install artifact exists at its final location, is a validated sbx kit, and is proven unchanged by the move — everything plan 02 (README) documents and plan 03 (live sandbox) measures already works on the host.
Output: `kit/spec.yaml`, `kit/files/home/.claude/statusline.sh` (moved), `tests/run.sh` (re-pointed, +1 check), `tests/render-fixtures.sh` (new), `.gitignore` (+`tests/out/`), `tests/out/host/` evidence (gitignored).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-install-dual-environment-validation/03-CONTEXT.md
@.planning/phases/03-install-dual-environment-validation/03-RESEARCH.md
@.planning/phases/03-install-dual-environment-validation/03-PATTERNS.md
@.claude/CLAUDE.md
@statusline.sh
@tests/run.sh
@.gitignore
</context>

## Artifacts this phase produces

New symbols and files created across Phase 3 (plans 01-03). The source-grounding pass excludes these from drift verification:

- `kit/` — new directory: the sbx mixin kit (D-30)
- `kit/spec.yaml` — new kit manifest: fields `schemaVersion`, `kind`, `name` (`claude-code-status-line`), `version`, `displayName`, `description`, `requires.agent`, `setup.install[].command` (string), `setup.startup[].command` (string array) with `user: "0"` (this plan)
- `kit/files/home/.claude/statusline.sh` — the relocated canonical script (`git mv` of `statusline.sh`, content unchanged) (this plan)
- `tests/run.sh` — `SL=` re-pointed; new harness check named `exec bit: ...` ; shellcheck advisory extended to `tests/*.sh`; new `INFO installed:` advisory line (this plan)
- `tests/render-fixtures.sh` — new helper; env inputs `INSTALLED` (installed-path override) and `REQUIRE_INSTALLED`; output layout `OUTDIR/repo/<fixture>.out` and `OUTDIR/installed/<fixture>.out` (this plan)
- `.gitignore` — new entry `tests/out/` (this plan)
- `tests/out/<env>/...` — gitignored evidence directories (`host`, `sandbox`) (this plan + plan 03)
- `README.md` — new, repo root (plan 02)
- `tests/sandbox.sh` — new host-side orchestrator; sandbox names `statusline-kit-test` and `statusline-kit-test-add`; `--rm` and `--help` flags; `tests/out/sandbox/EVIDENCE.txt` and `tests/out/sandbox/run.log` (plan 03)
- Removed: `statusline.sh` at the repo root (no shim — D-31)

## Flagged assumptions (spec-less edge probe — no silent drops)

No SPEC.md exists for this phase; the deterministic edge probe returned every requirement `unclassified → unresolved`. Each row is surfaced here as an explicit assumption for the verifier:

1. **PORT-01 (unclassified):** "identical output" is operationalized as byte-identical raw renders (escape sequences included) of the 7 deterministic fixtures through the repo path, host vs sandbox (`diff -r tests/out/host/repo tests/out/sandbox/repo` empty), plus the full harness exiting 0 with 0 failures in both environments. The git-state matrix and latency sections are NOT byte-compared across environments (temp-repo SHAs and timings differ) — their PASS/FAIL summary is the evidence for that part.
2. **PORT-04 (unclassified):** on the host the symlink is created by the user following the README (D-46); today `~/.claude/statusline.sh` is a regular-file copy with identical bytes (research-verified), so the installed-path render half of `tests/render-fixtures.sh` passes before and after the re-point. In the sandbox the installed path is a kit-delivered regular file, not a symlink — by design (D-30: symlinks to host paths cannot cross into the sandbox), so PORT-04's "symlink ... in both environments" is satisfied through the kit on the sandbox side. Flagged for verifier acceptance.
3. **Exec-bit preservation by the sandbox engine** (research A5, MEDIUM): the startup `chmod 0755` is insurance; the harness exec-bit check and plan 03's `test -x` inside the sandbox confirm it.
4. **`sbx exec` default user** (research A4): assumed `agent`; `tests/render-fixtures.sh` accepts `INSTALLED=` so plan 03 pins `/home/agent/.claude/statusline.sh` explicitly instead of trusting `$HOME`.

Canon referral (not minted as a prohibition): command injection via hostile stdin values and supply-chain trust of remote kit references are canon security — covered by the Phase 2 harness probes and /gsd-secure-phase; not re-minted here.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — relocate statusline.sh into the sbx mixin kit, validate the kit offline, re-point the harness</name>
  <files>kit/spec.yaml, kit/files/home/.claude/statusline.sh, statusline.sh, tests/run.sh</files>
  <read_first>statusline.sh (confirm it is self-contained: no $0/BASH_SOURCE-relative paths; source guard at the bottom; never-fail header), tests/run.sh (lines 1-12 header + SL assignment, lines 32-41 check_ok, lines 47-50 syntax-gate shape to copy for the exec-bit check, lines 409-414 shellcheck advisory shape), .planning/phases/03-install-dual-environment-validation/03-RESEARCH.md (§Pattern 1 — the verbatim recommended kit/spec.yaml and the v0.39.0 grammar facts; §Pattern 2 host facts; §Common Pitfalls 1-4; §Harness relocation), .planning/phases/03-install-dual-environment-validation/03-PATTERNS.md (kit/spec.yaml conventions, tests/run.sh edit excerpts), .planning/phases/03-install-dual-environment-validation/03-CONTEXT.md (D-30..D-34, D-46), .claude/CLAUDE.md (bash 3.2 / BSD-GNU portability table)</read_first>
  <reversibility rating="costly">D-30/D-31 (carried from CONTEXT.md): the README install story, the harness path, and the sandbox evidence all build on the kit location — moving the script again means re-documenting and re-verifying both environments.</reversibility>
  <action>
    1. Relocate (D-31): `mkdir -p kit/files/home/.claude` then `git mv statusline.sh kit/files/home/.claude/statusline.sh`. Do not open or edit the script content; do not leave a shim or wrapper at the repo root. Confirm `git ls-files -s kit/files/home/.claude/statusline.sh` shows mode 100755.

    2. Create `kit/spec.yaml` (D-30) following RESEARCH.md §Pattern 1 exactly — grammar verified against the installed `sbx` v0.39.0: top-level `schemaVersion: "2"` (quoted string, mandatory), `kind: mixin`, `name: claude-code-status-line`, `version: "1.0.0"`, `displayName: Claude Code Status Line`, a one-sentence `description` (ships ~/.claude/statusline.sh and sets the statusLine key via a jq merge that preserves all other keys), `requires: { agent: claude }` (settings.json is only read by Claude Code; inert on other agents). `setup.install` holds ONE entry whose `command` is a single STRING (not an array): guard with `command -v jq` and `command -v git` and fall back to `apt-get update && apt-get install -y --no-install-recommends jq git` (D-33 — both already ship in the claude-code base image, so this is a no-op insurance run once at creation); give it a `description`. `setup.startup` holds ONE entry whose `command` is a STRING ARRAY of exactly `sh`, `-c`, and the script text, with `user: "0"` and a `description`. The script (POSIX sh, runs on Linux as root — GNU tools are acceptable HERE ONLY) does, in order: `set -e`; `H=/home/agent`; `S=$H/.claude/settings.json`; `mkdir -p "$H/.claude"`; poll up to 60 one-second sleeps until `grep -q themeId "$S"` succeeds (the platform seeds settings.json late at create time and overwrites whatever exists — research Pitfall 2 / contrib claude-mem precedent), then continue regardless of the poll outcome; `[ -f "$S" ] || printf '{}\n' > "$S"` (jq exits 2 on a nonexistent path); `tmp=$(mktemp "$H/.claude/.settings.XXXXXX")`; run `jq -n` with the filter `try (input) catch {} | (if type=="object" then . else {} end) | .statusLine = {type:"command",command:"~/.claude/statusline.sh",padding:0,refreshInterval:60}` reading `"$S"` and writing to `"$tmp"` (so empty/invalid/non-object content still yields a valid object); `mv "$tmp" "$S"`; `chmod 0755 "$H/.claude/statusline.sh" || true` (exec-bit insurance, research A5); `chown agent:agent "$H/.claude" "$S" "$H/.claude/statusline.sh"` (non-recursive — no walk over session data). The merged object MUST be exactly the four D-34 keys in that order with those values — it is byte-for-byte the host snippet plan 02 documents. Comment the non-obvious lines with the decision they serve (`# D-32`, `# D-33`, `# D-34`, `# Pitfall 2`) — house style. Do NOT target ~/.claude/settings.json with a static file or `setup.files` (docs-reserved path; only the runtime merge is allowed).

    3. Validate offline (works with the daemon stopped): `sbx kit validate kit` must exit 0 and `sbx kit inspect --json kit` must list the statusline.sh static file. If validation rejects anything, fix the grammar per RESEARCH Pitfall 1 (install command = string, startup command = string array, schemaVersion "2") — never downgrade the schema or drop the setup block.

    4. Re-point the harness (D-31 / PATTERNS §tests/run.sh): in tests/run.sh update the line-2 header comment to name the new path; change the `SL=` assignment value to `kit/files/home/.claude/statusline.sh` (every downstream `$SL` use — syntax gate, sourcing, run_fixture, probes, git matrix, latency — needs no change); directly below the section-1 syntax gate add the exec-bit check in the same two-line shape: `[ -x "$SL" ]` followed by `check_ok "exec bit: $SL is executable (PORT-04)" $?`; in section 10 extend the shellcheck advisory to lint `"$SL" tests/*.sh` (still `|| true`, still INFO-only) and add an advisory line that prints `INFO installed: ` followed by `ls -l "$HOME/.claude/statusline.sh"` when that path exists (never a check — the harness must stay hermetic inside the sandbox). Keep bash 3.2 / BSD-GNU-neutral syntax throughout.

    5. Prove the new check bites (Phase 2 harness convention): `chmod -x kit/files/home/.claude/statusline.sh`, run `/bin/bash tests/run.sh` and observe the `FAIL exec bit:` line, then `chmod +x` it back, re-run and observe `PASS exec bit:`; confirm `git ls-files -s` still shows 100755 afterwards. Record both runs' summary lines in the SUMMARY.

    6. Tracer end-to-end on the host: execute `kit/files/home/.claude/statusline.sh < tests/fixtures/full.json` DIRECTLY (not via `/bin/bash path` — this exercises the shebang and exec bit the way Claude Code and the symlink will) and confirm the two stripped lines are `Opus 5 (high) · myproject` and `10%/100k/1M · 50%/5h (now) · 15%/1w (now)`; confirm byte identity of the relocation with `git show 64b3c1b:statusline.sh | cmp - kit/files/home/.claude/statusline.sh` (64b3c1b is the pre-phase HEAD). Do not touch anything under ~/.claude (D-46).
  </action>
  <verify>
    <automated>sbx kit validate kit && test ! -e statusline.sh && git show 64b3c1b:statusline.sh | cmp - kit/files/home/.claude/statusline.sh && /bin/bash tests/run.sh | tail -1 | awk '$1 >= 126 && $3 == 0 { ok = 1 } END { exit !ok }'</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -e statusline.sh` succeeds (no script and no shim at the repo root) and `test -f kit/files/home/.claude/statusline.sh` succeeds
    - `git ls-files -s kit/files/home/.claude/statusline.sh` prints a line starting with `100755`
    - `git show 64b3c1b:statusline.sh | cmp - kit/files/home/.claude/statusline.sh` exits 0 (byte-identical relocation)
    - `sbx kit validate kit` exits 0; `sbx kit inspect --json kit | grep -c 'statusline.sh'` outputs a number ≥ 1
    - `grep -c '^schemaVersion: "2"$' kit/spec.yaml` outputs 1; `grep -c '^kind: mixin$' kit/spec.yaml` outputs 1; `grep -c 'agent: claude' kit/spec.yaml` outputs ≥ 1
    - `grep -c 'command -v jq' kit/spec.yaml` outputs ≥ 1; `grep -c 'themeId' kit/spec.yaml` outputs ≥ 1; `grep -Fc 'refreshInterval:60' kit/spec.yaml` outputs 1; `grep -Fc 'padding:0' kit/spec.yaml` outputs 1; `grep -Fc 'command:"~/.claude/statusline.sh"' kit/spec.yaml` outputs 1; `grep -c 'chmod 0755' kit/spec.yaml` outputs ≥ 1; `grep -c 'user: "0"' kit/spec.yaml` outputs ≥ 1; `grep -c 'chown' kit/spec.yaml` outputs ≥ 1
    - `grep -c '^SL=kit/files/home/.claude/statusline.sh$' tests/run.sh` outputs 1 and `grep -v '^#' tests/run.sh | grep -c 'SL=statusline.sh'` outputs 0
    - `grep -c 'check_ok "exec bit' tests/run.sh` outputs 1; `grep -c 'INFO installed' tests/run.sh` outputs ≥ 1
    - `/bin/bash tests/run.sh` exits 0 and its last line reports a check count ≥ 126 with `0 failures`
    - `kit/files/home/.claude/statusline.sh < tests/fixtures/full.json | sed "s/$(printf '\033')\[[0-9;]*m//g" | sed -n 1p` prints exactly `Opus 5 (high) · myproject`
    - The SUMMARY records the bites evidence: a run with the exec bit removed showed `FAIL exec bit:` and the restored run showed `PASS exec bit:`
  </acceptance_criteria>
  <done>The canonical script is at its final kit path (mode 100755, content unchanged), `kit/` validates as an sbx mixin kit with the D-32/D-33/D-34 setup commands, and the harness is green (≥126 checks) against the new path with a proven exec-bit check — the host half of the install story works end-to-end.</done>
</task>

<task type="auto">
  <name>Task 2: Raw render dumper for byte-for-byte cross-environment comparison + gitignored evidence dir</name>
  <files>tests/render-fixtures.sh, .gitignore</files>
  <read_first>tests/run.sh (header/preamble shape, the run_fixture invocation discipline `/bin/bash "$SL" < fixture`, the mktemp form, PASS/FAIL/INFO vocabulary), .planning/phases/03-install-dual-environment-validation/03-PATTERNS.md (§tests/render-fixtures.sh — header, fixture-loop, installed-path guard, portability rules that bind this file), .planning/phases/03-install-dual-environment-validation/03-RESEARCH.md (§Pattern 3 determinism argument and the sketch under §Code Examples), .gitignore (current single entry), .claude/CLAUDE.md (portability table)</read_first>
  <action>
    Create `tests/render-fixtures.sh` (mode 0755, `#!/bin/bash`, bash 3.2-safe, BSD/GNU-neutral — it runs on the macOS host AND inside the sandbox):
    - Header comment: purpose (dump raw fixture renders for byte diff — D-39, PORT-01, PORT-04), usage `/bin/bash tests/render-fixtures.sh OUTDIR`, and the env inputs `INSTALLED` (path of the installed script, default `$HOME/.claude/statusline.sh`) and `REQUIRE_INSTALLED` (non-empty makes a missing/non-executable installed path a FAIL instead of an INFO skip).
    - Preamble: `cd "$(dirname "$0")/.." || exit 1`; `SL=kit/files/home/.claude/statusline.sh`; `OUT=${1:?usage: tests/render-fixtures.sh OUTDIR}`; `INSTALLED=${INSTALLED:-$HOME/.claude/statusline.sh}`.
    - Output layout: `mkdir -p "$OUT/repo"`; if `[ -x "$INSTALLED" ]` also `mkdir -p "$OUT/installed"`. Remove only stale `*.out` files inside those two subdirectories before rendering — no recursive deletion of OUT itself.
    - Fixture loop over `tests/fixtures/*.json`, name derived with `n=${f##*/}; n=${n%.json}`; write RAW bytes (escape sequences kept — the byte diff must include color codes): `/bin/bash "$SL" < "$f" > "$OUT/repo/$n.out" 2>/dev/null`, and when the installed dir exists, `"$INSTALLED" < "$f" > "$OUT/installed/$n.out" 2>/dev/null` — executed directly so the shebang and exec bit through the installed path (symlink on the host, kit copy in the sandbox) are what gets exercised (PORT-04).
    - After the loop print `INFO rendered <N> fixtures to <OUT>/repo`. Then: if the installed dir exists, run `diff -r "$OUT/repo" "$OUT/installed"` — on success print a `PASS` line naming PORT-04 (installed path renders byte-identical to repo path), on difference print a `FAIL` line and exit 1; else if `REQUIRE_INSTALLED` is non-empty print a `FAIL` line (installed path absent or not executable) and exit 1; else print an `INFO` line saying the installed half was skipped. Exit 0 otherwise. (Helpers may exit non-zero — tests/run.sh precedent; only statusline.sh itself is never-fail.)
    - Portability: obey the CLAUDE.md table — no bash-4-only builtins or associative arrays, no GNU-only date/sed/readlink flags, comparisons via diff/cmp only; temp files (if any) via the `mktemp "${TMPDIR:-/tmp}/name.XXXXXX"` form.
    - Determinism note in a comment: all 7 fixtures render time-independently (resets_at 0 renders "now"; empty/malformed fall back to the repo directory basename, identical in the sandbox because the workspace mounts at the same absolute path).

    Then append `tests/out/` to `.gitignore` (after the existing `.gsd/` line). Run `/bin/bash tests/render-fixtures.sh tests/out/host` on the host: expect 7 `*.out` files under `tests/out/host/repo`, an `installed` dir (the host has ~/.claude/statusline.sh today — research-verified regular-file copy with identical bytes) and the PASS line; confirm `git status --porcelain tests/out` is empty. Run the shellcheck advisory over the new file (non-failing). Do not write anything under ~/.claude (D-46).
  </action>
  <verify>
    <automated>/bin/bash -n tests/render-fixtures.sh && /bin/bash tests/render-fixtures.sh tests/out/host && [ "$(ls tests/out/host/repo/*.out | wc -l | tr -d ' ')" = 7 ] && /bin/bash kit/files/home/.claude/statusline.sh < tests/fixtures/full.json | cmp - tests/out/host/repo/full.out && grep -c '^tests/out/$' .gitignore | grep -qx 1 && [ -z "$(git status --porcelain tests/out)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `test -x tests/render-fixtures.sh` succeeds and `/bin/bash -n tests/render-fixtures.sh` exits 0
    - `/bin/bash tests/render-fixtures.sh` with no argument exits non-zero with a usage message (the `${1:?...}` guard)
    - `/bin/bash tests/render-fixtures.sh tests/out/host` exits 0; `ls tests/out/host/repo/*.out | wc -l` outputs 7 (empty, full, malformed, no-effort, no-rate-limits, null-context, only-five-hour)
    - `/bin/bash kit/files/home/.claude/statusline.sh < tests/fixtures/full.json | cmp - tests/out/host/repo/full.out` exits 0 (raw bytes, escape sequences included)
    - On this host the run prints a `PASS` line for the installed-vs-repo diff (PORT-04 host half) and `tests/out/host/installed/full.out` exists
    - `REQUIRE_INSTALLED=1 INSTALLED=/nonexistent /bin/bash tests/render-fixtures.sh tests/out/probe` exits 1 and prints a `FAIL` line (the sandbox-side strictness path works)
    - `grep -c 'diff -r' tests/render-fixtures.sh` outputs ≥ 1; `grep -c 'REQUIRE_INSTALLED' tests/render-fixtures.sh` outputs ≥ 1; `grep -c 'strip_ansi' tests/render-fixtures.sh` outputs 0
    - `grep -cE 'date -d|readlink -f|sed -i|mapfile|declare -A' tests/render-fixtures.sh` outputs 0
    - `grep -c '^tests/out/$' .gitignore` outputs 1 and `git status --porcelain tests/out` prints nothing
  </acceptance_criteria>
  <done>A portable render dumper exists that proves installed-path == repo-path rendering inside one environment and produces the raw per-fixture bytes plan 03 diffs host-vs-sandbox; host evidence is generated under the gitignored tests/out/host.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| kit setup.startup (root) → /home/agent/.claude/settings.json | A root shell from the kit rewrites an agent-owned, platform-managed config file every sandbox start |
| platform seed → settings.json | The sandbox engine writes/overwrites the same file at creation (and possibly on restart) |
| repo file → host ~/.claude | The user's symlink/copy points Claude Code at a repo-controlled executable |
| harness / dumper → temp and evidence dirs | Renders and temp repos written under $TMPDIR and tests/out/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | kit/spec.yaml startup jq merge on settings.json | medium | mitigate | Set ONLY `.statusLine`; `try (input) catch {}` + object check; atomic tmp + `mv`; wait for the platform seed (themeId poll) so the merge is not overwritten and never replaces the file wholesale; `chown` back to agent; verified live in plan 03 |
| T-03-02 | Denial of Service | empty/invalid/non-object settings.json breaking the merge → blank status line | low | mitigate | `[ -f ] || {}` pre-seed; `try (input) catch {}` yields `{}`; output always valid JSON |
| T-03-03 | Elevation of Privilege | root startup shell text inside spec.yaml | low | accept | Command text is repo-reviewed and static; no sandbox-runtime input is interpolated into it |
| T-03-04 | Denial of Service | exec bit lost on the delivered/installed script → Permission denied → blank line | low | mitigate | git mode 100755 preserved by `git mv`; startup `chmod 0755`; harness `exec bit` check; plan 03 `test -x` in the sandbox |
| T-03-05 | Tampering | stale `*.out` evidence mixing with fresh renders | low | mitigate | dumper clears `*.out` in its two output subdirs before rendering; tests/out/ gitignored so evidence never enters commits |
| T-03-SC | Tampering | `apt-get install jq git` in setup.install | low | accept | Guarded by `command -v` (no-op on the claude-code base image); Ubuntu official repos only; no npm/pip/cargo installs anywhere in this phase — package-legitimacy gate not applicable |
</threat_model>

<verification>
- `sbx kit validate kit` exits 0 (offline); spec grammar matches v0.39.0 (schemaVersion "2", string install command, string-array startup command)
- `/bin/bash tests/run.sh` exits 0 with ≥126 checks and 0 failures against `kit/files/home/.claude/statusline.sh`; the exec-bit check was shown to bite
- `git show 64b3c1b:statusline.sh | cmp - kit/files/home/.claude/statusline.sh` exits 0; `statusline.sh` absent from the repo root
- `/bin/bash tests/render-fixtures.sh tests/out/host` exits 0 with 7 repo renders and a PASS for installed == repo
- Nothing under ~/.claude was created, modified, or re-pointed by the executor (D-46)
</verification>

<success_criteria>
- Kit directory is the single source of the script consumed by both the host symlink and the sandbox kit (D-30, D-31) — zero duplication, no shim
- The D-34 statusLine object is produced by the kit's startup merge exactly as plan 02 documents it for the host (D-32, D-34); dependencies guarded at install (D-33)
- Harness and dumper give re-runnable, TMPDIR-independent evidence for PORT-01/PORT-04 that plan 03 consumes (D-39)
</success_criteria>

<output>
Create `.planning/phases/03-install-dual-environment-validation/03-01-SUMMARY.md` when done.
</output>
