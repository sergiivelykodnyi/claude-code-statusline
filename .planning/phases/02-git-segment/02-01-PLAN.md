---
phase: 02-git-segment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [statusline.sh, tests/run.sh]
autonomous: true
requirements: [GIT-01, GIT-02, GIT-03, GIT-04, GIT-05, GIT-06, PORT-02]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "In a dirty repo with an upstream, 2 unpulled commits, 3 unpushed commits, and 2 stashes, ANSI-stripped line 1 ends with '⎇ main* ≡ ↓2 ↑3 #2' (GIT-01, GIT-02, GIT-03, GIT-04, GIT-05; roadmap criterion 1)"
    - "Outside a git repo the entire git segment and its leading space are absent — line 1 for the stock fixtures is byte-identical to Phase 1 output minus the frame prefix (GIT-01; roadmap criterion 2)"
    - "Detached HEAD renders the 7-char short SHA in the branch position with no sync symbol at all (D-24, D-25)"
    - "Unborn branch (git init, no commits) renders the branch name with ≢, gaining * only when untracked/staged files exist; with zero files no dirty star appears (D-26; GIT-02 empty-input edge)"
    - "Both output lines render flush-left with no leading box-drawing frame characters; output is always exactly two lines — a blank second line when line 2 has no segments (D-21, D-22; roadmap criterion 5)"
    - "Worst-case fallback (empty/malformed stdin): line 1 is the bare blue directory basename from $PWD, line 2 is blank, exit 0 (D-23)"
    - "Every git invocation in statusline.sh carries GIT_OPTIONAL_LOCKS=0 so a render never takes optional index locks or contends with Claude's own git commands (GIT-02 concurrency edge, D-29)"
    - statement: "Ahead/behind counts pass through as unmodified base-10 integers taken verbatim from porcelain branch.ab with signs stripped — no k/M shortening, truncation, or arithmetic transformation at any magnitude (GIT-04 precision edge)"
      verification: backstop
    - statement: "The stash count passes through as an unmodified base-10 integer from git rev-list --walk-reflogs --count refs/stash — no transformation at any magnitude (GIT-05 precision edge)"
      verification: backstop
  artifacts:
    - path: "statusline.sh"
      provides: "seg_git renderer, MAGENTA palette constant, frameless two-line assembly"
      contains: "seg_git"
    - path: "tests/run.sh"
      provides: "frame-free fixture expectations, 35m palette-whitelist entry, two-line invariant check"
      contains: "35m"
  key_links:
    - from: "statusline.sh main()"
      to: "statusline.sh seg_git()"
      via: "git_seg captured once and appended to dir_seg with a plain space at the Phase 2 seam, before the line-1 join"
      pattern: "git_seg"
    - from: "statusline.sh seg_git()"
      to: "git porcelain v2"
      via: "single GIT_OPTIONAL_LOCKS=0 git -C status call parsed with a bash-3.2-safe read loop"
      pattern: "porcelain=v2"
  prohibitions:
    - statement: "MUST NOT perform any network operation in the render path — no git fetch, no curl; ahead/behind reflect only the last-fetched local tracking ref"
    - statement: "MUST NOT mutate the user's repository during a render — git invocations are read-only and carry GIT_OPTIONAL_LOCKS=0, so a render never creates or alters lock files, the index, or reflogs"
---

<objective>
Add the git segment to line 1 — `⎇ branch* ≡/≢ ↓N ↑N #N` with semantic per-marker colors (D-17..D-20) — correct in every repo state, and apply the layout correction that removes the two box-drawing frame prefixes (D-21..D-23). Git lookups stay uncached (D-29): one primary porcelain-v2 status call plus one stash count per render.

Purpose: Line 1 shows the full git situation at a glance in any repo state, without slowing the render (phase goal; GIT-01..GIT-06, PORT-02).
Output: `seg_git()` and `MAGENTA` in statusline.sh, frameless assembly in `main()`, and tests/run.sh updated so the existing 66-check harness stays green at every commit boundary.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-git-segment/02-CONTEXT.md
@.planning/phases/02-git-segment/02-PATTERNS.md
@statusline.sh
@tests/run.sh
@project-brief.md
</context>

## Artifacts this phase produces

New symbols created in this phase (plan 01 + plan 02 combined — the source-grounding pass excludes these from drift verification):

- `seg_git()` — new segment-renderer function in statusline.sh (this plan)
- `MAGENTA` — new palette constant `$'\033[35m'` in statusline.sh (this plan)
- `git_seg` — new local variable in `main()` holding the seg_git output at the seam (this plan)
- `tgit()`, `mk_repo()`, `git_render()`, `git_line1()` — temp-repo test helpers in tests/run.sh (plan 02)
- `TESTTMP` — temp-repo root variable in tests/run.sh (plan 02)
- New files: none — this phase only modifies statusline.sh and tests/run.sh.

## Flagged assumptions (spec-less edge probe)

No SPEC.md exists for this phase; the deterministic edge probe left these rows `unclassified → unresolved`. They are surfaced here as explicit assumptions, not silently dropped:

1. **GIT-01 (unclassified):** Assumed "the whole git segment is hidden" includes the plain-space joiner between the directory name and the segment — hiding is proven by exact line-equality assertions, which would catch a stray trailing space.
2. **GIT-03 (unclassified):** Assumed `≡` follows porcelain `# branch.upstream` line *presence* — an upstream that is configured but whose remote ref was deleted ("upstream gone") still shows `≡` with no counters (branch.ab absent). This matches the prescriptive porcelain mapping in .claude/CLAUDE.md; flagged for verifier review.

Canon referral (not minted as a prohibition): command-injection via hostile `.workspace.current_dir` values is canon security, already enforced by the harness section-7 injection probe (T-01-01 regression) and /gsd-secure-phase — not re-minted here.

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — frameless line 1 with the full git segment end-to-end</name>
  <files>statusline.sh, tests/run.sh</files>
  <read_first>statusline.sh (current seam at the "Phase 2 seam" comment in main(), palette block lines 8-14, seg_5h/seg_model_effort renderer shape), tests/run.sh (run_fixture expectations, palette-purity whitelist, trap/ERRTMP lifecycle), .planning/phases/02-git-segment/02-PATTERNS.md (exact analog excerpts and the seam integration snippet), .planning/phases/02-git-segment/02-CONTEXT.md (D-17..D-23, D-27, D-29), ./.claude/CLAUDE.md (bash 3.2 portability table, porcelain v2 field mapping, jq patterns)</read_first>
  <reversibility rating="reversible">Frame removal (D-21) and the segment addition are plain-text script edits, fully revertible via git.</reversibility>
  <behavior>
    - Dirty repo, upstream, behind 2 / ahead 3, 2 stashes: stripped line 1 ends "⎇ main* ≡ ↓2 ↑3 #2"
    - Non-repo directory: line 1 has no git segment and no trailing space
    - Empty stdin: line 1 is the bare directory basename, line 2 blank, exactly 2 output lines
  </behavior>
  <action>
    Implement the git segment and the frame removal in statusline.sh, then update tests/run.sh so the existing harness stays green.

    1) Palette (D-18): add MAGENTA as $'\033[35m' in the palette block, same style as the existing constants.

    2) seg_git() — new renderer following the seg_5h shape (guard, build "out" conditionally, printf '%s'):
    - Guard: [ -n "$DIR" ] || return 0 — the segment renders ONLY from the stdin workspace dir, never from $PWD. This keeps the D-23 worst-case line bare and keeps harness fixtures deterministic (the script's own cwd is not the workspace — research Pitfall 6).
    - Primary call: capture GIT_OPTIONAL_LOCKS=0 git -C "$DIR" status --porcelain=v2 --branch 2>/dev/null into a local; on non-zero exit return 0 (not-a-repo / missing dir → segment hidden, GIT-01). This is the single primary git status call (PORT-02); git state stays uncached per D-29.
    - Parse with a bash-3.2-safe while IFS= read -r loop fed by a here-string over the captured output (a pipe would run the loop in a subshell and lose the variables — classic 3.2 trap; no mapfile, no assoc arrays). Extract: "# branch.head <name>" → branch label; "# branch.upstream" presence → has-upstream flag; "# branch.ab +A -B" → ahead=A (strip leading +), behind=B (strip leading -); any line not starting with "# " → dirty flag (GIT-02). Default ahead/behind/stash to 0 before any -gt test (guard-empties-before-arithmetic style from seg_context).
    - Detached (D-24/D-25): when branch.head is the literal "(detached)", set the label from GIT_OPTIONAL_LOCKS=0 git -C "$DIR" rev-parse --short HEAD 2>/dev/null (7-char SHA; if that unexpectedly returns empty, keep the literal porcelain value so the label is never empty) and suppress the sync symbol entirely.
    - Stash count (GIT-05): GIT_OPTIONAL_LOCKS=0 git -C "$DIR" rev-list --walk-reflogs --count refs/stash 2>/dev/null; failure (no stash ref) means 0.
    - Unborn branch (D-26): no special case — porcelain reports "# branch.head <name>" with "(initial)" oid; the name renders as usual with the no-upstream symbol, and untracked/staged files drive the dirty star like any repo.
    - Assembly, exact byte composition (D-17..D-20; discretion resolved: each colored span covers the whole token, symbol plus number, for glanceability): out starts as MAGENTA + "⎇ " + label + RESET (glyph, space, and label inside one magenta span). If dirty append YELLOW + "*" + RESET with no space. If not detached append a space then GREEN + "≡" + RESET when has-upstream else RED + "≢" + RESET (GIT-03; red on the no-upstream glyph is the user's explicit choice — do not change it to yellow). If behind > 0 append a space then YELLOW + "↓" + behind + RESET; if ahead > 0 append a space then GREEN + "↑" + ahead + RESET (display order: behind before ahead, matching the project-brief example). If stash > 0 append a space then DIM + "#" + stash + RESET. Zero-value counters never render (D-27, GIT-04/GIT-05).
    - Counts and the stash number are printed verbatim as integers — no shorten_num, no arithmetic beyond the sign strip and the -gt 0 gates.

    3) Seam integration (main()): capture git_seg=$(seg_git) and append via dir_seg="$dir_seg${git_seg:+ $git_seg}" at the marked Phase 2 seam comment — plain space joiner, no dim-dot, no trailing space outside a repo. Replace the seam comment with one noting the segment is now attached.

    4) Frame removal (D-21/D-22/D-23): in main(), drop the leading box-drawing frame prefixes from both line assemblies. LINE1 becomes the joined body plus RESET. LINE2 becomes the joined body plus RESET when non-empty, otherwise the empty string — the existing two-printf epilogue then prints a literal blank second line, so output is always exactly two lines (D-22) and never jumps between renders. The D-23 structural fall-through is untouched: with empty/malformed stdin, model_seg is empty, seg_dir falls back to $PWD basename in blue, seg_git hides (DIR empty), line 2 is blank, exit 0. Update the stale header comment that calls the output "framed".

    5) tests/run.sh: rewrite the seven run_fixture expectations to the frame-free target strings —
       full: "Opus 5 (high) · myproject" / "10%/100k/1M · 50%/5h (now) · 15%/1w (now)";
       no-effort: "Opus 5 · myproject" / "10%/100k/1M · 50%/5h (now) · 15%/1w (now)";
       no-rate-limits: "Opus 5 (high) · myproject" / "10%/100k/1M";
       only-five-hour: "Opus 5 (high) · myproject" / "10%/100k/1M · 50%/5h (now)";
       null-context: "Opus 5 (high) · myproject" / "0%/0/200k";
       empty: "$HERE" / "";
       malformed: "$HERE" / "".
       (Fixture current_dir is /tmp/myproject — not a git repo — so no git segment appears in fixture output.)
       Add "35m" to the palette-purity whitelist case string. Add one new check_eq asserting the empty fixture still emits exactly 2 output lines (wc -l on the script output, whitespace-trimmed) — this pins D-22's blank-second-line behavior, which line-equality alone cannot distinguish from a missing line.

    Never-fail contract holds throughout: no set -e/-u, all git stderr to /dev/null, unconditional exit 0, printf '%s' only (branch labels are printf ARGUMENTS, never format strings — a hostile branch name containing % must render verbatim).
  </action>
  <verify>
    <automated>R=$(mktemp -d "${TMPDIR:-/tmp}/slgit.XXXXXX") && G() { GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_SYSTEM=/dev/null git "$@"; } && G init -q --bare "$R/remote.git" && G init -q "$R/w" && G -C "$R/w" symbolic-ref HEAD refs/heads/main && G -C "$R/w" config user.email t@t && G -C "$R/w" config user.name t && echo a > "$R/w/f" && G -C "$R/w" add f && G -C "$R/w" commit -qm c1 && G -C "$R/w" remote add origin "$R/remote.git" && G -C "$R/w" push -q -u origin main 2>/dev/null && G clone -q "$R/remote.git" "$R/w2" && G -C "$R/w2" config user.email t@t && G -C "$R/w2" config user.name t && G -C "$R/w2" commit -q --allow-empty -m r1 && G -C "$R/w2" commit -q --allow-empty -m r2 && G -C "$R/w2" push -q 2>/dev/null && G -C "$R/w" fetch -q 2>/dev/null && G -C "$R/w" commit -q --allow-empty -m l1 && G -C "$R/w" commit -q --allow-empty -m l2 && G -C "$R/w" commit -q --allow-empty -m l3 && echo x >> "$R/w/f" && G -C "$R/w" stash -q && echo y >> "$R/w/f" && G -C "$R/w" stash -q && touch "$R/w/untracked" && mkdir "$R/plain" && ST() { sed "s/$(printf '\033')\[[0-9;]*m//g"; } && L1=$(jq --arg d "$R/w" '.workspace.current_dir = $d' tests/fixtures/full.json | /bin/bash statusline.sh | ST | sed -n 1p) && N1=$(jq --arg d "$R/plain" '.workspace.current_dir = $d' tests/fixtures/full.json | /bin/bash statusline.sh | ST | sed -n 1p) && [ "$L1" = "Opus 5 (high) · w ⎇ main* ≡ ↓2 ↑3 #2" ] && [ "$N1" = "Opus 5 (high) · plain" ] && [ "$(/bin/bash statusline.sh < tests/fixtures/empty.json | wc -l | tr -d '[:space:]')" = "2" ] && /bin/bash tests/run.sh > /dev/null && rm -rf "$R" && echo TRACER-OK</automated>
  </verify>
  <acceptance_criteria>
    - The automated verify prints TRACER-OK: full-state temp repo renders line 1 exactly "Opus 5 (high) · w ⎇ main* ≡ ↓2 ↑3 #2" (ANSI-stripped), non-repo dir renders exactly "Opus 5 (high) · plain", empty stdin emits exactly 2 lines, and /bin/bash tests/run.sh exits 0
    - statusline.sh contains a seg_git() function and a MAGENTA constant: `grep -c 'seg_git()' statusline.sh` outputs 1 and `grep -c "MAGENTA=" statusline.sh` outputs 1
    - Single-pass constraints (PORT-02): `grep -v '^[[:space:]]*#' statusline.sh | grep -c 'porcelain=v2'` outputs 1 and `grep -v '^[[:space:]]*#' statusline.sh | grep -c 'jq -r'` outputs 1 (comments must reference the porcelain call without repeating the flag string)
    - Every git invocation line in statusline.sh contains GIT_OPTIONAL_LOCKS=0: `grep -v '^[[:space:]]*#' statusline.sh | grep 'git -C' | grep -vc 'GIT_OPTIONAL_LOCKS=0'` outputs 0
    - tests/run.sh palette whitelist line contains 35m: `grep -c '35m' tests/run.sh` outputs at least 1
    - /bin/bash -n statusline.sh exits 0
  </acceptance_criteria>
  <done>Line 1 renders the full git segment form in a real repo, hides it entirely outside one, both lines are frame-free with the two-line invariant intact (D-21/D-22/D-23), and the whole existing harness passes with updated expectations.</done>
</task>

<task type="auto">
  <name>Task 2: Expansion — detached HEAD, unborn, and no-upstream edge states</name>
  <files>statusline.sh</files>
  <read_first>statusline.sh (seg_git as landed by Task 1), .planning/research/PITFALLS.md (Pitfall 7: git edge states, lines 135-154), .planning/phases/02-git-segment/02-CONTEXT.md (D-24, D-25, D-26, specifics section detached example)</read_first>
  <behavior>
    - Detached HEAD at a commit: stripped line 1 tail is "⎇ <7-char-sha>" — no sync symbol, no star, counters only when nonzero
    - Unborn repo with no files: "⎇ main ≢" — no dirty star (GIT-02 empty edge)
    - Unborn repo with an untracked file: "⎇ main* ≢"
    - Committed repo with no upstream: "⎇ main ≢"
  </behavior>
  <action>
    Verify and harden the Task 1 parse against the four exotic repo states from research Pitfall 7 (GIT-06). Task 1's structure should already cover these via the porcelain mapping — this task proves each state against a real temp repo and fixes any divergence found (e.g. porcelain field-order assumptions, the literal "(detached)" match per D-24, sync-symbol suppression per D-25, the unborn "(initial)" oid path per D-26). If a state renders wrong, fix seg_git — do not special-case the tests. Confirm the label fallback: if rev-parse unexpectedly fails in detached state, the porcelain literal is rendered so the glyph is never followed by an empty label (research Pitfall 7 failure mode). No changes outside seg_git are permitted; the never-fail contract and the single-primary-call constraint (PORT-02) must survive any fix.
  </action>
  <verify>
    <automated>R=$(mktemp -d "${TMPDIR:-/tmp}/sledge.XXXXXX") && G() { GIT_CONFIG_GLOBAL=/dev/null GIT_CONFIG_SYSTEM=/dev/null git "$@"; } && S() { jq --arg d "$1" '.workspace.current_dir = $d' tests/fixtures/full.json | /bin/bash statusline.sh | sed "s/$(printf '\033')\[[0-9;]*m//g" | sed -n 1p; } && G init -q "$R/d" && G -C "$R/d" symbolic-ref HEAD refs/heads/main && G -C "$R/d" config user.email t@t && G -C "$R/d" config user.name t && G -C "$R/d" commit -q --allow-empty -m c1 && G -C "$R/d" commit -q --allow-empty -m c2 && G -C "$R/d" checkout -q --detach HEAD~1 && SHA=$(G -C "$R/d" rev-parse --short HEAD) && [ "$(S "$R/d")" = "Opus 5 (high) · d ⎇ $SHA" ] && G init -q "$R/u" && G -C "$R/u" symbolic-ref HEAD refs/heads/main && [ "$(S "$R/u")" = "Opus 5 (high) · u ⎇ main ≢" ] && touch "$R/u/x" && [ "$(S "$R/u")" = "Opus 5 (high) · u ⎇ main* ≢" ] && G init -q "$R/c" && G -C "$R/c" symbolic-ref HEAD refs/heads/main && G -C "$R/c" config user.email t@t && G -C "$R/c" config user.name t && G -C "$R/c" commit -q --allow-empty -m c1 && [ "$(S "$R/c")" = "Opus 5 (high) · c ⎇ main ≢" ] && /bin/bash tests/run.sh > /dev/null && rm -rf "$R" && echo EDGES-OK</automated>
  </verify>
  <acceptance_criteria>
    - The automated verify prints EDGES-OK: detached renders exactly "Opus 5 (high) · d ⎇ $SHA" (short SHA, nothing after it), unborn-clean renders exactly "Opus 5 (high) · u ⎇ main ≢", unborn-with-untracked renders exactly "Opus 5 (high) · u ⎇ main* ≢", committed-no-upstream renders exactly "Opus 5 (high) · c ⎇ main ≢"
    - Exact-equality assertions prove the absence of the sync symbols in detached state and the absence of the dirty star in clean states — no zero-value counter tokens appear in any of the four lines (D-25, D-26, D-27)
    - /bin/bash tests/run.sh still exits 0 (no regression from any hardening fix)
  </acceptance_criteria>
  <done>All four exotic states from Pitfall 7 render per D-24/D-25/D-26 with exact-equality proof, and the full harness remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stdin JSON → shell vars | `.workspace.current_dir` is attacker-influencable (hostile directory names) and crosses into git -C arguments |
| repo content → rendered line | Branch names and porcelain output come from potentially cloned hostile repos and cross into terminal output |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | seg_git `git -C "$DIR"` argument | high | mitigate | DIR flows only through the existing jq @sh-quoted eval and is always double-quoted; no eval of git output anywhere; harness section-7 injection probe (T-01-01 regression) must stay green |
| T-02-02 | Tampering | branch label / porcelain text in rendered output | medium | mitigate | Branch labels are printf arguments only (printf '%s'), never format strings; parse via read loop, never eval; hostile % or escape bytes render verbatim as text |
| T-02-03 | Denial of Service | git status on large/slow repos blanking the line | low | accept | Single local porcelain call + GIT_OPTIONAL_LOCKS=0, zero network (prohibition); latency budget asserted in plan 02-02; session-keyed cache remains a documented lever (D-29) |
| T-02-04 | Tampering | render mutating the user's repo (locks/index writes) | medium | mitigate | All git commands read-only with GIT_OPTIONAL_LOCKS=0; acceptance gate greps every git -C line for the env guard |

No package-manager installs occur in this phase — the package-legitimacy gate does not apply.
</threat_model>

<verification>
- /bin/bash tests/run.sh exits 0 after both tasks (existing 66+ checks with frame-free expectations, plus the two-line invariant check)
- Tracer verify (Task 1) and edge verify (Task 2) both pass against real temp repos built with isolated git config
- Roadmap criteria 1, 2, 3 (implementation half), and 5 are directly demonstrated; criterion 4 is structurally in place (single jq pass, one primary git status call) and gets its timed proof in plan 02-02
</verification>

<success_criteria>
- Full-state repo: stripped line 1 ends "⎇ main* ≡ ↓2 ↑3 #2"; non-repo: no git segment, no stray space
- Detached / unborn / no-upstream states render per D-24/D-25/D-26 with zero-value counters never present (D-27)
- Both lines flush-left, always exactly two lines, worst-case fallback bare (D-21/D-22/D-23)
- Harness green; every git call guarded by GIT_OPTIONAL_LOCKS=0; no network calls in the render path
</success_criteria>

<output>
Create `.planning/phases/02-git-segment/02-01-SUMMARY.md` when done.
</output>
