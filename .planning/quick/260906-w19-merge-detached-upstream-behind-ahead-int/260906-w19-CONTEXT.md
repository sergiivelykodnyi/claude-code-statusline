# Quick Task 260906-w19: Merge detached/upstream/behind/ahead into one git sync subsegment with new colors; stash color to cyan - Context

**Gathered:** 2026-09-06
**Status:** Ready for planning

<domain>
## Task Boundary

Update the Git segment in `kit/files/home/.claude/statusline.sh` (`seg_git`). Merge the detached, upstream (in-sync), behind, and ahead indicators into one mutually exclusive sync subsegment:

- Detached → detached indicator shown (the short commit hash label itself is the indicator), kept in **magenta** like the branch label; no sync glyph.
- Behind and/or ahead → the corresponding arrow indicators shown.
- In sync with origin (upstream present, ahead=0, behind=0) → upstream indicator `≡` shown, **green**.

New colors:
- Detached: unchanged (magenta SHA label, no glyph)
- In sync: green
- Ahead only: blue
- Behind only: yellow
- Ahead AND behind: red (both arrows + counts red)
- Stash count: cyan (was dim)

</domain>

<decisions>
## Implementation Decisions

### Detached indicator
- Keep current behavior EXACTLY as-is: short commit hash as the label, in magenta (same as branch name), no sync glyph. The hash IS the detached indicator. (User correction 2026-09-06: "I didn't say we need to change detached to red. It can be as it is. Magenta color, the same as branch name.")

### No-upstream case
- Keep today's behavior: red `≢` when the branch has no upstream (not detached).

### Ahead + behind display
- Show both arrows with counts, e.g. `↓1 ↑2`, all in red. Keep the existing arrow order (behind first, then ahead).

### Claude's Discretion
- Exact ordering/spacing within the subsegment; keeping the existing `↓N ↑N` order.
- Dirty star (`*` yellow) and branch label color for non-detached branches stay unchanged (magenta).
- Update tests/fixtures, README legend, and spec docs if they encode the old colors/behavior.

</decisions>

<specifics>
## Specific Ideas

Current code (statusline.sh `seg_git`, ~lines 144-183):
- `≡` green when upstream, `≢` red when not, hidden when detached (D-25)
- `↓N` yellow, `↑N` green, `#N` dim stash
- Detached label = short SHA in magenta (D-24)

New logic for the sync subsegment (mutually exclusive states):
1. detached → magenta SHA label (unchanged), no sync glyph
2. no upstream → red `≢`
3. ahead>0 && behind>0 → red `↓B ↑A`
4. behind>0 only → yellow `↓B`
5. ahead>0 only → blue `↑A`
6. in sync → green `≡`

Stash: `#N` cyan.

</specifics>

<canonical_refs>
## Canonical References

Prior related quick tasks: 260826-wid (branch symbol → dot separator), 260827-02m (dropped branch glyph from spec docs). README symbol legend may need color updates.

</canonical_refs>
