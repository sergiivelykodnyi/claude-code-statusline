---
phase: quick-260906-w19
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - kit/files/home/.claude/statusline.sh
  - tests/run.sh
  - README.md
  - project-brief.md
  - .planning/PROJECT.md
  - .claude/CLAUDE.md
autonomous: true
requirements: [GIT-03, GIT-04, GIT-05]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The git sync subsegment is mutually exclusive: exactly ONE of these renders per state — magenta short-SHA label (detached, no glyph — today's behavior, unchanged), red `≢` (no upstream), red `↓B ↑A` pair (ahead AND behind), yellow `↓B` (behind only), blue `↑A` (ahead only), green `≡` (in sync)"
    - "Stripped full-form line 1 reads `Opus 5 (high) · w · main* ↓2 ↑3 #2` — the `≡` glyph never co-renders with arrows"
    - "Stash count `#N` renders cyan (SGR 36m), no longer dim"
    - "Detached label stays magenta (SGR 35m) exactly as today; branch label stays magenta; dirty `*` stays yellow"
    - "`/bin/bash tests/run.sh` reports 0 failures with check count >= 224 (baseline 220 + 4 new checks: behind-only-yellow and ahead-only-blue scenarios, each with one byte assertion and one stripped-line assertion)"
    - "README.md, project-brief.md, .planning/PROJECT.md, .claude/CLAUDE.md describe the shipped mutually exclusive behavior and new colors; milestone archives under .planning/milestones/ are byte-untouched"
  artifacts:
    - kit/files/home/.claude/statusline.sh
    - tests/run.sh
    - README.md
    - project-brief.md
    - .planning/PROJECT.md
    - .claude/CLAUDE.md
  key_links:
    - "tests/run.sh section 8 byte assertions must match seg_git's exact span composition (per-token coloring: each arrow is its own `${ESC}[31m...${ESC}[0m` span with a plain space between) — a single combined red span breaks the established one-colored-unit convention (PROJECT.md:115)"
    - "run.sh:427-434 palette purity whitelist already includes 34m (blue) and 36m (cyan) — no whitelist change needed"
    - "tests/fixtures/*.json and tests/render-fixtures.sh point current_dir at /tmp/myproject (not a repo) — no git segment renders there, so fixtures need no changes"
---

<objective>
Merge the detached / upstream / behind / ahead indicators in `seg_git` into ONE mutually exclusive sync subsegment with the user's new color scheme, and change the stash count color to cyan. Update the test suite assertions and the four live spec docs to match.

Purpose: today `≡` renders alongside `↓`/`↑`, which is redundant — the arrows already imply an upstream. The new scheme makes the sync state a single glanceable token with color encoding severity (green in-sync, blue ahead, yellow behind, red diverged/no-upstream). The detached state already behaves this way (magenta short-SHA label, no glyph) and stays byte-identical.
Output: updated `kit/files/home/.claude/statusline.sh`, `tests/run.sh`, and 4 docs; suite green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/quick/260906-w19-merge-detached-upstream-behind-ahead-int/260906-w19-CONTEXT.md
@.planning/quick/260906-w19-merge-detached-upstream-behind-ahead-int/260906-w19-RESEARCH.md
@kit/files/home/.claude/statusline.sh
@tests/run.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework seg_git sync subsegment + update test suite</name>
  <files>kit/files/home/.claude/statusline.sh, tests/run.sh</files>
  <action>
**A. `kit/files/home/.claude/statusline.sh` — `seg_git` (lines 139-183, verified live at planning time):**

1. Label rendering stays untouched (per CONTEXT locked decision, user correction 2026-09-06): line 173 `out="${MAGENTA}${label}${RESET}"` is kept as-is — the detached short-SHA label stays magenta like today (D-24 kept). Keep the dirty-star append (line 174) unchanged.
2. Replace the independent glyph/arrow appends (lines 175-180) with one bash-3.2-safe if/elif chain implementing the 6 mutually exclusive states (per CONTEXT decisions):
   - `detached=1` → no sync glyph at all (the magenta SHA label IS the indicator — today's D-25 behavior, kept)
   - not detached, `upstream=0` → red `≢` (keeps D-19)
   - `behind>0 && ahead>0` → red `↓${behind}` and red `↑${ahead}` as two separate colored spans with a plain space between (per-token convention, PROJECT.md:115); keep behind-first order
   - `behind>0` only → yellow `↓${behind}`
   - `ahead>0` only → blue `↑${ahead}`
   - else (upstream, ahead=0, behind=0) → green `≡`
   RESEARCH.md "Suggested new sync block" gives a drop-in shape; all color vars (`RED GREEN YELLOW BLUE MAGENTA CYAN DIM`) already exist at statusline.sh:26-33 — define nothing new.
3. Change the stash append (line 181) from the dim span to `${CYAN}#${stash}${RESET}`.
4. Update the comment block above `seg_git` (lines 139-143) so it describes the new scheme: one mutually exclusive sync token (green in-sync / blue ahead-only / yellow behind-only / red diverged-pair / red no-upstream; detached = magenta SHA label with no glyph, unchanged), cyan stash. Keep the requirement/decision references — D-19, D-24, and D-25 all still hold; only the old "`≡` renders whenever upstream exists" semantics is superseded (now `≡` means in-sync: upstream AND ahead=behind=0).
5. Do NOT touch: dirty detection, porcelain parsing, unborn-branch handling (`(initial)` head with `upstream=0` falls into the `≢` state naturally — ahead/behind guards at 164-165 hold), stash counting, `GIT_OPTIONAL_LOCKS=0`.

**B. `tests/run.sh` section 8 (lines 330-434, verified live):**

1. Line 366 expected → `Opus 5 (high) · w · main ↓1 ↑1 #1` (glyph gone — mutually exclusive).
2. Line 381 expected → `Opus 5 (high) · w · main* ↓2 ↑3 #2`.
3. Line 415 `want` → `${ESC}[34mw${ESC}[0m ${ESC}[2m·${ESC}[0m ${ESC}[35mmain${ESC}[0m${ESC}[33m*${ESC}[0m ${ESC}[31m↓2${ESC}[0m ${ESC}[31m↑3${ESC}[0m ${ESC}[36m#2${ESC}[0m` (full form is ahead+behind → both arrows red; stash cyan). Rename the check at line 417 to describe the new colors (pitfall 2 in RESEARCH: stale color names in check names mislead readers).
4. Line 353 (`main ≡` clean in-sync), 396 (`≢` text), 405 (detached text), 410 (unborn text), 420-422 (red `≢` bytes), 427-434 (palette whitelist) stay as-is — behavior/text unchanged.
5. Add two new byte-check scenarios closing the color-coverage gaps (4 new checks total — each scenario asserts both raw bytes and the stripped line):
   - **Ahead-only blue:** right after the line-353 in-sync check, `tgit -C "$W" commit -q --allow-empty -m a1`, capture via `git_render "$W"`, assert raw output contains `${ESC}[34m↑1${ESC}[0m` AND stripped line 1 equals `Opus 5 (high) · w · main ↑1`, then `tgit -C "$W" reset -q --hard origin/main` to restore in-sync so the existing boundary sequence at 357+ is undisturbed.
   - **Behind-only yellow:** between the fetch at line 362 and the `l1` commit at line 363, capture via `git_render "$W"`, assert raw output contains `${ESC}[33m↓1${ESC}[0m` AND stripped line 1 equals `Opus 5 (high) · w · main ↓1`.
   Do NOT add a detached-label byte check — the detached case is byte-identical to today (magenta SHA, no glyph) and the existing scenario 8.4 text check already covers it.
   Keep all additions bash-3.2-safe and in the existing `check_eq`/`check_ok` style.
</action>
  <verify>
    <automated>/bin/bash tests/run.sh</automated>
  </verify>
  <done>Suite exits 0 with 0 failures and check count >= 224 (baseline 220 + the 4 genuinely new checks from action B.5: 2 scenarios x byte assertion + stripped-line assertion). Full-form stripped line shows `main* ↓2 ↑3 #2` with no `≡`; clean in-sync still shows green `≡`; detached still shows the magenta SHA with no glyph; new byte checks prove yellow behind-only and blue ahead-only; updated line-415 bytes prove red diverged pair and cyan stash.</done>
</task>

<task type="auto">
  <name>Task 2: Update the four live spec docs to the shipped behavior</name>
  <files>README.md, project-brief.md, .planning/PROJECT.md, .claude/CLAUDE.md</files>
  <action>
Follow the 260827-02m precedent: edit ONLY the live spec docs; milestone archives under `.planning/milestones/` and prior quick-task dirs stay byte-untouched (note D-19/D-24/D-25 supersession in the SUMMARY instead).

Exact stale spots (all verified live at planning time):

1. `README.md:8` — example line: `main* ≡ ↓2 ↑3 #2` → `main* ↓2 ↑3 #2`.
2. `README.md:23` — legend row `≡ / ≢ — Branch has / has no upstream` → rework: `≡` = in sync with upstream (green), `≢` = no upstream (red); note the sync token is mutually exclusive with the arrows.
3. `README.md:24-26` — `↓2` / `↑3` / `#2` rows: add the colors — behind-only yellow, ahead-only blue, both red when diverged; stash cyan. Detached behavior is unchanged (magenta short SHA as the label, no sync glyph) — only touch any existing detached wording if it contradicts that; do not add color changes for it.
4. `project-brief.md:17-20` — `branch_status`/`ahead`/`behind`/`stash` semantics: same rework (mutually exclusive sync token + colors); `project-brief.md:30` — example line: drop `≡` as in README.
5. `.planning/PROJECT.md:21` — example line: drop `≡`. `:35` — `branch_status` prose: `≡` now means in-sync (upstream present AND ahead=behind=0), mutually exclusive with arrows. `:115` — replace the color list with the new scheme: magenta branch (magenta short SHA when detached — unchanged), yellow `*`, green `≡` in-sync, red `≢`, yellow `↓N` behind-only, blue `↑N` ahead-only, both red when diverged, cyan `#N`.
6. `.claude/CLAUDE.md:104` — the porcelain mapping note: upstream present now yields `≡` only when ahead=behind=0; arrows replace it otherwise.

Keep wording in the docs' existing style; do not restructure sections.
</action>
  <verify>
    <automated>/bin/bash tests/run.sh && for f in README.md project-brief.md .planning/PROJECT.md; do grep -q '↓2 ↑3 #2' "$f" || { echo "missing new example in $f"; exit 1; }; done && ! grep -n '≡ ↓' README.md project-brief.md .planning/PROJECT.md</automated>
  </verify>
  <done>All four docs describe the mutually exclusive sync subsegment and new colors; example lines match the shipped render; no doc pairs `≡` with arrows in one example; suite still 0 failures; `git status --porcelain` shows no changes under `.planning/milestones/`.</done>
</task>

</tasks>

<verification>
- `/bin/bash tests/run.sh` → 0 failures, >= 224 checks.
- Manual spot-check (optional): in this repo itself, `echo '{"model":{"display_name":"Opus"},"workspace":{"current_dir":"'$PWD'"},"session_id":"t"}' | kit/files/home/.claude/statusline.sh` renders one sync token only.
- `git diff --stat` touches exactly the 6 files in `files_modified`.
</verification>

<success_criteria>
- Sync subsegment is mutually exclusive with the 6 states and colors locked in CONTEXT (detached magenta SHA unchanged / red `≢` / red pair / yellow behind / blue ahead / green `≡`); stash cyan.
- Bash 3.2-safe, BSD/GNU-portable (no new constructs beyond if/elif and existing color vars).
- Tests and docs updated; archives untouched.
</success_criteria>

<output>
Create `.planning/quick/260906-w19-merge-detached-upstream-behind-ahead-int/260906-w19-SUMMARY.md` when done. Record there that D-19/D-24/D-25 are kept intact; only the old "`≡` whenever upstream exists" semantics is superseded by the mutually exclusive sync token.
</output>
