---
phase: quick-260827-02m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - project-brief.md
  - .planning/PROJECT.md
  - .claude/CLAUDE.md
  - README.md
autonomous: true
requirements: [GIT-01, D-01]

estimate:
  tokens: 12000
  raw_tokens: 12000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The three live spec docs (`project-brief.md`, `.planning/PROJECT.md`, `.claude/CLAUDE.md`) no longer contain the branch glyph anywhere — 9 stale lines rewritten, glyph occurrence count goes 9 -> 0"
    - "`project-brief.md` and `.planning/PROJECT.md` describe the SHIPPED rendering: layout line and whole-line example both read `… · myproject · main* ≡ ↓2 ↑3 #2`"
    - "`.claude/CLAUDE.md:109` still states the same not-a-repo rule (hide the git segment on non-zero exit) without naming a glyph — the substance is unchanged, only the stale shorthand is replaced"
    - "README's Symbol legend gains exactly one new row explaining the `·` separator, so `myproject · main` no longer reads as two unrelated tokens; the other 12 rows are unchanged"
    - "The 33 historical records that carry the glyph (milestones, research, RETROSPECTIVE, STATE, the 260826-wid quick dir) are byte-untouched — `git status --porcelain` on those paths is empty and their glyph count stays 33"
    - "`/bin/bash tests/run.sh` still reports `220 checks, 0 failures` (baseline unchanged)"
    - "`kit/` and `tests/` are untouched — no rendering change, no test-expectation change, no `kit/spec.yaml` version bump"
  artifacts:
    - project-brief.md
    - .planning/PROJECT.md
    - .claude/CLAUDE.md
    - README.md
  key_links:
    - "Every replacement string below was asserted byte-present and UNIQUE (grep -cF == 1) in a pre-plan dry run — Edit old_string values can be used verbatim without re-deriving them"
    - "The negative glyph gate is FILE-SCOPED to the three doc files; PLAN.md/SUMMARY.md are never grep targets, so this plan naming the glyph cannot self-invalidate its own gate"
    - "No test in tests/ reads any of the four target docs (verified: the only `README`/`CLAUDE.md` hits in tests/ are shell comments and `emit` message strings) — the README edit is test-neutral"
    - "The out-of-scope `git status --porcelain` gate runs at TASK time, before the quick-task workflow appends its own STATE.md bookkeeping row; that later workflow write is not part of this task's diff"
---

<!-- planner-discipline-allow: ⎇ -->
<!-- The acceptance criteria negative-grep the glyph inside project-brief.md, .planning/PROJECT.md
     and .claude/CLAUDE.md ONLY. This PLAN.md is never a grep target, so quoting the glyph here
     (required for exact-match Edit old_strings) cannot make a gate self-invalidating. -->

<objective>
Close the documentation drift left open by quick task 260826-wid. That task changed line 1 of
`kit/files/home/.claude/statusline.sh` from `… · myproject ⎇ main* ≡ ↓2 ↑3 #2` to
`… · myproject · main* ≡ ↓2 ↑3 #2`, updating README.md and tests/run.sh but deliberately leaving
the spec docs disagreeing with the shipped code (recorded in that task's SUMMARY under "Known
Documentation Disagreement").

Two distinct jobs:

1. **Remove the stale glyph from the three LIVE spec docs** (9 lines). `project-brief.md:16` is the
   sharpest case — it currently *requires* the glyph as a prefix, directly contradicting shipped
   behaviour. In `.claude/CLAUDE.md:109` and `.planning/PROJECT.md:55` the substance is correct and
   only the glyph-as-shorthand-for-"the git segment" is stale, so those are reworded, not deleted.

2. **Add a `·` legend row to README.** Before the change the glyph disambiguated `myproject ⎇ main`
   for free. Now `myproject · main` reads as two unrelated tokens with nothing in the legend
   explaining the joiner. One new row fixes that.

Purpose: the authoritative brief and PROJECT.md stop specifying a behaviour the code no longer has,
and a reader of README can tell where one segment ends and the next begins.

Output: four edited files, three commits. No code, no tests, no `kit/spec.yaml` version bump.

Caveat worth knowing (non-blocking, do not act on it): the `## Technology Stack` block in
`.claude/CLAUDE.md` is GSD-generated from `.planning/research/`. Since `.planning/research/` is
out of scope here, a future regeneration of that block could reintroduce the old wording on line 109.
That is accepted — the research docs are historical records.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/quick/260826-wid-change-the-branch-symbol-to-a-dot-separa/260826-wid-SUMMARY.md
</context>

<scope_boundary>
**IN SCOPE — exactly 4 files.** Nothing else. Do not widen, do not re-derive.

**OUT OF SCOPE — these are historical records, not specs. Do NOT edit them:**

| Path | Why it must keep the glyph |
|------|----------------------------|
| `.planning/milestones/**` (incl. `v1.0-REQUIREMENTS.md`, all `v1.0-phases/**`) | Archived PLAN.md files embed runnable verify commands whose expected strings must keep matching what was actually executed; rewriting a VERIFICATION.md would claim a check that never ran; rewriting `02-DISCUSSION-LOG.md` would misrepresent a decision the user actually made |
| `.planning/research/**` | Point-in-time research output |
| `.planning/RETROSPECTIVE.md` | Point-in-time record |
| `.planning/STATE.md` | Line 32 and the Quick-Tasks row quote the 260826-wid task description verbatim — correct as-is |
| `.planning/quick/260826-wid-*/**` | The preceding task's own artifacts |
| `kit/spec.yaml` | Version bump deliberately deferred as a separate release decision |
| `kit/files/home/.claude/statusline.sh`, `tests/run.sh` | Already correct — changed by 260826-wid |

33 files under those paths carry the glyph today and must still carry it (count `== 33`) when this
plan is done. That gate exists to catch an over-zealous repo-wide scrub, which is the realistic
failure mode here.
</scope_boundary>

<gate_polarity_note>
Every gate below was dry-run against the real repo BEFORE this plan was written:

| Gate | Pre-edit (must FAIL) | Post-edit, simulated in `$TMPDIR` (must PASS) |
|------|----------------------|-----------------------------------------------|
| glyph count over the 3 spec docs | `9` | `0` |
| `current_dir_name · current_git_branch` in brief / PROJECT | `0` / `0` | `1` / `1` |
| `Opus 5 (high) · myproject · main* ≡ ↓2 ↑3 #2` in brief / PROJECT | `0` / `0` | `1` / `1` |
| README separator legend row present | `0` | `1` (legend rows 13 -> 14) |
| out-of-scope `git status --porcelain` | empty (PASS, must stay) | empty |
| historical glyph-bearing file count | `33` | `33` |

The out-of-scope gate was also proven NON-VACUOUS: a throwaway `.gsd-gate-polarity-probe` file under
`.planning/milestones/` made it emit `?? .planning/milestones/.gsd-gate-polarity-probe`, and the
probe was deleted (repo verified clean).
</gate_polarity_note>

<tasks>

<task type="tracer">
  <name>Task 1: De-glyph project-brief.md — the authoritative brief, end to end with the full gate battery</name>
  <precondition>`git status --porcelain` is empty at the start of this task (a dirty tree makes the out-of-scope gate unreadable). If it is not empty, halt and report.</precondition>
  <files>project-brief.md</files>
  <action>
Make four exact-match Edit replacements in `project-brief.md`. Use the Edit tool, NOT `sed` — three
of these lines carry multi-byte characters and RESEARCH.md §5.6 warns that BSD `sed` under a
non-UTF-8 locale can mangle them (this is the same reasoning task 260826-wid followed). Each
old_string below was verified to occur exactly once, so no `replace_all` is needed.

Line 8 (the Row 1 layout line) — replace
`current_dir_name ⎇ current_git_branch`
with
`current_dir_name · current_git_branch`

Line 16 (the `current_git_branch` bullet — this is the line that currently *requires* behaviour the
shipped code no longer has) — replace
``it should show the current branch name with the symbol `⎇` as a prefix, e.g. `⎇ main` ``
with
``it should show the current branch name as its own segment after the directory, e.g. `myproject · main` ``
(no leading/trailing space inside the backticks — the literal replacement text ends at the closing
backtick of `` `myproject · main` ``).

Line 17 (the `branch_status` bullet, trailing example only) — replace
``e.g. `⎇ main* ≡` ``
with
``e.g. `main* ≡` ``

Line 30 (the whole-line example inside the ```text fence) — replace
`myproject ⎇ main*`
with
`myproject · main*`

Do not touch any other line, do not reflow the file, and do not add a changelog note. The edits are
in-place line rewrites: the file's line count and every other line number stay identical.

This is the tracer slice — it exercises every gate shape the remaining tasks reuse (positive
dot-form assertion, negative glyph assertion, out-of-scope untouched, historical-record
preservation) on one file, so a gate design flaw surfaces after one commit rather than three.

Commit as `docs(260827-02m): drop the branch glyph from project-brief.md`.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)" && fail=0
[ "$(grep -cF '⎇' project-brief.md)" = "0" ] || { echo "FAIL: glyph still in project-brief.md"; fail=1; }
[ "$(grep -cF 'current_dir_name · current_git_branch' project-brief.md)" = "1" ] || { echo "FAIL: layout line 8"; fail=1; }
[ "$(grep -cF 'Opus 5 (high) · myproject · main* ≡ ↓2 ↑3 #2' project-brief.md)" = "1" ] || { echo "FAIL: example line 30"; fail=1; }
[ "$(grep -cF 'e.g. `myproject · main`' project-brief.md)" = "1" ] || { echo "FAIL: branch bullet 16"; fail=1; }
[ "$(grep -cF 'e.g. `main* ≡`' project-brief.md)" = "1" ] || { echo "FAIL: branch_status bullet 17"; fail=1; }
[ "$(wc -l < project-brief.md | tr -d ' ')" = "36" ] || { echo "FAIL: line count drifted (expected 36)"; fail=1; }
OOS=$(git status --porcelain -- .planning/milestones .planning/research .planning/RETROSPECTIVE.md .planning/STATE.md '.planning/quick/260826-wid-change-the-branch-symbol-to-a-dot-separa' kit tests)
[ -z "$OOS" ] || { echo "FAIL: out-of-scope touched -> $OOS"; fail=1; }
[ "$(grep -rlF '⎇' .planning/milestones .planning/research .planning/RETROSPECTIVE.md .planning/STATE.md '.planning/quick/260826-wid-change-the-branch-symbol-to-a-dot-separa' | wc -l | tr -d ' ')" = "33" ] || { echo "FAIL: historical records over-scrubbed"; fail=1; }
[ "$fail" = "0" ] && echo OK
    </automated>
  </verify>
  <done>`project-brief.md` contains zero glyph occurrences, its layout line and whole-line example both show the dot-separated form, both example bullets are updated, the file is still 36 lines, and every out-of-scope path is untouched with the historical glyph count still at 33.</done>
</task>

<task type="auto">
  <name>Task 2: De-glyph .planning/PROJECT.md and .claude/CLAUDE.md</name>
  <files>.planning/PROJECT.md, .claude/CLAUDE.md</files>
  <action>
Five exact-match Edit replacements across two files. Edit tool, not `sed` (same multi-byte reason as
Task 1). Every old_string occurs exactly once.

`.planning/PROJECT.md` line 14 (Target Layout line) — replace
`current_dir_name ⎇ current_git_branch`
with
`current_dir_name · current_git_branch`

`.planning/PROJECT.md` line 21 (whole example) — replace
`myproject ⎇ main*`
with
`myproject · main*`

`.planning/PROJECT.md` line 34 (segment-definition bullet) — replace the whole line
``- `⎇ branch` — current git branch, only when inside a git repo``
with
``- `branch` — current git branch, rendered as its own segment, only when inside a git repo``

`.planning/PROJECT.md` line 55 (validated-requirement bullet) — the requirement's substance is
correct and must be preserved; only the glyph shorthand for "the git segment" changes. Replace
``(no `⎇` outside git repos,``
with
`(no git segment outside git repos,`
leaving the rest of that bullet (`no `#0`, no `↓0`/`↑0`) — v1.0 (Phase 2)`) exactly as-is.

`.claude/CLAUDE.md` line 109 — the not-a-repo detection rule is correct and must keep saying the same
thing; only the glyph shorthand goes. Replace
``hide the whole `⎇` segment.``
with
`hide the whole git segment.`

Do NOT add a new "Layout correction" note to PROJECT.md even though the file has that convention for
the Phase 2 and Phase 4 corrections — the scope for this task is the 9 enumerated lines only.
Do not touch PROJECT.md's other Requirements bullets, and do not renumber anything.

Commit as `docs(260827-02m): drop the branch glyph from PROJECT.md and CLAUDE.md`.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)" && fail=0
[ "$(cat .planning/PROJECT.md .claude/CLAUDE.md | grep -cF '⎇')" = "0" ] || { echo "FAIL: glyph still present"; fail=1; }
[ "$(grep -cF 'current_dir_name · current_git_branch' .planning/PROJECT.md)" = "1" ] || { echo "FAIL: PROJECT layout line 14"; fail=1; }
[ "$(grep -cF 'Opus 5 (high) · myproject · main* ≡ ↓2 ↑3 #2' .planning/PROJECT.md)" = "1" ] || { echo "FAIL: PROJECT example line 21"; fail=1; }
[ "$(grep -cF '- `branch` — current git branch, rendered as its own segment, only when inside a git repo' .planning/PROJECT.md)" = "1" ] || { echo "FAIL: PROJECT legend bullet 34"; fail=1; }
[ "$(grep -cF '(no git segment outside git repos, no `#0`, no `↓0`/`↑0`) — v1.0 (Phase 2)' .planning/PROJECT.md)" = "1" ] || { echo "FAIL: PROJECT requirement bullet 55 (substance must survive)"; fail=1; }
[ "$(grep -cF 'Detect not-a-repo by the status command failing (exit ≠ 0) and hide the whole git segment.' .claude/CLAUDE.md)" = "1" ] || { echo "FAIL: CLAUDE.md line 109"; fail=1; }
[ "$(grep -rlF '⎇' . --exclude-dir=.git | sed 's|^\./||' | grep -xE '\.claude/CLAUDE\.md|project-brief\.md|\.planning/PROJECT\.md' | wc -l | tr -d ' ')" = "0" ] || { echo "FAIL: a spec doc still carries the glyph"; fail=1; }
OOS=$(git status --porcelain -- .planning/milestones .planning/research .planning/RETROSPECTIVE.md .planning/STATE.md '.planning/quick/260826-wid-change-the-branch-symbol-to-a-dot-separa' kit tests)
[ -z "$OOS" ] || { echo "FAIL: out-of-scope touched -> $OOS"; fail=1; }
[ "$(grep -rlF '⎇' .planning/milestones .planning/research .planning/RETROSPECTIVE.md .planning/STATE.md '.planning/quick/260826-wid-change-the-branch-symbol-to-a-dot-separa' | wc -l | tr -d ' ')" = "33" ] || { echo "FAIL: historical records over-scrubbed"; fail=1; }
[ "$fail" = "0" ] && echo OK
    </automated>
  </verify>
  <done>All three live spec docs are glyph-free; PROJECT.md's layout line, example, legend bullet and validated-requirement bullet read in the dot-separated/plain form with their substance intact; CLAUDE.md:109 states the same not-a-repo rule without naming a glyph; historical records still at 33.</done>
</task>

<task type="auto">
  <name>Task 3: Add the `·` separator row to the README symbol legend</name>
  <files>README.md</files>
  <action>
The Symbol legend table currently has 12 data rows ordered to mirror left-to-right reading of line 1
(`Opus 5 (high)`, `myproject`, `main`, `*`, `≡`/`≢`, `↓2`, `↑3`, `#2`, then the line-2 tokens). The
`·` separator first appears between the model token and the directory token, so by that same
left-to-right rule the new row belongs immediately after the `Opus 5 (high)` row and immediately
before the `myproject` row.

Single Edit. Replace this line (occurs exactly once, currently line 19)
``| `myproject` | Basename of the working directory |``
with these two lines
``| `·` | Dim separator between segments — each segment is independent, so `myproject · main` is directory then branch |``
``| `myproject` | Basename of the working directory |``

Do not restructure the table, do not reorder or reword any of the 12 existing rows, and do not touch
the sample block at the top of README (it was already corrected by 260826-wid and is accurate). The
table grows from 12 data rows to 13.

Then run the full suite as the final gate. It is expected to be unaffected: no test in `tests/` reads
README (the only `README`/`CLAUDE.md` mentions there are shell comments and `emit` message strings),
so `220 checks, 0 failures` must hold exactly.

Commit as `docs(260827-02m): add the separator row to the README symbol legend`.
  </action>
  <verify>
    <automated>
cd "$(git rev-parse --show-toplevel)" && fail=0
[ "$(grep -cF '| `·` |' README.md)" = "1" ] || { echo "FAIL: separator row missing or duplicated"; fail=1; }
[ "$(sed -n '/^## Symbol legend/,/^## Install/p' README.md | grep -c '^| ')" = "14" ] || { echo "FAIL: legend row count (expect 14 = header + 13 data rows)"; fail=1; }
[ "$(grep -n '| `·` |' README.md | cut -d: -f1)" = "$(( $(grep -n '| `myproject` | Basename' README.md | cut -d: -f1) - 1 ))" ] || { echo "FAIL: separator row not immediately above the myproject row"; fail=1; }
[ "$(grep -cF '| `main` | Current branch (short SHA when detached) — the whole git segment is hidden outside a repo |' README.md)" = "1" ] || { echo "FAIL: existing branch row was disturbed"; fail=1; }
[ "$(grep -cF 'Opus 5 (high) · myproject · main* ≡ ↓2 ↑3 #2' README.md)" = "1" ] || { echo "FAIL: README sample block disturbed"; fail=1; }
SUITE=$(/bin/bash tests/run.sh 2>&1 | tail -1)
[ "$SUITE" = "220 checks, 0 failures" ] || { echo "FAIL: suite -> $SUITE"; fail=1; }
OOS=$(git status --porcelain -- .planning/milestones .planning/research .planning/RETROSPECTIVE.md .planning/STATE.md '.planning/quick/260826-wid-change-the-branch-symbol-to-a-dot-separa' kit tests)
[ -z "$OOS" ] || { echo "FAIL: out-of-scope touched -> $OOS"; fail=1; }
[ "$fail" = "0" ] && echo OK
    </automated>
  </verify>
  <done>README's Symbol legend has exactly one new `·` row sitting directly above the `myproject` row, the 12 pre-existing rows and the sample block are byte-unchanged, and `/bin/bash tests/run.sh` still reports `220 checks, 0 failures`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| *(none introduced)* | This plan edits Markdown prose only. No executable path, no parser input, no new process spawn, no I/O, no network, no dependency install. `kit/files/home/.claude/statusline.sh` — the only file that ever handles untrusted stdin — is explicitly out of scope and gated as untouched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260827-02m-01 | Tampering | Out-of-scope historical records (`.planning/milestones/**`, `.planning/research/**`, RETROSPECTIVE, STATE, the 260826-wid quick dir) silently rewritten by an over-broad find-and-replace | medium | mitigate | Every task's verify asserts `git status --porcelain` is empty over those paths AND that their glyph-bearing file count stays exactly `33`; the porcelain gate was proven non-vacuous with a throwaway probe before this plan was written |
| T-260827-02m-02 | Tampering | `.planning/PROJECT.md:55` / `.claude/CLAUDE.md:109` — a "de-glyph" edit that also deletes the requirement's substance | medium | mitigate | Both tasks assert the full reworded sentence verbatim (`(no git segment outside git repos, no #0, no ↓0/↑0) — v1.0 (Phase 2)` and the complete line-109 sentence), not merely glyph absence |
| T-260827-02m-03 | Tampering | `kit/files/home/.claude/statusline.sh` (the only security-relevant file in the repo) modified as collateral | high | mitigate | `kit` and `tests` are in the porcelain out-of-scope gate on all three tasks; a rendering change would additionally break the `220 checks, 0 failures` gate in Task 3 |
| T-260827-02m-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install task exists in this plan — nothing is installed, so the package-legitimacy gate does not apply |
</threat_model>

<verification>
Run after all three tasks are committed, from the repo root:

1. **All 9 stale lines gone, nothing else scrubbed:**
   `cat .claude/CLAUDE.md project-brief.md .planning/PROJECT.md | grep -cF '⎇'` -> `0` (was `9`)
2. **No live spec doc anywhere in the repo still carries the glyph:**
   `grep -rlF '⎇' . --exclude-dir=.git | sed 's|^\./||' | grep -xE '\.claude/CLAUDE\.md|project-brief\.md|\.planning/PROJECT\.md' | wc -l` -> `0`
3. **Historical records preserved (over-scrub guard):**
   `grep -rlF '⎇' .planning/milestones .planning/research .planning/RETROSPECTIVE.md .planning/STATE.md '.planning/quick/260826-wid-change-the-branch-symbol-to-a-dot-separa' | wc -l` -> `33`
4. **The 260826-wid history in STATE.md still quotes its task description verbatim** (positive
   assertion — the workflow is expected to rewrite the `Last activity` line as normal bookkeeping,
   but the Quick Tasks row must survive):
   `grep -cF 'change the branch symbol ⎇ to the dot separator' .planning/STATE.md` -> `>= 1`
5. **Out-of-scope tree clean** (evaluate before the quick-task workflow writes its own STATE row):
   `git status --porcelain -- .planning/milestones .planning/research .planning/RETROSPECTIVE.md '.planning/quick/260826-wid-change-the-branch-symbol-to-a-dot-separa' kit tests` -> empty
6. **Suite baseline holds:** `/bin/bash tests/run.sh` -> `220 checks, 0 failures`
7. **Diff is docs-only and small:** `git diff --stat HEAD~3..HEAD` lists exactly
   `project-brief.md`, `.planning/PROJECT.md`, `.claude/CLAUDE.md`, `README.md` and no other file.

Explicitly NOT gates here (same reasoning as 260826-wid): `tests/render-fixtures.sh`'s repo-vs-installed
diff still reports a difference because `~/.claude/statusline.sh` is a stale regular-file copy, and
refreshing it is the user's environment, out of scope. This plan changes no rendering, so no fixture
baseline moves.
</verification>

<success_criteria>
- [ ] Glyph count across the three live spec docs: `9` -> `0`
- [ ] `project-brief.md` lines 8, 16, 17, 30 rewritten; file still 36 lines
- [ ] `.planning/PROJECT.md` lines 14, 21, 34, 55 rewritten with requirement substance intact
- [ ] `.claude/CLAUDE.md` line 109 states the same not-a-repo rule without naming a glyph
- [ ] README Symbol legend has exactly one new `·` row, directly above the `myproject` row; 12 pre-existing rows byte-unchanged
- [ ] `/bin/bash tests/run.sh` reports `220 checks, 0 failures`
- [ ] 33 historical glyph-bearing files untouched; `kit/` and `tests/` untouched; `kit/spec.yaml` not version-bumped
- [ ] Three atomic commits, docs-only, no file outside the four in scope
</success_criteria>

<output>
Create `.planning/quick/260827-02m-drop-the-branch-glyph-from-the-three-liv/260827-02m-SUMMARY.md` when done.
</output>
